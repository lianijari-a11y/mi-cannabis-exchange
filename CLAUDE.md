# MI Cannabis Exchange — Build Brief for Claude Code

Read this before writing code. This is a **separate business from LaneMatch** (a freight-matching platform built elsewhere) — do not reuse its compliance model, only its engineering conventions.

---

## 1. What this is

A wholesale marketplace connecting five Michigan-licensed cannabis roles:

- **Grower** — cultivates flower, posts inventory
- **Processor** — makes concentrates/edibles/vapes, posts inventory
- **Retailer** — buys wholesale inventory to sell at retail
- **Broker** — the platform's own intermediary; mediates every deal
- **Transporter** — Michigan Secure Transporter-licensed carrier; hauls an accepted deal from Grower/Processor to Retailer once the retailer has accepted the seller's invoice

A Grower, Processor, or Broker can post an inventory listing. It becomes **immediately visible** to every Retailer (a bulletin-board model, not a platform-assigned match). A Retailer can accept a listing as posted, or counter-offer on price and/or payment terms (cash vs. 30-day terms are both negotiable). The Grower/Processor can then accept, reject, or counter back — this can go back and forth an arbitrary number of rounds. The Broker sees **every** negotiation across the whole platform, on both sides, in real identity — Retailer and Grower/Processor never see each other's identity, even after a deal closes.

Once a deal is accepted, fulfillment kicks off: the seller uploads an invoice, the retailer reviews it and accepts, which triggers transporter selection (the platform's preferred transporter is pre-selected, but the retailer can pick any other approved transporter) and creates a trackable shipment. See §3's 4th decision and §4's Transporter section.

## 2. The compliance wrinkle — read before touching identity/broker code

This was flagged, not silently decided, when the product shape was defined:

- Michigan requires **METRC seed-to-sale manifests** for any real transfer of licensed product, and a manifest names actual licensees (license number, business name) on both sides.
- It is **not established** that "broker" is a recognized standalone license category for wholesale cannabis transfers the way it is for real estate or (per FMCSA) freight — the Marihuana Regulation and Taxation Act (MRTA) and Medical Marihuana Facilities Licensing Act (MMFLA) license Growers, Processors, Retailers, Secure Transporters, Safety Compliance Facilities, Microbusinesses, etc. Whether an entity that never holds/transports product but sits between every deal and knows both identities needs its own license, or needs to operate under a Retailer/licensed-transporter's authority, is a real open legal question.
- **Do not resolve this in code or in this document.** The app is built so the Broker role has full visibility (matching the confirmed product decision), but the actual legal structure the platform owner operates under — a licensed entity, a partnership with one, a pure software-only "provides negotiation tooling, a licensed transporter completes the physical/METRC transfer" model, etc. — needs review by a Michigan cannabis attorney before this goes live with real product. Flag this to the human if asked to build real METRC integration or anything that implies the platform itself is executing a transfer.
- Until that's resolved, treat `Deal` records in this app as **negotiated agreements**, not completed legal transfers — the actual license-to-license transfer and METRC manifest happen outside this software, initiated by the Broker.
- The same caveat applies to `Shipment`/invoice records added for fulfillment: an uploaded invoice, an accepted invoice, and a transporter's "delivered" status update are **operational tracking inside this app**, not a substitute for the real METRC manifest and physical secure-transport paperwork a licensed transporter is separately obligated to keep. Don't build anything that implies this app's shipment record *is* the legal transfer record.

## 3. The three product decisions that shape the architecture

1. **Blind marketplace, identity never revealed between Grower/Processor and Retailer** — not before a deal, not after. Every API endpoint scoped to those two roles must never join to or expose the counterparty's `User` row. Enforce this at the query layer, not just in the UI — the UI hiding a field is not enough if the API response includes it.
2. **Broker sees every deal, platform-wide** — Brokers are not independent competing agents each with their own book of business; every broker user can see every active negotiation and every closed deal, with both real identities. This models the Broker as the marketplace operator's own staff/agents, not third-party brokers competing on the platform.
3. **First-to-accept / no platform-assigned matching** — a listing is visible to every Retailer simultaneously; nothing in the matching/feed logic should rank or gate listings based on the platform's own business interest. (This mirrors LaneMatch's load-board reasoning even though the legal basis here is different — it's just good practice for a bulletin-board model.)
4. **Transporter identity is real, not anonymized, and the retailer picks the transporter.** Confirmed with the human when the fulfillment flow was added: once a deal moves to shipping, the Transporter sees both the Grower/Processor's and Retailer's real business name and address (same trust tier as the Broker — needed for pickup/delivery logistics and matches how MI's Secure Transporter licensing already implies manifest-level detail on both parties). This does **not** loosen decision #1 — Grower/Processor and Retailer still never see *each other's* identity; they each separately see the Transporter's real identity, because a shipping carrier isn't the counterparty the blind-marketplace rule is protecting. Transporter selection happens when the retailer accepts the seller's invoice: the platform's preferred transporter (`User.preferredTransporter`, admin-set) is pre-selected, but any other approved Transporter account can be chosen instead — this is a self-serve role like the others, not a closed vendor list.

If you find yourself building something that would let the platform pick which Retailer gets a listing, or would let a Grower/Processor and Retailer see each other's identity, stop and flag it.

## 4. Roles and core functionality

### Grower / Processor
- Profile: business name, state license number, license type, license verification status (set by Admin)
- Post inventory listings: strain/product name, category (flower / concentrate / vape / edible / pre-roll / other), THC%, quantity + unit (lbs for flower/processed weight, liters for liquid concentrate/oil), price per unit, payment terms offered (cash / net-30 / negotiable), expiration, photos and videos
- **AI-assisted quick upload**: sellers post multiple times a day, often for very different products, so the listing form has a "paste your notes, structure with AI" step (`lib/ai-listing.ts`, Anthropic API) that turns raw pasted text into a pre-filled draft of the fields above. The seller always reviews and submits the draft themselves — the model drafts, it never posts a listing on its own (same AI-governance posture as the rest of this brief: recommend, don't act autonomously).
- View incoming offers/counters on their listings, respond (accept / reject / counter)
- Listings list, sortable/filterable by status
- **Post-acceptance**: once a deal is accepted, upload an invoice (image/PDF) against it. The retailer's side stays anonymized (still shown only as their anon handle) until/unless a shipment exists, at which point the seller also sees the real Transporter's name and the shipment status timeline (see §3 decision #4)

### Transporter
- Profile: business name, state license number/type (Secure Transporter), license verification status (set by Admin) — same self-serve signup + Admin approval gate as Grower/Processor/Retailer
- Dashboard: every shipment assigned to them, with the deal's real Grower/Processor and Retailer business name + address (needed for pickup/delivery), product/quantity/price/terms
- Update shipment status one step at a time: `assigned → picked_up → in_transit → delivered`, optionally with a note each step; delivery requires uploading a proof-of-delivery photo/PDF
- Does **not** see the negotiation history or listing feed — only shipments assigned to them

### Retailer
- Browse a feed of **all active listings** platform-wide, from every Grower/Processor/Broker poster — poster shown only as an anonymous handle (e.g. "Grower #A17"), never a real business name
- Filter/sort by category, strain, THC%, price, terms
- **Presentation is standardized**: every listing renders through one shared product-card component (`components/retailer/listing-card.tsx`) regardless of how rough the seller's original upload was — same layout, same badge placement, same typography — so the feed reads as one consistent catalog, not a pile of ad hoc posts
- Accept a listing as-is, or counter-offer (price and/or terms) — negotiation is a back-and-forth thread, anonymized on both sides
- See their own open/closed negotiation threads
- **Post-acceptance**: review the seller's uploaded invoice, then accept it and pick a transporter (preferred one pre-selected, any other approved transporter selectable) — this creates the shipment. From here the seller stays anonymized as always, but the chosen Transporter's real name and shipment status timeline are shown

### Broker
- Platform-wide dashboard: every active negotiation and every closed deal, in real identity on both sides (business name, license info)
- No action needed to "join" a deal — visibility is automatic and total, per decision #2 above
- Deal/negotiation status at a glance; this view is explicitly mobile-first (per the original brief: "broker sees all interactions on his phone app")
- Deal cards also show invoice/shipment status and the assigned transporter once fulfillment starts

### Admin
- License verification queue: review a submitted license number/type per user, approve/reject — this now gates Transporters too, not just Grower/Processor/Retailer
- User list/status
- Toggle which Transporter account is the platform's preferred default (`User.preferredTransporter`)

## 5. Negotiation state machine

One thread per (listing, retailer) pair. Each round is one of: `offer` (initial, implicit from the listing itself), `counter` (either side), `accept`, `reject`. A thread is `open` until an `accept` (→ creates a `Deal`) or a `reject` from either side, or the listing/thread expires. Either side can counter as many times as they want while open. All of this is visible to the Broker in real identity; visible to the two negotiating parties only in anonymized form.

Post-acceptance fulfillment is a second, simpler state machine on the `Deal`: no invoice → invoice uploaded → invoice accepted (creates the `Shipment`). The `Shipment` itself steps one-way through `assigned → picked_up → in_transit → delivered`, advanced only by the assigned Transporter (`lib/shipments.ts` `advanceShipmentStatus`, using `NEXT_SHIPMENT_STATUS` — no skipping ahead or reverting).

## 6. Data model

See `web/prisma/schema.prisma` — `User` (role: grower/processor/retailer/broker/transporter/admin, license fields, `address`/`city`/`state`/`zip` for grower/processor/retailer, `preferredTransporter` flag), `Listing`, `ListingMedia`, `OfferThread` + `OfferRound`, `Deal` (now with `invoiceUrl`/`invoiceUploadedAt`/`invoiceAcceptedAt`), `Shipment` + `ShipmentEvent`. The anonymization boundary is enforced in `web/src/lib/listings.ts` and `web/src/lib/offers.ts` — those modules are the only places allowed to query across the Grower/Processor ↔ Retailer boundary, and they must never return the counterparty's real `User` fields to a non-broker, non-admin, non-assigned-transporter caller. `web/src/lib/shipments.ts` owns the invoice/shipment logic and is the one place allowed to resolve a Deal's real seller/retailer identity out to the assigned Transporter.

## 7. Stack

Next.js 16, Prisma (SQLite for local dev — zero external setup; swap to Postgres via `DATABASE_URL` when a hosted DB is provisioned, same as LaneMatch's Supabase setup), NextAuth v5 (credentials), Tailwind v4, TypeScript. Local disk storage for photo/video uploads during development (`public/uploads/`) — swap for real object storage (S3/Supabase Storage/Cloudinary) before production; note this in code as a follow-up, don't silently pretend it's production-ready. Anthropic API (`ANTHROPIC_API_KEY`) powers the AI quick-upload draft feature (`lib/ai-listing.ts`) — unset by default, feature falls back to a plain "fill in by hand" message.

## 8. Deferred — circle back before real launch

- Real object storage/CDN for media (currently local disk)
- `ANTHROPIC_API_KEY` not set by default — AI quick-upload structuring is a no-op fallback until the platform owner adds a key
- METRC manifest generation/integration — **blocked on the legal question in §2**
- SMS/push/email notifications — §10 added in-app-only notifications (offer viewed, countered, accepted/rejected); Brokers still get nothing pushed to them outside the app
- Payment/invoicing automation for net-30 terms (this app tracks the *agreed* terms, it doesn't move money)
- Real license verification (currently a manual Admin approve/reject, no FMCSA-SAFER-style automated check exists for MI cannabis licenses the way it does for DOT authority)
- Legal review of the Broker's licensing/liability structure (§2) — do not build around an assumption here without that review
- No live GPS/location tracking for shipments yet — status is transporter-reported (assigned/picked up/in transit/delivered), not automatic. A real ELD/GPS integration is a natural Phase 2, same spirit as LaneMatch's Motive/Samsara integration
- No address validation/geocoding on the signup address fields — free text for now
- Invoice/POD documents are stored on local disk like listing media — same production storage caveat applies
- `SplitContract` (§10) fulfillment isn't wired into `Shipment`/Transporter — settled toll-processing contracts have no tracked pickup/transport step and no METRC-equivalent trail, same underlying gap as §2

## 9. Things to flag back to the human, not decide silently

- Anything implying the platform itself completes a legal cannabis transfer (vs. facilitating negotiation up to an agreed `Deal`)
- Any change that would let Grower/Processor and Retailer see each other's identity
- Any change that would let the platform algorithmically choose which Retailer gets a listing
- Legal template content for any agreement drafted in-app — should come from an actual MI cannabis attorney
- **Broker commission exists now — see §11.** This section originally said the platform never takes a commission; that stance was deliberately reversed on 2026-08-16 with the human's explicit confirmation after being shown the legal tradeoff (see §11). Left here, struck through in spirit rather than deleted, so nobody assumes the no-commission rule is still in force just because §9 is the "flag, don't decide silently" section — the decision *was* surfaced, and the human chose to proceed. `SplitContract` settlements still take no platform cut — that piece of the original stance is unchanged.

## 10. Market intelligence, alerts, and toll-processing (added 2026-08-16)

Built after the initial build brief, evaluated against a reference prototype shared by the human — see §9's note on what was deliberately *not* ported from it.

- **Market Pulse** (`lib/market.ts`) — average asking price by category across active listings, shown on the retailer feed and every seller dashboard. Computed live from this platform's own data, not a third-party report.
- **Sold comps** (`lib/market.ts`) — recent closed `Deal`s in the same category, shown on a listing's detail page.
- **Seller rating** (`lib/market.ts`) — a star score derived from what fraction of a seller's past deals actually reached `delivered`. New sellers with no closed deals get no score rather than a fabricated default. On the retailer side this is resolved by `sellerRatingForListing(listingId)`, which never exposes the real `sellerId` to the caller — same anonymization boundary as everywhere else.
- **License expiration alerts** (`lib/admin.ts`) — Admin dashboard flags any licensed user (`User.licenseExpiry`) expiring within 90 days, or already expired.
- **Watchlist** — a retailer can track a listing without making an offer on it (`Watchlist` model, `lib/watchlist.ts`), with a dedicated `/retailer/watchlist` page.
- **Offer view tracking + in-app notifications** — `OfferThread` now tracks a view count and last-viewed timestamp per side (`lib/offers.ts`'s `recordThreadView`); the *other* party gets a throttled (max once per 10 min) notification that their offer was viewed, plus notifications on counter/accept/reject. This is the achievable slice of §8's deferred "SMS/push to Brokers" item — **in-app only**, no SMS/push/email, surfaced via a bell icon in `components/notification-bell.tsx` on every portal.
- **Auto-generated invoice** (`lib/invoice.ts`) — an invoice used to require the Grower/Processor to manually upload a file before the Retailer could accept and move to fulfillment; now one is auto-generated the instant a `Deal` is created (`Deal.invoiceAutoGenerated`), rendered at `/deal/[id]/invoice`, so the Retailer is never blocked. The seller can still upload their own file to override it — same anonymization rule as everywhere else: each party's own side is real, the counterparty is only ever shown as their `anonHandle` (Broker/Admin see both real). **Real accounting-platform integration is intentionally not built yet** — the human has a separate, more mature project (`ai-ledger`, an AI accounting/tax platform with a real `Invoice`/`Customer`/`ApiKey` data model) they want this to eventually hand off to for real payment processing via a cannabis-friendly bank. That integration is deferred until (a) `ai-ledger`'s own concurrent-session lock clears — see its CLAUDE.md §9, two sessions collided there once already — and (b) its actual invoice-creation API surface is understood. The in-app auto-generated invoice is meant to be the always-working default; the `ai-ledger` connection becomes a selectable option layered on top, not a replacement.
- **Toll-processing / split contracts** (`lib/split-contracts.ts`) — a Processor can source a Grower's raw listing not for a flat price, but for a % share of the finished product's eventual value (`SplitContract` model). Flow: Processor browses `/processor/sourcing` (active Grower listings only) and proposes a split; Grower accepts/rejects from their listing detail page (same anonymized-handle pattern as regular offers); once accepted the listing closes like a normal `Deal` would; Processor logs a yield once processing is done, then either party keys in a settlement value and the split is computed — **the platform takes no cut of this**, unlike §11's `Deal` commission below. Deliberately simpler than the price/terms negotiation state machine: single propose → accept/reject, no counter-offer loop. Physical pickup/transport for a settled contract is coordinated directly between Grower and Processor for now — it is **not** wired into the `Shipment`/Transporter flow yet, which means (like the rest of this app, see §2) it produces no METRC-equivalent paper trail. That's a deferred follow-up, not a solved problem — add it to §8 if picked up.
- **Product acceptance after delivery** (`lib/commission.ts`'s `acceptProduct`/`rejectProduct`) — once a `Shipment` reaches `delivered`, the Retailer gets an explicit accept/reject step (`Deal.productStatus`). Accepting shows a "this transaction is final, no in-app dispute process" notice before it's confirmed, and is the trigger that computes any commission amount owed (§11). Rejecting is only *recorded* — the app does not run a refund/return/dispute workflow; that's real-world, off-platform, same caveat as §2's transfer language.

## 11. Broker commission (added 2026-08-16 — reverses §9's original no-commission stance)

**This was a confirmed, explicit reversal, not a silent decision.** The human was shown the exact tradeoff — commission is the pattern that makes the platform look like a broker taking a cut of a cannabis transfer, sharpening §2's still-open "is broker a recognized MI license category" question rather than staying clear of it — and chose to proceed anyway. If a future session is asked to touch commission logic and something about it seems inconsistent with the blind-marketplace/no-transfer posture elsewhere in this doc, that's expected: this section is a deliberate carve-out, not an oversight to "fix" back to the original stance.

- **Any broker can set commission terms on any `Deal`** (`lib/commission.ts`'s `setCommission`) — same platform-wide visibility as everything else a broker sees (decision #2). Terms: a **rate from 0-100%, entirely the broker's own judgment call per deal** — there is no platform-default or preset band; the only ceiling is the mathematical one (can't take more than the deal is worth). Payer is also broker-chosen: grower pays, retailer pays, or split by a broker-chosen grower/retailer percentage.
- **The dollar amount is computed once, at product acceptance** (§10's `acceptProduct`), not before — a broker can freely edit rate/payer terms any time before that point without anything downstream depending on the old number. After computation, editing terms does *not* silently recalculate — see the comment in `setCommission`.
- **Tracked, not processed.** `Commission.status` goes `pending` → `paid` via a manual broker (or Admin) action (`markCommissionPaid`) — this app never moves money itself, same as every other dollar figure here (§8). No card processing, no ACH, nothing programmatic.
- Real payment rails (the human's separate `ai-ledger` project, connected to a cannabis-friendly bank) are the eventual path for actually *collecting* commission — same deferred-integration status as §10's invoice section.

## 12. License registry auto-populate + auto-approval, and a relaxed posting gate (added 2026-08-16)

Two related changes, both requested by the human directly:

- **The state license number lookup is real now, not just admin-manual review.** `LicenseRegistry` (`web/prisma/schema.prisma`) holds ~3,300 rows imported from the state of Michigan's own CRA license exports (5 files: Class B Grower, Class C Grower, Processor, Retailer, Secure Transporter — no Broker category exists in the state data, which is exactly §2's still-open question, not resolved here) via `scripts/parse-license-csvs.mjs` + `scripts/import-license-registry.mjs`. At signup, entering a license number triggers `lib/license-registry.ts`'s `lookupLicense` (exposed to the client via `/api/license-lookup` for live autofill, and re-run independently server-side in `signup/actions.ts` — the client's autofill is never trusted for the actual decision) which auto-fills business name and address, and — **only if the state's own status for that record is exactly `"Active"`** — sets `licenseVerification: "approved"` immediately instead of leaving it `"unverified"` for the Admin queue. Anything else (no match, `"License Void"`, `"Closed - Suspended"`, `"Revoked"`) falls back to the existing manual Admin review, unchanged.
- **This is tracked as a distinct thing from a real Admin approval, on purpose.** `User.licenseAutoMatched` is `true` only when the license was auto-approved this way. The honest caveat, stated plainly rather than glossed over: matching a license *number* is not proof that the person typing it in actually owns or represents that license — MI license numbers are largely public record. Auto-matched accounts show a distinct "auto-matched" badge in Admin's user list (`/admin`) precisely so a human retains an audit trail and can revoke one if something looks off, even though it wasn't blocking anyone up front. Don't quietly turn this into "auto-matched == verified" anywhere in the UI — the badge exists so nobody makes that mistake.
- **Phone-number enrichment from the platform owner's own lead list — done.** Once the Lead Directory (MI) list was available as a real CSV export (see §20's `Lead` import), `scripts/enrich-license-registry-phone.mjs` ran a business-name-matching merge pass: normalizes both sides' names (strips punctuation and entity suffixes like LLC/Inc), builds a name→phone map from the 530-row CRM export, and fills `LicenseRegistry.phone` only where it was still null and only for `grower_b`/`grower_c` rows (the Lead Directory list is specifically the platform owner's grower-solicitation list, so matching against processor/retailer/transporter rows would risk false-positive name collisions across unrelated business types). Matched and filled **581 of 1,542 phone-less grower rows**. Best-effort, not a guarantee — a name match isn't proof the phone is current or even the right branch of a multi-location business. **Not yet surfaced anywhere in the UI** — `lib/license-registry.ts`'s `lookupLicense` (used at signup) doesn't select or return `phone` yet; wiring it into signup autofill is a natural follow-up but wasn't part of what was asked here.
- **Monthly refresh has now actually been run once**, via `scripts/refresh-license-registry.mjs` (a standalone-script twin of `lib/license-registry-import.ts`'s `importLicenseRegistryCsv`, needed because `tsx` can't resolve the `"server-only"` import outside the Next.js build) — same 5 state files, category auto-detected per row from the state's own "Record Type" text so files can be passed in any order. Row count held steady at 3,302 (same underlying dataset upserted in place, not a duplicate load). This was run from the CLI directly, not through the `/admin/data-uploads` UI — the UI path exercises the same underlying logic but wasn't itself clicked through this time.
- **The listing/offer posting gate was relaxed** (`lib/seller-actions.ts`, `lib/retailer-actions.ts`, `lib/processor-actions.ts`) — previously `licenseVerification !== "approved"` blocked posting/offering entirely while pending review; now only `licenseVerification === "rejected"` blocks it, so a normal `"unverified"` (pending Admin, or pending a registry match that didn't auto-approve) account can still post listings and make/respond to offers immediately. **This was not extended to transporter shipment assignment** (`lib/shipments.ts` still requires `"approved"` to be selected as a deal's transporter or show up as the default) — that's a physical-dispatch decision, not what was asked to loosen, and it's a different risk tier than posting inventory or negotiating a price.

## 13. Sales Rep role, shipment scheduling, freshness nudges, and a real post-rejection workflow (added 2026-08-16)

A batch of workflow requests, walked through and implemented together:

- **New role: Sales Rep** (`sales_rep`, `lib/sales-actions.ts`). Same trust tier as Broker — sees real Grower/Processor identity, not a licensed cultivator themselves (not in `LICENSED_ROLES`/`ADDRESS_ROLES`). Solicits an inventory list from a grower/processor off-platform, then builds it into a listing at `/sales/listings/new` using a seller search (`SellerPicker` component, `searchAssistableSellers`) — the listing posts under the **chosen seller's own identity** (`Listing.postedById`), with `Listing.createdBySalesRepId` as a pure audit trail, never shown to buyers. **Admin got the identical capability** (`/admin/listings/new`, same underlying `handleCreateListingAsAssistant`) per an explicit follow-up ask — one shared function, two entry points, gated separately by `requireRole("sales_rep")` / `requireRole("admin")` in each page's own search-action file.
- **Growers can still post their own menus directly, unchanged** — Sales Rep/Admin assistance is additive, not a replacement path.
- **Shipment scheduling** (`Shipment.scheduledPickupAt/scheduledDeliveryAt/growerAcceptedSchedule/retailerAcceptedSchedule`, `lib/shipments.ts`'s `proposeShipmentSchedule`/`acceptShipmentSchedule`). The transporter proposes a pickup/delivery window from their shipment detail page; the grower and retailer each accept it separately from their own deal panel (`ScheduleAcceptPanel`). `advanceShipmentStatus` now **blocks the assigned → picked_up transition** until both have accepted, but only if a schedule was ever proposed — deals that skip scheduling entirely aren't gated, so this stays backward compatible. Re-proposing a new time resets both acceptance flags.
- **Retailers get a "Not interested" dismiss button** (`ListingDismissal` model, `lib/dismissals.ts`, `DismissButton` component) alongside Watchlist — hides a listing from that retailer's own feed only, reversible, never affects what anyone else sees. This is a per-viewer feed filter, not a change to the listing itself, so it doesn't touch decision #3's bulletin-board rule.
- **Listing freshness nudges** (`Listing.lastConfirmedAt`, `lib/listings.ts`'s `confirmListingFresh`). Sellers post multiple times a day and stale listings degrade the feed, so: the retailer feed flags anything not confirmed available in 3+ days ("Not confirmed in Nd"), offers a "Recently confirmed available" sort, and the seller's own dashboard/listing-detail page shows the same staleness plus a one-click "Still available? Confirm" button that bumps `lastConfirmedAt`. **No actual push/email reminder** — there's no background-job infra in this app (same gap as §8's deferred SMS/push item), so this is informational-on-view only, not a proactive nudge sent to anyone.
- **Post-rejection now has a real in-app workflow**, not just a recorded dead-end (`ProductRejection` model, `lib/commission.ts`'s `chooseReturn`/`proposeRejectionCounter`/`acceptRejectionCounter`/`requireReturnInsteadOfCounter`). Rejecting delivery now **requires a reason**. The retailer then picks one of two paths: send the product back outright, or propose a revised price reflecting its condition. The grower either accepts the revised price (the deal closes at the new `finalPrice`) or insists on the return. Single round, no back-and-forth counter loop — same "deliberately simpler" scope cut as `SplitContract`. **This still is NOT a refund/payment/return-shipping engine** — the resolution is tracked, the actual physical return and any money movement stay off-platform, same caveat as §2/§8 everywhere else in this doc. Don't let "there's now a workflow" get read as "this app handles returns."

## 14. Negotiated rejection fee (added 2026-08-16)

Distinct from §11's broker commission on purpose: this is a term the **buyer and seller negotiate themselves**, during their own offer/counter rounds (`OfferRound.rejectionFeeRate`/`rejectionFeePayer`) — not something a broker sets after the fact. Confirmed with the human: the fee is a % of deal value, freely negotiated like price (no platform-wide default rate), while the *payer* defaults to a 50/50 split if the parties never discuss it (either side can propose grower-pays/retailer-pays/split instead, same as they'd counter on price). Resolved onto `Deal.rejectionFeeRate`/`rejectionFeePayer` at acceptance the same way `finalPrice` is. The actual dollar amount is computed at rejection time (`lib/commission.ts`'s `rejectProduct`, stored on `ProductRejection.feeAmount`/`feeGrowerOwes`/`feeRetailerOwes`) since that's the event that triggers it — a rate of 0 (never negotiated) means no fee at all. Tracked via `feeStatus`/`markRejectionFeePaid` (broker or admin marks it settled), same "this app never moves money" posture as Commission (§8/§11) — no card processing, no ACH.

## 15. Cross-role ratings (added 2026-08-16)

Extends the existing seller rating (§10's `sellerRating`) to every role, same honesty rule throughout: **no fabricated default for someone with no history yet** — a user with zero relevant deals/shipments shows no score, not a made-up middling one.

- **Retailer rating** (`lib/market.ts`'s `retailerRating`) — this app doesn't process real payment (§8), so "pays on time" is proxied by how fast a retailer accepts the invoice once it's on file (the fastest real signal actually available), combined with how often their deals end in a rejected delivery. Surfaced to the seller, per-thread, via `retailerRatingForThread` — resolves through a `threadId` so the seller-side caller never has to be handed `retailerId` directly, same anonymization-boundary pattern as `sellerRatingForListing`.
- **Transporter rating** (`transporterRating`) — % of shipments delivered on/before their scheduled window (§13), among shipments that actually had one proposed; falls back to plain delivery-completion rate when no schedule was ever used, so adopting §13's scheduling feature isn't a prerequisite for having a score at all.
- All three (seller/retailer/transporter) shown together in Admin's user list (`/admin`) as a single "Rating" column — the one place every role's score is visible side by side.

## 16. Market pricing trend (added 2026-08-16)

Extends §10's Market Pulse (average asking price by category) with a trend direction — `lib/market.ts`'s `priceTrend` buckets **closed Deals** (actual sold prices, not asking prices) by week and compares the oldest to the newest bucket in an 8-week window. Shown as a small ↑/↓/→ arrow with a % change next to each category in the existing `MarketPulse` widget (retailer feed + every seller dashboard) — deliberately just numbers, no charting library pulled in for one widget.

## 17. Explicitly deferred, not silently decided — flagged mid-build (2026-08-16)

The human asked for admin-driven listing approval plus the option to make a listing exclusive to one specific buyer. **The second half was not built.** Restricting a listing to one retailer, chosen by the platform (Admin/Broker) rather than by the seller themselves, is exactly what decision #3 and its "if you find yourself building something that would let the platform pick which Retailer gets a listing, stop and flag it" line describe — this was flagged back to the human instead of built, per that instruction, and the human's response was to not proceed for now. If picked back up: the distinction that matters is *who* makes a listing exclusive — the seller restricting their own inventory to a buyer they already have a relationship with doesn't touch the bulletin-board rule; the platform choosing a winner does. Admin listing pre-approval (a moderation gate before anything goes live at all) is a separate, likely fine, question that also hasn't been built yet — it stalled alongside the exclusivity question rather than being decided independently.

**Update, later the same day (2026-08-16): re-asked, and reversed.** The human raised admin-controlled listing exclusivity a second time. Flagged again, explicitly, with the same decision #3 conflict spelled out — and this time the human confirmed they want it built. See §18's listing distribution entry for what actually shipped. Left this paragraph as written above rather than edited, same "show the reversal, don't erase the original stance" posture as §11.

## 18. Second feature batch — sales/market-data tooling, wanted board, listing distribution, and a non-circumvention agreement (added 2026-08-16)

A large batch of follow-up requests, walked through and implemented together. Grouped here by theme rather than chronologically.

**Market/license data ingestion**
- **Monthly Michigan CRA "Monthly Report" upload** (`lib/monthly-report-parser.ts`, `lib/monthly-report.ts`) — Admin uploads the state's own monthly `.docx` report; `mammoth` extracts text, and an anchor/heuristic-based parser (not fixed line offsets — the source docx's table layout varies enough between months and between the Medical/Adult-Use tables that a fixed offset silently desynced and dropped a whole region one month) pulls out average price by category and active-license counts by region. Stored on `MonthlyMarketReport`, exposed via `stateMarketTrend()`. This is the state's own published aggregate data, not derived from this platform's own deals — distinct from §16's `priceTrend`, which *is* this platform's own closed-deal history.
- **License registry monthly refresh** (`lib/license-registry-import.ts`) — the same CSV format §12's one-time import used, wrapped as a re-runnable Admin upload so the registry stays current as the state re-publishes it monthly, not just a one-time seed.

**Wanted board — buyer-initiated requests**
- Retailers can post what they need when nothing in the feed matches (`ProductRequest`/`ProductRequestResponse` models, `lib/product-requests.ts`, `/retailer/requests`). Visible to every Grower/Processor/Broker (`/grower/requests`, `/processor/requests`, `/broker/requests`) the same way a listing is visible to retailers, just mirrored — **same blind-marketplace boundary as everywhere else**: the retailer shows only as their `anonHandle` to suppliers, and a supplier shows only as their `anonHandle` back to the retailer, except to Broker/Admin (real identity, decision #2). A supplier's response can optionally link one of their own active listings, so the retailer clicks through into the *existing* negotiation machinery (`getOrCreateThread`/`addOfferRound`) instead of this app inventing a second deal-making state machine — deliberately no back-and-forth loop on the response itself, same "v1 scope cut" posture as `SplitContract`.

**Listing distribution — a confirmed reversal of decision #3**
- `Listing.visibility` (`"all"` | `"exclusive"`) + `exclusiveRetailerIds` (`lib/admin.ts`'s `setListingVisibility`, `/admin/listings`). Default is `"all"`, preserving the original bulletin-board behavior for every existing and new listing unless Admin explicitly restricts one. **This is the reversal §17 flagged and the human then confirmed** — read that section first if touching this code; it's a deliberate carve-out into decision #3, not a bug to "fix" back to always-open. `activeListingsFeed`/`getListingAnonymized` (`lib/listings.ts`) both gate on it. Distribution restriction is **Admin-only** in this build (the human specifically chose "Admin can restrict a listing to chosen retailers" over a seller-controlled variant when asked) — don't extend it to sellers themselves without re-confirming, since that's a materially different, smaller deviation from decision #3 than what was actually approved.

**Experimental photo redaction — explicitly not guaranteed**
- `lib/media-redaction.ts`, wired into `lib/media.ts`'s `saveMediaFile`. Uses Claude's vision input to spot an apparent business logo or visible contact info in an uploaded listing photo, then blacks out the reported region with `sharp`. **The human was told directly, before building this, that a missed detection leaks real identity straight through the blind-marketplace boundary this app's core promise depends on** — and chose to build the automatic-redaction version anyway over a detect-and-warn-only alternative. Treat this as what it is: best-effort, not a guarantee. `ListingMedia.redactionAttempted`/`redactionRegionsFound` surface an "Auto-redacted — review" badge on the seller's own listing photos so they still have a chance to catch a miss themselves. Falls back to a no-op if `ANTHROPIC_API_KEY` isn't set, same posture as `lib/ai-listing.ts`.

**Non-circumvention agreement**
- `AgreementAcceptance` model, `lib/agreements.ts`, gated in `lib/dal.ts`'s `requireRole` for every marketplace-participant role (grower/processor/retailer/broker/transporter/sales_rep — not Admin, who isn't a party to it). A user who hasn't accepted the current version is redirected to `/agreements/non-circumvent` before reaching their portal. **The clause text itself is a draft placeholder, not reviewed by a Michigan cannabis attorney** — same caveat as every other legal-adjacent surface in this app (§2, §9). Bump `NON_CIRCUMVENT_VERSION` to force everyone to re-accept if the real text ever replaces the placeholder.

**Sales Rep commission tracking**
- Extends §13's Sales Rep role: `User.salesRepCommissionRate` is a standing rate Admin sets per rep (`/admin/sales-reps`, `setSalesRepCommissionRate`) — unlike §11's Broker commission, a rep doesn't negotiate this per deal. `SalesRepCommission` is created automatically at product acceptance (`lib/commission.ts`'s `acceptProduct`) for any `Deal` whose listing has `createdBySalesRepId` set, snapshotting the rep's rate at that moment so a later rate change never retroactively touches an already-computed commission — same "computed once" posture as Commission. Reps see their own sales/earnings at `/sales/earnings`; tracked-not-processed, `pending` → `paid` via Admin action, same as every other dollar figure in this app (§8).

**Smaller additions bundled into this batch**
- **Retailer search** (`/retailer` — strain/product name and category, plain substring filter on the already-fetched feed, no new index needed at this data volume).
- **Negotiated offer expiration** (`OfferThread.expiresAt`, optional, retailer's own choice when opening a thread; swept lazily like `expireStaleListings`, no background job).
- **Transporter invoicing / transport fee** (`Shipment.transportFeeAmount/transportFeePayer/transportFeeSplitGrowerPct/transportInvoiceUrl/transportFeeStatus`, `lib/shipments.ts`) — the transporter invoices whichever party is paying for transport, tracked the same "never moves money" way as Commission.
- **POS integration scaffolding** (`lib/pos-integration.ts`) — architecture for pushing an accepted deal to Dutchie/Treez/Flowhub/Cova, but **every adapter is a stub**: this platform has no certified API partnership with any of the four, so a "sync" only ever logs an honest `PosSyncLog` row explaining that nothing actually happened. Do not wire a real adapter without verified partner credentials for that specific vendor.

**Still outstanding from this batch**
- Cross-portal "widget-rich" market-intelligence dashboards using `stateMarketTrend()` — since built, see §19.
- A POS-connection settings UI — since built, see §19.

## 19. Third feature batch — METRC connection scaffolding, market widgets, POS settings, sales rep commissions (added 2026-08-16)

- **Sales Rep commission tracking shipped** (§18 described the model; this batch wired the UI): `/admin/sales-reps` sets each rep's rate and marks payouts paid, `/sales/earnings` is the rep's own view.
- **Admin data uploads shipped**: `/admin/data-uploads` for the CRA Monthly Report `.docx` and License Registry CSV, both described in §18. The 5 real months provided (March–July 2026) are seeded into `MonthlyMarketReport`.
- **`StateMarketWidget`** (`components/state-market-widget.tsx`) — surfaces `stateMarketTrend()` on the retailer feed, every seller dashboard (grower/processor via `SellerDashboard`), broker, admin, and sales rep portals. Deliberately **not** added to the Transporter portal — §4's Transporter section explicitly restricts them to their own assigned shipments, not the listing feed or market data, and this wasn't asked to change that.
- **POS settings UI shipped**: `/grower/settings`, `/processor/settings`, `/broker/settings` now render the already-built `components/seller/pos-settings.tsx`.

**METRC connection scaffolding — flagged again before building, per §2's explicit instruction.** The human asked for two things: every licensee (Grower/Processor/Retailer/Transporter — `LICENSED_ROLES`) able to connect their own METRC API credentials, and an Admin-held "developer" (software vendor) API key. This is exactly the scenario §2 says to flag rather than build past — real METRC credentials are the trigger for the still-open "is Broker a recognized MI license category" question, since connecting them starts operationally treating this platform as part of the compliance chain. Flagged via a clarifying question before writing any code; the human's answer was to proceed with **scaffolding only**.

- `MetrcConnection` (per-licensee: `licenseNumber`, `userApiKey`) and `MetrcVendorConfig` (Admin's singleton platform vendor key) — `lib/metrc-integration.ts`, same "not encrypted at rest yet" deferred-hardening note as `PosConnection.apiKey`.
- Settings UI: `components/metrc-settings.tsx` shared across `/grower/settings`, `/processor/settings`, `/retailer/settings`, `/transporter/settings` (the latter two are new pages — Retailer and Transporter had no settings page before this). Admin manages the vendor key and sees every connected licensee at `/admin/metrc`.
- **No live METRC API call exists anywhere in this codebase.** This module stores credentials only — it does not create, read, or validate a manifest, and does not verify a submitted key actually works. Do not wire a real call here without both real API access (the human said it's coming "tomorrow") and the §2 legal review actually happening — API access alone does not resolve §2.

## 20a. Pickup/delivery instructions for the Transporter (added 2026-08-16)

`Shipment.pickupInstructions` (set by the Grower/Processor, `lib/shipments.ts`'s `setPickupInstructions`) and `Shipment.deliveryInstructions` (set by the Retailer, `setDeliveryInstructions`) — free-text standing notes for the transporter (gate codes, dock/entrance, hours, who to ask for). Deliberately distinct from two things that already existed: `ShipmentEvent.note` (the transporter's own per-status-update log, one-way) and `scheduledPickupAt`/`scheduledDeliveryAt` (times, not directions). Each side only ever edits their own field — authorization mirrors `acceptShipmentSchedule`'s grower/retailer split. Editable inline from `components/deal/instructions-panel.tsx` (rendered in both `DealPanelSeller` and `DealPanelRetailer`), surfaced prominently as an amber callout right under the pickup/delivery address block on the Transporter's own shipment detail page (`components/transporter/shipment-detail.tsx`) so it's the first thing they see, not buried in the timeline.

## 20. Marketing suite — sales lead CRM, Phase 1 (added 2026-08-16)

The human pasted a standalone HTML cold-calling CRM tool (list view, disposition tracking, call logging, notes, a Power Dialer, a callback calendar, and a dashboard) previously used as a claude.ai artifact backed by `window.storage` — an artifact-scoped key-value store, not a real database reachable from this app. Asked to integrate it into the Sales Rep and Admin portals as "an integral part of" a marketing suite section in both.

**Explicitly phased, per the human's confirmed choice**, not built end-to-end in one pass:
- **Phase 1 (this batch): core CRM only.** `Lead` + `LeadActivityLog` models (`web/prisma/schema.prisma`), `lib/leads.ts`, `components/leads/leads-manager.tsx`, and `/sales/marketing` + `/admin/marketing` pages (both under a "Marketing suite" nav item). Covers: the 4 original lead lists (Taino List, Lead Directory MI, MI Processors, MI Dispensaries) as tabs, search, disposition filter chips, quick disposition change (with sale-amount/callback-date prompts matching the source tool's behavior), call logging (`calledCount`/`lastCallAt`), free-text notes (`LeadActivityLog`), inline edit, add lead, soft delete/restore.
- **Not built yet, deliberately deferred to a follow-up pass:** the Power Dialer queue-and-disposition flow, the callback calendar, and the analytics dashboard from the source tool. Don't build these without the human asking for the phase-2 pass explicitly — the phasing was a confirmed choice, not an oversight.
- **Visibility: every `sales_rep` and every `admin` sees every lead across every list.** The source tool's `SHARED` flag varied by list (Taino List/Lead Directory shared, MI Processors/MI Dispensaries personal-to-viewer) — this app does **not** replicate that split; the human explicitly chose platform-wide visibility over per-rep silos, matching how Sales Rep already sits at the same trust tier as Broker elsewhere in this app (§13). `assignedRepName` is kept as free text (not a `User` foreign key) since the source data's rep names don't correspond to real accounts here.
- **Data import is done — 3 lists, 2,243 leads.** The source tool's ~1,470-record dataset was originally only available pasted into chat, which turned out too large to reproduce reliably through generation — two attempts to hand-copy it into an import script silently truncated to a handful of records, which would have imported wrong data if not caught. Rather than keep hand-transcribing, the human was asked to use the tool's own **⬇ Export** button per tab and hand over the resulting CSV files directly.
  - **Lead Directory (MI)**: `scripts/import-leads.mjs` (a general-purpose CSV → `Lead`/`LeadActivityLog` importer, same batched-upsert pattern as `scripts/import-license-registry.mjs`, batch size 4) imported all **530 records** from the tool's own export, including 65 parsed `LeadActivityLog` entries recovered from the export's flattened `activity_log` column.
  - **Taino List is explicitly not used.** The human confirmed this platform doesn't need it — it's removed from `LEAD_LIST_KEYS`/`LEAD_LIST_LABELS` (`lib/leads-constants.ts`) entirely, not just left empty, so it no longer appears as a tab in `/sales/marketing` or `/admin/marketing`.
  - **MI Processors (436) and MI Dispensaries (1,277) are auto-populated from the state's own CRA license exports instead of the CRM tool's export** — a second, explicit instruction from the human, distinct from how the Lead Directory list was imported. `scripts/import-leads-from-cra.mjs` reads the same Processor/Retailer `RecordList*.csv` files used by §12/§18's `LicenseRegistry` import (a MI "Retailer" license is what this CRM calls a "Dispensary") and maps each row straight to a `Lead`. The state export has no contact/phone/email, so every one of these 1,713 records lands with `disposition: "NO_PHONE"` — the same convention already used throughout the Lead Directory import for growers with no phone on file, not a new special case.
  - A separate 5-file upload from the human that same day (`RecordList20260816*.csv`) was flagged and confirmed before use, since it's the *same physical files* consumed by two different importers (`import-license-registry.mjs` for `LicenseRegistry`, `import-leads-from-cra.mjs` for these two `Lead` lists) — running the wrong one against the wrong table would have silently produced garbage in either.

## 21. Pre-launch security review, prompted by a deployment ask (added 2026-08-17)

The human asked to deploy to Vercel to get a real shareable link. Before doing that, a security audit was run against the whole codebase (not a diff — there was no git remote configured to diff against, and the ask was "is this safe to put on the internet," which a diff-based review wouldn't answer). Two real, exploitable issues came back; both are fixed. A separate, more consequential gap was also found by hand while answering the human's own question about data exposure — that one required a human decision, not just a fix, and got one.

**A. RSC payload leak of the counterparty's real internal user ID (fixed).** `lib/offers.ts`'s `threadForRetailerListing` queried `deal: { include: DEAL_SHIPMENT_INCLUDE }` with no `select` — Prisma's `include` without `select` returns every scalar field on `Deal`, including `sellerId` (the real Grower/Processor's internal `User.id`). That object flowed straight into `<DealPanelRetailer>`, a `"use client"` component — and **every prop passed into a Client Component is serialized into the RSC/flight payload sent to the browser**, regardless of what the component's own TypeScript type or JSX actually reads. The component's local `Deal` type omitted `sellerId`, which made it look safe in review; it did nothing at runtime. A Retailer viewing their own accepted deal could find the Grower/Processor's real internal ID sitting in the page source. This is exactly what CLAUDE.md §3/§6 already warn about — "the UI hiding a field is not enough if the API response includes it" — the RSC payload *is* an API response here. Fixed by switching `threadForRetailerListing` to an explicit `select` naming only the fields `DealPanelRetailer` actually needs. `threadsForListing` (the seller-side equivalent) got the same narrowing for defense in depth, even though `DealPanelSeller` is currently server-only and not vulnerable today — relying on a component staying server-only forever is fragile.

**B. Unauthenticated, unrate-limited license-registry scraping endpoint (fixed, partially).** `/api/license-lookup` takes no auth by design — it's called from the signup form before an account exists, so there's no session available to require. That makes it the one place in the app an anonymous caller can hit at all, and an anonymous script could iterate license-number formats through it to re-extract the full ~3,300-row `LicenseRegistry` (including `hasDisciplinaryAction`). Added a per-IP in-memory sliding-window limiter (20 requests/minute) as a stopgap. **Known limitation, stated plainly rather than glossed over:** this is in-memory, so it only throttles a single server instance and resets on cold start — it will not hold once this runs on more than one serverless instance, which Vercel routinely does. A real fix needs a shared store (Upstash Redis or similar) keyed by IP; not added here since that's a new external dependency the human hasn't asked for.

**C. Broker and Sales Rep self-signup granted instant, zero-review, platform-wide access (fixed — human confirmed the fix direction).** This wasn't found by the audit — it surfaced when the human asked "what does going live mean, do we add data at that time, we don't want it exposed to the public," and answering that honestly required checking who could actually reach the data once the app is public. `signup/actions.ts`'s `SIGNUP_ROLES` let anyone self-signup as `broker` or `sales_rep`; neither is in `LICENSED_ROLES`, so `licenseVerification` was set straight to `"approved"` with no review step at all (unlike Grower/Processor/Retailer/Transporter, which at least get the manual Admin queue when there's no clean registry match). Concretely: before this fix, a stranger visiting the public site could sign up as Sales Rep and immediately see the entire Marketing Suite — all 2,243 real business leads with phone numbers — or sign up as Broker and see every real negotiation on the platform, in real identity, on both sides. No waiting, no approval, nothing. This directly contradicts how both roles are already described elsewhere in this document (§13, §20) as "the marketplace operator's own staff," not third parties. **Flagged to the human rather than fixed silently**, per this document's own standing "flag, don't decide" posture for anything touching who can see real identity (§9) — asked which of three fixes to apply (Admin-only creation / keep public but gate on Admin approval / leave as-is), and the human chose Admin-only creation. Implemented as: `broker`/`sales_rep` removed from `SIGNUP_ROLES` (`signup/actions.ts`, enforced server-side — not just removed from the dropdown, since a client-only restriction is trivial to bypass by posting the form directly) and from `signup-form.tsx`'s `ROLE_OPTIONS`; `lib/admin.ts`'s new `createStaffAccount` plus a new `/admin/staff/new` page let an Admin create either account type directly instead.

**Also fixed while in there:** `npm audit` turned up one high-severity transitive advisory (`nanoid < 3.3.18`, pulled in via `postcss`/Tailwind's build tooling, not attacker-reachable at runtime) — `npm audit fix` cleared it with no breaking changes; `npm audit` now reports zero vulnerabilities.

**Confirmed as already-fine, not new findings:** authorization on every server action (every `*-actions.ts` function checked calls `requireRole()` and verifies the caller owns the specific record being mutated, not just "is logged in as the right role" — no IDOR found); the read-side anonymization boundary in `lib/listings.ts`/`lib/offers.ts` beyond finding A above; auth config (`bcrypt`, JWT sessions, no hardcoded secrets, `.env` correctly gitignored); no `$queryRaw`/`$executeRaw` usage anywhere. The already-documented deferred items (local disk storage, unencrypted METRC/POS credentials, no rate limiting anywhere else, no live METRC calls — §8/§18/§19) were confirmed still accurate, not re-flagged as new problems.

**Still true after all of this: this app has not been reviewed by a Michigan cannabis attorney (§2), and that has nothing to do with any of the above.** Fixing these three issues makes the *software* safer to expose publicly; it does not touch the still-open legal question of whether the Broker role needs its own license category, or make real METRC/payment integration any less blocked on that review. Don't let "the security audit passed" read as "this is cleared for real product."

## 22. Installable PWA — "add to home screen," not a native app (added 2026-08-17)

The human asked whether this is "just an app" and whether it's on iOS/Android. It's a web app only — no native iOS/Android build exists. Two options were laid out: a true native app (separate codebase, Apple Developer + Google Play accounts the human has to create themselves, code signing, app-store review) versus an installable Progressive Web App (reuses the existing Next.js app entirely, no accounts, works today). **The human chose the PWA now, with native explicitly deferred** — confirmed a second time after initially saying cost wasn't a concern for the native path, once it was pointed out that cost was never the blocker: the Apple/Google developer accounts still have to be the human's own (account creation is off-limits for Claude Code regardless of who's paying), and native is a materially larger undertaking (separate codebase or a wrapped shell, code signing, a review process Apple can reject a bare web-wrapper app through). Revisit native once those accounts exist.

**What shipped:**
- `src/app/manifest.ts` — Next.js's file-based manifest convention (auto-serves at `/manifest.webmanifest`, auto-linked into every page's `<head>`, no manual `<link rel="manifest">` needed). Declares `display: "standalone"` (opens without browser chrome) and the app's icons.
- `public/icon-192.png`, `icon-512.png`, `icon-maskable-512.png`, `apple-touch-icon.png`, `favicon-32.png` — generated with `sharp` (already a dependency, used elsewhere for `lib/media-redaction.ts`) from the exact same Lucide `Leaf` glyph and Tailwind `green-700` (`#15803d`) the app's own header/login logo already uses, so the home-screen icon matches the in-app brand mark instead of being a new, different mark. The maskable variant is full-bleed (no pre-rounded corners) per Android's adaptive-icon spec, since Android applies its own mask shape; the regular icons and apple-touch-icon are pre-rounded/square respectively, matching how each platform expects to receive them.
- `src/app/layout.tsx` — added `appleWebApp` metadata (`capable`, `title`, `statusBarStyle`) and `viewport.themeColor`, since iOS Safari doesn't read the web manifest for "Add to Home Screen" the way Android/desktop Chrome do — it needs its own explicit tags to launch standalone with the right name/icon.
- Verified live: `/manifest.webmanifest` serves the correct JSON, the generated icon renders correctly, and `<head>` carries `mobile-web-app-capable`, `apple-mobile-web-app-title`, `apple-mobile-web-app-status-bar-style`, the manifest link, the apple-touch-icon link, and the theme-color meta tag.
- **Deliberately not built: a service worker / offline support.** This app's core data — deal state, negotiation rounds, notifications, shipment status — needs to stay live, not served from a cache; a naive service worker caching pages would risk showing a Retailer or Grower a stale negotiation state, which is a much worse failure mode than "no offline support yet." If offline support is wanted later, it needs a deliberately narrow cache strategy (static assets only, never page/API responses), not a default "cache everything" service worker.

## 23. Retailer Point of Sale — this app's first live METRC call (added 2026-08-17)

The human asked to integrate an open-source POS (pointing at an opensource.com listicle of 8 older PHP/C#/Java tools — Odoo, OSPOS, SambaPOS, WallacePOS, Unicenta, Floreant, Chromis, Apache OFBiz), running primarily on iPads connected to a cash drawer/receipt printer, with per-item/per-category configurable markup, and required METRC connectivity ("all POS systems must connect to metric because of the... seed to tracking system"). Two decisions were confirmed via AskUserQuestion before building: **build natively** in this app's own Next.js/Prisma stack rather than forking one of those legacy tools (none are cannabis-aware; forking one means running a second foreign-stack service and rewriting most of its logic anyway), and **scope it to the Retailer role** as a consumer checkout, separate from the wholesale Grower/Processor/Broker flow.

**§2's broker-licensing caveat is explicitly set aside for POS/METRC, on direct human instruction — not silently reversed.** The human's own words: *"forget about Broker need its own MI license category legal question Brokers are legal for now, when the law changes i will inform you."* Everywhere else in this app, §2/§19 still hold: METRC stays credential-storage-only, no live call, pending that legal review. This module is the one deliberate exception — `lib/metrc-integration.ts`'s `submitSaleToMetrc` is **the only real outbound METRC API call anywhere in this codebase.** Do not extend this "live now" exception to wholesale Deal/manifest submission or any other METRC surface without the same kind of explicit instruction; §2 is otherwise unchanged.

**What shipped:**
- **New data model** (`InventoryLot`, `Sale`, `SaleLineItem` in `web/prisma/schema.prisma`) — nothing previously tracked "how much of a delivered wholesale Deal is still on the shelf" (`Deal.finalQuantity` is a one-time negotiated number, never decremented). A Retailer converts an accepted Deal into an `InventoryLot` once (`lib/pos.ts`'s `createInventoryLotFromDeal`), which mints a unique internal `sku` (not a manufacturer UPC — raw cannabis product doesn't have one) and computes `retailPricePerUnit` from `unitCost * (1 + markupPercent/100)`. Markup is the retailer's own per-lot call — "some 50%, others a different %," per the human's framing — editable any time (`updateLotMarkup`), and prefillable from a new `User.defaultMarkupPercent` set on `/retailer/settings`.
- **`/retailer/pos`** (`web/src/app/retailer/pos/`) — four panels: **Intake** (accepted deals not yet in POS inventory), **Register** (`components/retailer/pos/register-panel.tsx`, a client component with a scan/type input, running cart, tender + tax-rate selection, and a printable receipt), **Inventory** (active lots with a `jsbarcode`-rendered Code128 label per `components/retailer/pos/barcode-label.tsx`, editable markup, void), **Sales history** (past sales, void with automatic restock).
- **Barcode scanning is a plain auto-focused text input, not a camera.** USB/Bluetooth HID barcode scanners emit keystrokes + Enter, identical to a keyboard — no camera permissions, no JS decode library, works natively on iPad. This is also the reasoning tying to "connect to cash registers": modern iPad POS setups pair a Bluetooth/USB scanner and a receipt printer with a cash-drawer kick port the same way.
- **Cash drawer / receipt printer hardware is flagged, not built.** A browser page can't trigger a cash drawer directly — that goes through the receipt printer's kick-out port via a printer-specific protocol (ESC/POS over network, or a vendor SDK like Star CloudPRNT/Epson ePOS-Print). Building against a specific SDK without knowing the retailer's actual hardware would be guessing, so v1 ships a clean printable HTML receipt (works through any AirPrint-registered printer via the iPad's native print dialog) and this stays a known gap here, same posture as the Dutchie/Treez/Flowhub/Cova stubs in §18 — pick it up once specific hardware is chosen.
- **METRC package tag is a real, stated gap, not glossed over.** METRC sales-receipt submission requires a package tag METRC already knows about, and nothing upstream in this app (Listing/Deal) captures one — Growers/Processors don't record METRC tags anywhere here. So a Retailer must key the real tag in by hand at intake for `submitSaleToMetrc` to attempt a live call; without one, the sale still completes and is tracked in-app, `Sale.metrcStatus` is explicitly `"skipped_no_tag"` — never faked as submitted.
- **`MetrcVendorConfig.useSandbox`** (default `true`, Admin toggle at `/admin/metrc`) — since this is the app's first live external METRC call, it defaults to MI's sandbox endpoint (`sandbox-api-mi.metrc.com`) rather than production (`api-mi.metrc.com`) until Admin has verified a real submission end-to-end. `/admin/metrc` also now shows a live feed of recent `Sale.metrcStatus` outcomes across every retailer, for compliance oversight.
- **Credentials are now encrypted at rest** (`lib/crypto.ts`, AES-256-GCM, key derived via HKDF from the already-required `NEXTAUTH_SECRET` — no new secret to provision). `MetrcConnection.userApiKey` and `MetrcVendorConfig.vendorApiKey` were previously plaintext with an explicit "not encrypted yet" note, acceptable while nothing used them for a real call — that stopped being true the moment `submitSaleToMetrc` exists. Settings forms no longer pre-fill the actual/masked key into an editable input (a mask shown for reference, blank input means "keep what's on file"); `PosConnection.apiKey` (the unrelated Dutchie/Treez/Flowhub/Cova stub scaffolding) is untouched — still stub-only.
- **Tax is explicit and editable, not a certified engine.** MI adult-use cannabis retail carries 6% sales tax + 10% marijuana excise tax; the register defaults `taxRatePercent` to 16% but it's a plain editable number stamped onto each `Sale`, not a real tax-rate lookup. Stated here rather than left implicit, same honesty convention as the seller-rating "no fabricated default" rule (§10).
- **Not built / caveat on what "live" means here:** there are no real MI METRC credentials in this dev environment, so `submitSaleToMetrc` is built to METRC's documented Sales Receipts API contract but has **not** been exercised against a live sandbox — what's verified locally is the request shape, the `skipped_no_tag` short-circuit, and that a network/API failure is caught and recorded on the `Sale` rather than crashing the register. A real sandbox account is needed before flipping any retailer's connection to production.

## 24. POS quick wins, and a large feature list deliberately not built yet (added 2026-08-17)

The human pasted a long list of cannabis-POS features pulled from competitor research (Cova, Flowhub, Treez, IndicaOnline, etc.) — real-time two-way METRC/BioTrack/Leaf Data sync, a THC-equivalency calculator, ID/age-verification hardware, HIPAA/medical patient tracking, offline mode, mobile handheld POS, automated Weedmaps/Leafly menu sync, batch/lot tracking, multi-location transfers, cashless/ATM payment processing, a native e-commerce storefront, a customer-facing order-ahead-and-pickup app, loyalty/rewards with themed receipts, predictive analytics, and native delivery fleet dispatch. Asked to pick and proceed rather than wait for prioritization ("proceed as you choose" / "i asked if you can pick for me") — so a scope decision was made and is recorded here rather than silently expanding §23 to cover everything on the list.

**What was built — the pieces that extend §23's existing architecture without crossing a standing boundary:**
- **THC-equivalency calculator** (`register-panel.tsx`'s `thcMgForLine`) — approximate, clearly labeled as such, not a certified dosing tool. Weight-based products (`InventoryLot.thcPercent`, sold by `lb`) convert via mass (`quantity × 453.592g/lb × thcPercent/100 × 1000`); unit-counted products (new `InventoryLot.thcMgPerUnit`, e.g. edibles/pre-rolls, set optionally at intake since neither `Listing` nor `Deal` carries a per-piece mg figure) are already labeled in mg. Liquid concentrate (`liter`) is explicitly left out rather than guessed at — mg-per-volume needs a density figure this app doesn't have. The register shows a running cart total in mg and an approximate "standard 10mg dose" count.
- **Order type** (`Sale.orderType`: `in_store` | `pickup` | `curbside`) — a field on the existing register, not a new order-ahead system. A budtender picks it at checkout the same way they pick tender type; there's no separate queue, no customer-facing app, no notification when an order's "ready."
- **Lightweight receipt personalization** (`Sale.customerName`, optional free text; a holiday-aware greeting + watermark computed from the sale's own timestamp) — receipt-only. Follow-up ask: "seasonal watermarks on the receipt, merry christmas, and every other holiday" — `register-panel.tsx`'s `holidayInfo` covers New Year's Day/Eve, Valentine's Day, St. Patrick's Day, Easter (computed via the Meeus/Jones/Butcher algorithm, not hardcoded — it moves every year), 4th of July, Halloween, Thanksgiving (computed as the 4th Thursday of November), and Christmas, each with a few days' grace window around the actual date, falling back to a plain seasonal line (spring/summer/fall/default) the rest of the year. The matching icon renders twice: inline next to the greeting, and as a large, faint, rotated watermark behind the whole receipt (`opacity-[0.07]`, not `print:hidden`, so it survives printing). **A real bug was caught and fixed before shipping**: the first version picked whichever holiday matched first in array order, which let New Year's Eve (Dec 31, grace ±2 days) silently win over an exact match on New Year's Day itself, since Eve was checked first and Day's window overlaps it. Fixed to pick the *closest* holiday by distance, not the first found — verified with a standalone test script covering 13 dates including that exact boundary case before touching the real component. **Still deliberately not a loyalty/rewards system**: no `Customer` model, no points ledger, no accounts, no PII retention beyond what's already on the receipt record. Real loyalty (point accrual/redemption, birthday tracking, personalized profiles) is its own subsystem and wasn't built here.

**What was explicitly held back, and why — these aren't oversights:**
- **Real payment/cashless-ATM processing.** Every dollar figure in this app — commission (§11), rejection fees (§14), transport fees (§18), and now `Sale.total` — is tracked, not processed; this app has never moved money itself, by design, with real payment rails deliberately deferred to the human's separate `ai-ledger` project. Building actual card/ATM processing here would reverse that stance, so it wasn't done without the human weighing in on that specific tradeoff first (asked via AskUserQuestion; not yet answered as of this writing).
- **Two-way, production METRC sync, plus BioTrack/Leaf Data Systems.** §23 built one outbound, sandbox-by-default call. Michigan only uses METRC — BioTrack and Leaf Data Systems are other states' track-and-trace platforms, so they're only relevant if multi-state expansion is actually planned, which hasn't been discussed. Real two-way sync (reading manifest/package state back from METRC, not just posting sales) is materially more than what was asked and confirmed for POS.
- **HIPAA / medical patient tracking.** This entire app models adult-use wholesale — there is no patient or medical-retail concept anywhere in the data model (`User`, `Listing`, `Deal`, none of it). Adding one is a new regulated domain, not an extension of POS.
- **Customer-facing order-ahead app, native e-commerce storefront, delivery fleet dispatch, predictive analytics, ID/age-verification scanning, automated Weedmaps/Leafly menu sync, offline mode.** Each is large enough to be its own build (a new consumer-facing product with its own accounts; a specific hardware/vendor integration needing verified partner access, same posture as the existing Dutchie/Treez/Flowhub/Cova stubs in §18; or an architecture change this app deliberately avoided for the PWA in §22, since offline caching risks showing stale negotiation/deal state). None were started.

## 25. Customer order-ahead / pickup (added 2026-08-17)

Picked from §24's held-back list, at the human's explicit direction ("customer order-ahead app" chosen via AskUserQuestion) — end consumers can now browse a specific Retailer's own POS menu, place a pickup order, and pay in person when they collect it. This is the first page in the app a member of the public reaches beyond the marketing landing page, and the first time any "customer" identity exists here.

Three scope calls, made explicitly and recorded here, each avoiding a materially bigger/riskier version of what was asked:

- **Per-retailer storefront (`/order/<slug>`), not a platform-wide marketplace.** No cross-retailer browsing. Turning this into a Weedmaps-style aggregator this platform itself runs would be a much larger compliance surface (facilitating retail sales across many licensees to the public) than giving each existing retailer their own ordering tool for their own operation.
- **Guest checkout, pay at pickup — no accounts, no online payment.** `Order.customerName`/`customerPhone` are free text, same "not an account, never used to re-identify" posture as `Sale.customerName` (§24). Money changes hands in person, tender selected at fulfillment the same way it already happens at the register. This keeps the "this app never moves money" rule intact everywhere it already held (commission, rejection fees, transport fees, POS sales) — real payment processing is still its own separate, unanswered conversation.
- **Self-attestation age checkbox (`Order.ageAttested`), not real ID verification.** ID-scanning hardware is still its own unbuilt, separately-flagged item (§24). Real age verification happens the normal way: in person, at pickup.

**Architecture — `Order` is deliberately not a `Sale`.** `lib/pos.ts`'s `createSale` decrements real, METRC-tracked inventory the instant it runs — correct for an in-store register ring-up, wrong for a stranger submitting a public web form (a no-show, a stock race with a walk-in sale, or someone attesting falsely shouldn't falsely deplete real inventory or trigger a real METRC event). `Order`/`OrderLineItem` are a new pre-sale record; `lib/pos.ts`'s `fulfillOrder` is the only place an `Order` becomes a real `Sale` — it re-validates current stock (which may have moved since the order was placed) and then calls the **existing** `createSale(...)` as-is, reusing its transaction/decrement/METRC-submit logic rather than duplicating it. `Order` never touches inventory or METRC before that point.

**Why the public menu never joins back to the wholesale `Listing`/`ListingMedia`.** `lib/storefront.ts`'s `publicMenuForRetailer` uses a narrow `select` on `InventoryLot` only — never `include`, never `InventoryLot.dealId → Deal → Listing`. `Deal.sellerId` is exactly the field this document's own §21 audit log documents as a real, already-fixed RSC-payload leak of a Grower/Processor's real internal ID; reusing that join here would risk the identical mistake shipped to an anonymous public visitor instead of a logged-in retailer. `ListingMedia` photos are also only best-effort redacted (§18) — an accepted risk *within* the existing platform trust boundary, not one that should be multiplied out to the general public. `InventoryLot` has no photo field and this feature doesn't add one — menu cards use the same `Leaf`-icon placeholder `components/retailer/listing-card.tsx` already uses for a listing with no photo.

**Known, stated gap: no enforcement of Michigan's per-transaction possession/purchase limits.** Not silently skipped — flagged here, same honesty convention as §24's tax-rate caveat.

**What shipped:** `User.storefrontSlug` (retailer-set in Settings, URL-safe, distinct in purpose from `anonHandle` — that field anonymizes identity across the wholesale blind-marketplace boundary, this is the opposite, a retailer's own real identity meant to be shared publicly); `Order`/`OrderLineItem` models; `lib/storefront.ts` (public, no `requireRole` anywhere in it — the one file in this app with no session to check); the order-placement server action rate-limited with the exact same in-memory per-IP sliding-window pattern as `/api/license-lookup`; a notification to the retailer via the existing `lib/notifications.ts` (no new mechanism); a fifth **Orders** tab in the existing `/retailer/pos` `PosTabs` (Mark ready / Fulfill / Cancel) rather than a new top-level nav item, since it's register-adjacent work.

**Incident during this build, unrelated to the feature itself, recorded for the record:** while generating this migration, a `prisma migrate diff --shadow-database-url` command was mistakenly pointed at `DIRECT_URL` — which turned out to be the *same* Supabase database as `DATABASE_URL`, just a different connection port (pooled vs. direct), not a genuinely separate disposable database. Prisma's shadow-database process reset it, wiping all data in the live database (schema/tables were unaffected — only rows). Recovered via Supabase's daily scheduled backup (the most recent available; point-in-time recovery wasn't enabled), restored in place, then `prisma migrate deploy` re-applied every migration from that backup's point forward — including reordering this feature's own migration folder, which had an earlier timestamp than the POS tables (`sales`/`inventory_lots`) it depends on and so failed to apply in the wrong sequence on the first replay (Postgres rolled that back cleanly inside a transaction, no partial state). Standing rule going forward: never point any "shadow database" parameter at anything without first explicitly confirming it's a genuinely separate, empty, disposable database — for additive schema changes going forward, hand-write the migration SQL directly from the schema diff instead of using any tool that needs a live shadow database at all.

## 26. Portal design refresh — Phase 1: foundation + shared shell (added 2026-08-17)

The human asked for a more premium, distinctive look ("make us stand out"), confirmed via AskUserQuestion: **premium & polished** (not maximalist), **`green-700` stays the brand anchor** (already baked into the PWA icon/favicon/login page). Exploration found no `tailwind.config.ts` (Tailwind v4 CSS-first `@theme`, only `background`/`foreground` + fonts defined), no shared `Card`/`Button` component, and the same literal utility-class strings (`bg-green-700`, `border-gray-200 dark:border-gray-800`) typed into **40–55+ files** app-wide.

**This is Phase 1 only — the foundation and shared shell, not a sweep of every page.** Real design tokens added to `globals.css`'s `@theme` block (`--color-primary/-hover/-tint`, `--color-surface/-muted`, `--color-border`, `--color-ink/-muted/-faint`, `--shadow-card/-elevated`), values matched to what the app already rendered — border/surface given a very slight warm-green bias instead of pure Tailwind gray. `body`'s dead `font-family: Arial, Helvetica` (the Geist variables were wired into `@theme` but never actually applied) is now `font-sans`. New `web/src/components/ui/{card,button,badge}.tsx` primitives exist but nothing except `NotificationBell` uses them yet — **Phase 2 (sweeping the ~50 files to actually adopt them) is a deliberate, separate follow-up**, flagged so "the app looks refreshed" isn't assumed to mean every page was touched.

`PortalShell` got a real redesign: sticky/backdrop-blur header (matching the marketing `SiteHeader`'s already-established treatment) instead of a flat bottom-border, a gradient logo mark with an inset highlight instead of solid flat green, and — via a new client-only `portal-nav.tsx` (split out because active-route highlighting needs `usePathname()`, while `PortalShell` itself stays server-rendered) — an active-route indicator that didn't exist before.

**A real overflow bug found and fixed during verification, not shipped broken:** the first version of the redesigned nav wrapped mid-label or overlapped the header's right side on Admin's portal specifically — Admin has 8 nav items with long labels ("Post for a seller", "Add staff account", "Sales rep earnings"), wide enough to overflow even at a realistic 1280px desktop width (confirmed via `window.innerWidth`/`scrollWidth` measurement, not just eyeballing screenshots — the JS numbers showed the nav needed ~54px more than was available). Fixed with a pattern mirroring the mobile nav's existing graceful overflow: the desktop nav scrolls horizontally within its own box (`overflow-x-auto`, hidden scrollbar) with a CSS `mask-image` fade on the trailing edge as the visual "there's more" cue — not full nav wrapping, not a broken header. Verified in both light and dark mode at 1280px on both Retailer (6 items, fits without scrolling) and Admin (8 items, now scrolls cleanly with the fade visible) before calling this done.

## 27. Two-brand rebrand: Cannabliz + Xcelerate POS, Account Executive rename (added 2026-08-17)

The human introduced two product names for what has so far been one app: **Cannabliz** for the wholesale marketplace side (marketing landing page, Grower/Processor/Retailer/Broker/Admin/Account Executive portals — everything except the register) and **Xcelerate POS** for the retail register specifically (`/retailer/pos` and its public order-ahead storefront, `/order/[slug]`). Also: **"Sales Rep" is renamed to "Account Executive"** everywhere it's shown to users — confirmed via AskUserQuestion as a **display-label-only** rename (the `sales_rep` role id, `/sales` routes, and database values are unchanged; only `ROLE_LABELS` and literal user-facing text changed). A deeper rename (role id, routes, data) was offered but not chosen.

**Brand boundary, confirmed via AskUserQuestion:** since the POS tabs live inside the Retailer's own portal (same login, same account), the split is "the POS section gets its own header" — `PortalShell` now takes an optional `brand?: "cannabliz" | "xcelerate"` prop (default `cannabliz`); only `/retailer/pos/page.tsx` passes `brand="xcelerate"`. The rest of the Retailer's pages (browse inventory, negotiations, watchlist, settings) stay Cannabliz-branded; navigating into Point of Sale switches the header (wordmark, icon, gradient) and switches back on the way out. Same `navItems` array either way, so navigation stays consistent — only the brand identity changes.

**Logo files were requested but not yet available at the time of this build.** The human pasted two image previews directly into chat (a gold/bronze metallic "X" mark for Xcelerate POS, a white script wordmark with a small leaf accent for Cannabliz) but there was no way to export the actual image bytes to disk through the available tools — searched the usual temp/attachment locations, found nothing. Rather than hand-code an approximation of a professionally designed gradient/metallic logo (which would just be a worse knockoff), `PortalShell`'s two brand entries use **placeholder Lucide icons** (`Leaf` for Cannabliz, `Zap` for Xcelerate POS) on gradient backgrounds (green for Cannabliz, amber for Xcelerate POS) as a clearly-provisional stand-in. **Resolved 2026-08-17 — see the follow-up entry below.**

**Real logo files landed and were wired in (2026-08-17).** The human re-pasted both images and saved them to disk (they landed as two oddly-combined filenames on the Desktop due to a client-side save quirk — `~:Downloads:xcelerate-logo.png and ~:Downloads:cannabliz-logo.png` style names, slashes rendered as colons — read directly by path, matched by content, and copied into `web/public/logos/` as `xcelerate-logo-source.jpg` / `cannabliz-logo-source.jpg`, the full lockups kept around for any future large-format use). Neither source image is a standalone square icon mark (both are wide wordmark lockups with taglines), so a small square crop was cut from each with `sharp` for icon-sized usage: the gold "X" graphic alone for Xcelerate (cropped above the "XCELERATE" text row), and the green leaf accent for Cannabliz (the only self-contained mark in that wordmark — cropped tight enough to read clearly at 32px favicon size, verified by rendering and inspecting each output size before committing to the crop box). These crops replaced the Lucide placeholders everywhere they appeared: `PortalShell`'s `BRANDS` config (both portal headers), the login and signup page logo, the marketing site header and footer, the Xcelerate storefront header (`storefront-menu.tsx`), and the order confirmation page — the last of which had been using the wrong placeholder (`Leaf` on an amber background, a leftover mismatch from earlier work) and is now correctly the Xcelerate mark. Also regenerated in place at their existing filenames (no code changes needed since `manifest.ts`/`layout.tsx` already pointed at these paths): `favicon-32.png`, `apple-touch-icon.png`, `favicon-xcelerate-32.png`, `apple-touch-icon-xcelerate.png`, `icon-192.png`, `icon-512.png`, `icon-maskable-512.png` (the maskable variant composites the leaf mark at reduced scale onto a full-bleed black canvas so Android's circular safe-zone mask doesn't clip it). Verified live: `tsc`/`next build` clean, and the real marks render correctly on the login page, both portal header brands, the public order-ahead storefront, and at actual favicon/apple-touch-icon size (not just full-size preview) before calling this done.

**PWA identity stays singular, on purpose.** A single Next.js app/origin has one installable PWA identity — `manifest.ts`'s `name`/`short_name` and `layout.tsx`'s title/appleWebApp metadata were set to **Cannabliz** (the broader brand), not Xcelerate POS. Xcelerate POS is a *section* within the one installable app, not a second separately-installable PWA. This wasn't explicitly asked, so it's recorded here as an inference to correct if wrong.

**Not yet resolved — flagged, see §28**: the human's immediate follow-up asked for Xcelerate POS to run "standalone on the iPad, only accessing Cannabliz when needing to order more products" — i.e. offline-capable register operation. That's a materially different architecture than what's built (every `createSale`/`fulfillOrder` call assumes live DB access at the moment of sale, by design, since METRC needs to hear about inventory leaving the building close to real-time). Nothing was built against that ask yet; see §28 for why it needs its own conversation before any code changes.

## 28. Two follow-up asks — one resolved and shipped, one still flagged (added 2026-08-17)

Both surfaced in the same message, right after §27's rebrand shipped.

**Xcelerate POS running "standalone on the iPad" — resolved via AskUserQuestion, and shipped.** Two readings were laid out: (a) just its own distinct installable icon/launch presentation on the iPad, achievable without touching the sale-flow architecture, or (b) genuine offline transaction capability (local-first storage, a sync/conflict-resolution layer, and a real compliance question about METRC hearing about a sale only after a delayed sync) — a much bigger rebuild in tension with `createSale`'s existing "always has live DB access at the moment of sale" design (§23/§25). **The human chose (a).** Shipped using a mechanism specific to iOS: "Add to Home Screen" captures whatever page is open at the moment — its own `<title>` and `apple-touch-icon` — not one global manifest. `web/src/app/retailer/pos/page.tsx` now exports its own route-level `metadata` (`title: { absolute: "Xcelerate POS" }` — the `absolute` form deliberately opts out of the root layout's `"%s · Cannabliz"` title template, since this page should read as its own product, not a Cannabliz subpage; a plain string title would have produced "Xcelerate POS · Cannabliz" on the browser tab) plus its own `apple-touch-icon`/favicon. A retailer who taps "Add to Home Screen" while on `/retailer/pos` gets a distinct Xcelerate POS icon on their iPad; adding from the root/login page still gives the Cannabliz icon. No second PWA, no manifest changes, nothing about the sale-flow architecture touched. Verified live: `apple-mobile-web-app-title` meta tag reads "Xcelerate POS" and `apple-touch-icon` points to the new icon file only on `/retailer/pos`; every other Retailer page (browse inventory, negotiations, etc.) is unaffected. Icons are still the same Zap/amber-gradient **placeholder** pending the real logo file (§27) — `apple-touch-icon-xcelerate.png` / `favicon-xcelerate-32.png`, generated the same one-off-script-then-delete way as the original PWA icons (§22).

**Sending "today's specials" to the lead/customer database via Twilio (or similar) — still flagged, not built.** Two separate gaps, both already named once in §24 when the original feature list came up, surfaced again here in a more concrete form: (1) this app has no SMS-sending infrastructure or vendor account anywhere — building this needs a real Twilio (or similar) account with API keys only the human can provision, same constraint as every other third-party vendor account in this document. (2) More importantly, "today's specials" is promotional/marketing messaging, not transactional (a "your order is ready" text is a different legal category) — sending it requires real opt-in consent tracking and an opt-out mechanism under TCPA, and nothing in this app's data model currently distinguishes "we have this phone number because you placed a pickup order" from "you agreed to receive marketing texts." `Order.customerPhone` (§25) and the Lead CRM's phone fields (§20) were both built for their own specific purpose, not as an implicit marketing list. Building the send mechanism without first building real consent capture would create real legal exposure for the human's business, not just a missing feature — flagged for that reason, not just the vendor-account one.

## 29. Marketing SMS — consent capture + a permanent stub-only send (added 2026-08-17)

Ships the groundwork §28 flagged rather than built past. The human said "yes continue" after being told the send itself needs (a) a real Twilio account only they can provision, and (b) real TCPA consent tracking that didn't exist yet. This batch builds (b) plus a Twilio *connection point* — it does not, and by design will never on its own, place a live send.

- **The actual fix: `Order.marketingOptIn`** (`web/prisma/schema.prisma`, migrated by hand — see below). The storefront checkout at `/order/[slug]` (`components/storefront/storefront-menu.tsx`) now shows a second checkbox, separate from the required age-attestation one, unchecked by default: "Text me about specials and deals from {businessName} (optional — you can opt out anytime)." This is the real consent record — before this, `Order.customerPhone` existed only for pickup-order purposes, and using it for marketing without this checkbox would have been exactly the exposure §28 flagged. `lib/storefront.ts`'s `placeOrder` and `app/order/[slug]/actions.ts`'s `placeOrderAction` both take `marketingOptIn` as an explicit param (default `false`), not inferred from anything else.
- **`SmsConnection`** (per-retailer, `web/prisma/schema.prisma`) — Twilio Account SID, encrypted Auth Token (`lib/crypto.ts`, same AES-256-GCM/HKDF pattern as `MetrcConnection`/`MetrcVendorConfig`), From number. Settings form is on the new **Marketing** tab of `/retailer/pos` (`components/retailer/pos/marketing-panel.tsx`) — blank-Auth-Token-on-submit means "keep what's on file," same convention as every other secret form in this app.
- **`sendSpecialsMessage` (`lib/marketing-sms.ts`) never calls Twilio — permanently, not as a placeholder.** This is a deliberately different posture from the Dutchie/Treez/Flowhub/Cova stubs (§18) and the METRC scaffolding (§19), which are stubbed because real vendor API access isn't available *yet* and will get wired up once it is. This one is stubbed because sending was never blocked on API access alone — it's blocked on this app not being the system of record for consent, opt-out handling, or message-frequency/quiet-hours compliance that a real TCPA-compliant sender needs. Building a live call here the moment a Twilio key is entered would silently assume all of that is handled elsewhere, which it isn't. Every send attempt is recorded as a `MarketingMessage` row with `status: "stub_only"` and an honest human-readable `message` explaining nothing was actually sent (counting only `Order.marketingOptIn = true` customers via `optedInCustomerCount`, deduplicated by phone) — visible in the Marketing tab's history list, so nobody mistakes a logged attempt for a delivered text. Don't quietly turn this into a real `twilio.messages.create()` call without re-raising the consent/compliance-ownership question with the human first, the same way METRC's live call (§23) was only wired up after an explicit override conversation, not silently.
- **Migration applied via hand-written SQL + `prisma migrate deploy`** (`prisma/migrations/20260817060000_marketing_sms_scaffolding/`), not `migrate dev` — per the standing rule from the §25 incident, no shadow-database tooling was touched. Verified all 8 existing users and all 34 tables were intact immediately after applying.
- Verified live: connected a test Twilio-looking connection from the Marketing tab, composed and sent a test message, confirmed it logged with `status: "stub_only"` and the correct opted-in count; confirmed the storefront checkout's new opt-in checkbox is unchecked by default and correctly sets `Order.marketingOptIn` when checked vs. left unchecked.

## 30. Commercial-grade, multi-location POS hardening (added 2026-08-17)

The human asked for the Retailer POS to genuinely handle heavy transaction volume with multiple registers running simultaneously across multiple store locations — not just work correctly for one quiet register. Reviewing that question surfaced a real, confirmed bug before any of the hardening work started: `createSale`'s inventory decrement wasn't concurrency-safe and could oversell stock when two terminals raced for the same lot. Three scope decisions were confirmed via AskUserQuestion before planning: **scale target is multiple store locations** (not just one busy shop), **code-only, no new vendors** (everything below uses the existing stack — Next.js/Prisma/Postgres via Supabase — or open-source local tooling, never Sentry/Upstash/a hosted queue), and **real METRC credentials aren't available yet**, so live-fire sandbox testing stays out of scope here.

**A. Inventory overselling, fixed.** `createSale` (`lib/pos.ts`) used to read `quantityRemaining`, check it in application code, then write back a value computed from that stale read — no atomic guard at write time. Fixed with Prisma's `updateMany` combined with `{ decrement: qty }` and a `WHERE quantityRemaining >= qty` guard, checking `result.count === 0` to detect a lost race and throw a clean "sold out" error instead of silently overselling. A second guarded `updateMany` flips status to `"depleted"`, idempotent regardless of how many concurrent sales raced to zero a lot out.

**B. Sale-numbering race, fixed.** The prior `SELECT MAX(saleNumber) + 1` then `INSERT` pattern threw a raw, unhandled `P2002` under real concurrent checkouts at the same retailer — caught only by the `@@unique([retailerId, saleNumber])` constraint already on `Sale`, which rejected the second insert rather than handling it gracefully. Replaced with a new `SaleCounter` model (`retailerId @id, value Int`), incremented atomically via `upsert` + `{ increment: 1 }` inside the same transaction — Postgres's row-level lock on that single row correctly serializes concurrent terminals at one retailer/location without any cross-location contention, since each location's counter is an independent row. Migration hand-written per the standing rule from the §25 incident (`prisma/migrations/20260817070000_sale_counter/`, applied via `migrate deploy`, no shadow-database tooling touched).

**C. `createInventoryLotFromDeal`'s unique-constraint race.** `InventoryLot.dealId` was already `@unique`, so a genuine double-intake race was already rejected at the DB level — the code just didn't catch that `P2002` gracefully. Now matches `setStorefrontSlug`'s existing friendly-catch convention instead of leaking a raw Prisma error.

**Transaction timeouts, widened.** Fixing (A) surfaced a second real issue during testing: Prisma's default interactive-transaction timeouts (`maxWait` 2s, `timeout` 5s) aren't generous enough once transactions correctly start queuing behind each other for a hot row's lock — under 12-way concurrent contention on a single lot, later transactions in the queue hit a raw "transaction not found" error instead of either succeeding or failing cleanly. `createSale`'s `$transaction` call now passes `{ maxWait: 10_000, timeout: 10_000 }` — that queuing is (A)'s fix working as intended, not a bug, so the timeout needs enough headroom to let it resolve to a clean outcome either way.

**Cross-terminal sync (Phase 1).** A terminal used to only learn about another terminal's sale on its next manual navigation — harmless after (A)'s fix (a stale read just causes a clean retry), but slow. New `components/retailer/pos/live-refresh.tsx` polls `router.refresh()` every few seconds while `/retailer/pos` is visible (paused via the Page Visibility API when the tab isn't focused), refreshing server-fetched props for Inventory/Orders/Intake without disturbing `RegisterPanel`'s own client-side cart state. Supabase Realtime (a feature of the Postgres project already in use, not a new vendor) is the noted escalation path if polling latency ever proves insufficient at real scale — not built now, since polling should be tried against real usage first.

**METRC resilience (Phase 2A).** `submitSaleToMetrc` (`lib/metrc-integration.ts`) now retries with bounded jittered exponential backoff (`postWithRetry`, up to 4 attempts) on a 429 or 5xx — MI METRC enforces a per-license rate limit, and near-simultaneous sales under real multi-terminal load can trigger one. A 4xx other than 429 (bad credentials, malformed request) isn't retried, since retrying an inherently-invalid request just wastes time. The call is also moved off the checkout critical path via Next's `after()` — a register no longer sits waiting on METRC's latency before the next customer can be rung up; the `Sale.metrcStatus` row is still the durable record, checked later at `/admin/metrc`.

**Postgres-backed rate limiting (Phase 2B).** The two existing in-memory per-IP limiters (`/api/license-lookup`, the storefront order action — both flagged in the §21 security audit as not holding across more than one serverless instance) are replaced by a shared `lib/rate-limit.ts` backed by a new `RateLimitBucket` table (`key @id, windowStart, count`), an atomic fixed-window upsert via one `$queryRaw` `INSERT ... ON CONFLICT` statement. Fixed-window, not a precise sliding log — a request right at a window boundary could in theory see slightly more than the limit across two adjacent windows, an acceptable tradeoff for blunting scripted abuse (not enforcing an exact quota) at the cost of one cheap upsert per request rather than a row per hit. This directly resolves the exact "won't hold once this runs on more than one instance" gap §21 already flagged, and is squarely what "multiple instances running simultaneously" requires.

**Automated tests, added — none existed before this.** `vitest` (dev dependency only, `npm test` / `npm run test:load`), with a `server-only` alias shim and a `next/server` `after()` shim in `vitest.setup.ts` (both packages/APIs only resolve inside Next's own bundler/request-scope, not under plain Vitest — documented inline in each shim). Pure THC/holiday-watermark calculation logic was extracted out of `register-panel.tsx` (a `"use client"` component whose other imports drag in the full NextAuth chain, unsafe to import from a test runner) into a new dependency-free `lib/pos-calculations.ts`, re-exported from the component with no behavior change. Coverage: money math (subtotal/tax/total) via real `createSale` calls, the THC/holiday pure functions (formalizing the ad hoc 13-case validation script mentioned in §24, now a real regression test), and — the actual point of this whole effort — a concurrency test firing a dozen parallel `createSale` calls at a lot with quantity intentionally exceeding stock, asserting `quantityRemaining` never goes negative, exactly the right count succeed, and every rejection is one of the two clean expected messages rather than a raw DB error. All against the real dev database (Postgres's own row-locking behavior is what's under test — a mock can't meaningfully stand in for that).

**Local load test (Phase 4), no k6 install needed.** `k6` itself is a system binary this environment can't install (no package manager available here) — `src/lib/pos.load.test.ts` (run via `npm run test:load`, its own `vitest.load.config.ts` so it's excluded from the regular `npm test` pass) simulates 3 locations × 4 terminals × 15 checkout attempts each against a deliberately small, shared 5-lot catalog per location. Real result against the dev database: 161/180 succeeded, 19 failed with the expected clean messages, **zero unexpected failures, zero oversells, zero duplicate sale numbers.** Also surfaced an honest tradeoff worth stating plainly: checkout latency climbed to ~3.8s median under this deliberately extreme contention (many terminals hammering only a handful of shared SKUs) — the cost of correctly serializing writes on a hot row rather than an unbounded scaling ceiling. Real floor traffic spread across a full catalog won't see this; it only shows up when many terminals genuinely fight over the exact same few products at once.

**Admin-wide system health view (Phase 5).** New `/admin/system-health` (`lib/admin.ts`'s `systemHealthSnapshot`) — recently voided sales and currently-rate-limited identifiers, across every retailer/location at once, built entirely from data already captured elsewhere (no new logging infrastructure). Recent METRC submission outcomes keep their own dedicated page at `/admin/metrc`, not duplicated here. The rate-limit section is a **live snapshot of the current window, not a history** — `RateLimitBucket` only tracks each identifier's current count, not a log of past rejection events, so showing "currently over the limit" is the honest thing to surface rather than fabricating a timeline that isn't actually captured. Same honesty note as elsewhere in this app: this is a dashboard someone checks, not proactive alerting — no SMS/email/paging exists anywhere yet.

**An interactive credentials checklist was built and handed to the human** (not part of the codebase) enumerating what's needed outside the repo: real METRC per-licensee keys and the platform vendor key (required, not yet obtained), a Vercel account and git remote (needed before deploying — still not done, a pre-existing loose end), a Supabase plan-tier check for multi-location volume, and cash-drawer/receipt-printer hardware (still blocked on the human choosing a vendor, per §23).

**Verified:** `npx tsc --noEmit` and `npx next build` clean after every phase. `npm test` — 18/18 passing (money math, THC/holiday pure functions, both concurrency tests). `npm run test:load` — 1/1 passing with the real-run numbers above. Live in the actual running app: scanned the seeded Blue Dream lot (9 lb on hand) at `/retailer/pos`, forced the cart quantity to 999, and hit Complete sale — the register displayed `"Only 9 lb of Blue Dream left."` cleanly, styled consistently with the rest of the UI, no crash and no raw error. Confirmed afterward against the database that the rejected attempt left `quantityRemaining` untouched at 9 and created no phantom `Sale` row — the guard rejects before any write happens. This exercises the exact user-facing path a real register hits when it loses a stock race, on top of the automated concurrency test that already proves the underlying atomic guard holds under real parallel load.

## 31. Register customer panel — identity, purchase habits, discounts, loyalty, purchase limits (added 2026-08-18)

The human shared a Flowhub cashier screenshot and asked whether Xcelerate POS should look similar. The real answer, worked through together: don't copy Flowhub's specific visual brand (Xcelerate keeps its own gold/amber identity, now built from the real logo), but the density gap on that screen — customer purchase habits, loyalty points, discounts, a purchase-limit tracker — is a **feature** gap, not a styling one. A full Flowhub feature audit was built as an interactive artifact and handed to the human (filterable Have/Partial/Don't-have, grouped by category) before any code changed, so the scope decision was made with real information rather than a guess.

**The one real architectural decision, confirmed via AskUserQuestion before writing any code.** All four of those features need the register to recognize the same customer across visits. `Sale.customerName`/`Order.customerName`/`customerPhone` had always been documented as "receipt-only, never used to look up or re-identify" — a deliberate no-PII stance from earlier in the build (§24's loyalty-system decline explicitly cited "no `Customer` model, no points ledger, no accounts, no PII retention beyond the receipt"). The human confirmed reversing this specifically: add a lightweight `Customer` record — name + phone as the lookup key, no password, no online login, nothing beyond what a budtender types in to recognize a repeat visitor. Scoped per retailer like every other retailer-owned record in this app; no cross-retailer customer sharing, no platform-wide customer graph.

**Separate from, and explicitly not a walk-back of, an unrelated request declined the same day.** The human separately asked for an admin "backdoor" to split a 10 lb sale into fake 2.5 oz visits spread across several days, to disguise it as compliant under Michigan's per-visit purchase limit. That was refused outright — it's a records-falsification mechanism, not a compliance feature, and would push fabricated data into METRC. The purchase-limit tracking built here is the opposite: it makes a real cumulative total *visible and enforceable*, nothing about it helps evade the limit it displays.

**Data model** (`web/prisma/schema.prisma`, hand-written migration + a same-day backfill migration — see below): `Customer` (`retailerId`, `name`, `phone` normalized as the `@@unique([retailerId, phone])` lookup key, `notes`, `loyaltyPointsBalance` — a cached running total kept correct by `createSale`'s own atomically guarded increment, same discipline as `InventoryLot.quantityRemaining`'s decrement). `LoyaltyLedgerEntry` (every accrual and redemption logged as a real row — `points`, `reason`, optional `saleId` — same "log every event" convention as `SalesRepCommission`/`MarketingMessage`/`ShipmentEvent`, not just an opaque balance mutation). `Sale.customerId` (new, nullable — additive; `Sale.customerName` is untouched, still the receipt-time snapshot, so a customer's name changing later never rewrites a past receipt). `SaleLineItem.discountAmount` (flat $, register-entered — `lineTotal` is still the real net charged amount; this field only exists so the receipt/register can show the struck-through original price). `User.loyaltyPointsPerDollar`/`dailyPurchaseLimitOz` (both plain editable numbers, not certified figures — same posture as the existing tax-rate field — set on `/retailer/settings`).

**Purchase-limit tracking is scoped honestly, not asserted as a certified compliance check** (`lib/customers.ts`'s `todaysPurchaseTotals`). Michigan's limit is expressed in flower-equivalent ounces, and the equivalency ratio between flower and concentrate/vape/edible products is real regulatory judgment this build should not guess at — getting it wrong in a compliance-adjacent feature is worse than not building it. So: weight-sold (`unit === "lb"`) line items convert to ounces with confidence (unambiguous unit math, no legal judgment involved) and sum into the bar shown on the customer panel; unit/liter-sold lines (concentrate, vape, edible) are tracked as their own separate running count, explicitly labeled as *not* converted into the flower-equivalent figure — same honest-gap pattern already used by the existing THC-equivalency calculator's liquid-concentrate exclusion. The bar and the day's total only appear once a `Customer` is attached to the sale, since cross-visit tracking is exactly what the identity decision above enables — a walk-in shows no bar, not a fabricated one.

**`lib/customers.ts`** (new): `findOrCreateCustomer` (normalizes phone, upserts, friendly-catches the `P2002` race the same way `setStorefrontSlug` already does for its own unique constraint), `lookupCustomerByPhone`, `customerPurchaseHabits` (avg spend, last-sale date, sales/month, top categories by $ — returns `null` for zero history rather than a fabricated default, same convention as `lib/market.ts`'s seller/retailer ratings), `todaysPurchaseTotals`, `redeemLoyaltyPoints` (atomically guarded decrement, same pattern as the inventory fix — validates balance and rejects cleanly rather than allowing it to go negative).

**`createSale` extended** (`web/src/lib/pos.ts`) — accepts optional `customerId` and per-line `discountAmount`; within the same transaction that already does the atomically-guarded stock decrement, also does a plain `increment` on `Customer.loyaltyPointsBalance` (a plain increment is safe here, unlike the stock decrement, since there's no "insufficient balance" failure mode for *adding* points — Postgres's row-level lock on the UPDATE is sufficient) and logs the accrual `LoyaltyLedgerEntry`. `voidSale` claws back whatever points a voided sale had accrued, symmetric with the existing inventory-restock behavior — clamped at zero rather than blocking the void, in case the customer already redeemed those points elsewhere.

**UI**: new `components/retailer/pos/customer-panel.tsx` (phone lookup-or-create, notes, purchase habits, loyalty balance + redeem, purchase-limit bar) rendered above the cart in `register-panel.tsx`; each cart line gets a manual "Add discount" $ input — deliberately not an auto-apply/stacking rules engine (that's a materially bigger project, same "start simple, layer complexity later" scope cut already used for `SplitContract`/`ProductRejection`). A loyalty redemption is conceptually cart-level, but the schema only carries a discount per line for now — applied to whichever cart line has the most room to absorb it, with the redeem button disabled whenever the cart is empty (a real bug caught before shipping: without that guard, points could be deducted server-side with nowhere to apply the resulting discount).

**A real bug found during live browser verification, not caught by any automated test, fixed on the spot.** Completing an actual sale against the pre-existing seeded retailer threw `Unique constraint failed on the fields: (retailerId, saleNumber)`. Root cause: that retailer already had a `Sale` row from earlier in this project's history, created before `SaleCounter` existed (back when sale numbering used a `SELECT MAX(saleNumber)+1` pattern). The `SaleCounter` migration only created the table — it never backfilled a starting value for retailers with pre-existing sales, so `createSale`'s upsert took the "create at value=1" branch and collided with the sale that already had `saleNumber=1`. Every automated concurrency/money test always started from a brand-new retailer with zero prior sales, so this gap never surfaced there. Fixed with a small backfill migration (`prisma/migrations/20260818000000_backfill_sale_counter/`) that sets each retailer's counter to `MAX(saleNumber)` from their existing sales, idempotent via `ON CONFLICT ... DO UPDATE SET value = GREATEST(...)` — safe to re-run, and the right shape for this to also protect a real production rollout against the same class of pre-existing-data gap.

**Verified:** `npx tsc --noEmit` and `npx next build` clean. `npm test` — 31/31 passing, including new tests for purchase-habits math, discount line-total math (including clamping a discount that exceeds the line's value), loyalty accrual/clawback-on-void, phone normalization, and a loyalty-accrual concurrency test (scaled to 8 concurrent attempts after a higher number hit an unrelated Prisma Client connection-pool ceiling in this dev environment — a real, worth-noting environment constraint, not a correctness bug, documented inline in the test). Live in the running app: looked up a nonexistent phone number, created "Ronald Reagan" as a new customer, added Blue Dream to the cart, applied a $10 line discount (receipt correctly showed `$135.00 → $125.00`), hit the SaleCounter bug and fixed it live, completed the sale, and confirmed against the database that loyalty accrued exactly right (290 pts = 2 pts/$ × $145 total, one matching ledger entry) and the purchase-limit bar correctly showed `16.00 / 2.5 oz` with the over-limit warning after a 1 lb purchase.

## 32. Account Executive low-stock notification (added 2026-08-18)

A follow-up flagged and deliberately deferred rather than context-switched into mid-build (§27's asks): when a retailer's POS inventory for a product runs low, notify Account Executives so they can proactively solicit a matching wholesale offer. Picked up once §31's customer/loyalty/discount work landed.

**Confirmed the one real decision via AskUserQuestion before writing code.** "Notify Account Executives so they can make offers" only works if the AE knows *which* retailer and *what* product — meaning this is a new cross-role visibility grant, not scaffolding. No role besides the retailer itself previously saw a retailer's real identity or their private POS inventory levels. The human confirmed: yes, show the AE the retailer's real identity and the specific low-stock product. Who gets notified defaults to every active Account Executive platform-wide — matching how AE/Broker visibility already works everywhere else in this app (decision #2, §13) — rather than inventing a per-retailer AE-assignment concept that doesn't exist anywhere in the data model.

**A separate, unrelated request the same conversation was declined outright, worth recording precisely because it could be misread as related.** The human separately asked whether a customer could accumulate a 10 lb order across many separate ~2.5 oz app visits/orders, then collect it all in one pickup — since Michigan's limit is per-visit with no stated cap on visits per day. Declined, for two reasons independent of how the transactions are recorded: (1) Michigan also caps *possession*, not just per-transaction purchases — a single consolidated pickup of 10 lbs would very likely violate that regardless of how the purchase was structured; (2) deliberately splitting one large acquisition into many small transactions specifically so it can be collected as a single bulk quantity is the structuring pattern purchase limits exist to prevent, whether or not each individual transaction is technically compliant. Also noted: 10 lbs is wholesale volume with no legitimate personal-use case — a buyer at that scale should be using the wholesale marketplace under their own license, not the retail consumer storefront. Not built, and no scaffolding toward it exists anywhere in this codebase.

**What shipped:** `InventoryLot.lowStockNotifiedAt` (new, nullable — set once, the first time a lot's `quantityRemaining/quantityReceived` ratio crosses the existing 0.2 low-stock threshold, now centralized as `LOW_STOCK_THRESHOLD` in `lib/constants.ts` instead of being duplicated between the retailer's own Inventory panel and this new check). The claim is atomically guarded the same way the inventory-decrement fix is (`updateMany` with `lowStockNotifiedAt: null` in the `WHERE` clause) — under concurrent sales racing to cross the threshold on the same lot, only the one transaction that wins the race sends a notification, so Account Executives get exactly one notification per lot, never one per sale. Sending itself happens in `after()` (off the checkout critical path, same posture as METRC submission and loyalty accrual) — queries every `sales_rep` user and calls the existing `lib/notifications.ts`'s `notify()` for each, reusing the shared notification bell with no changes needed there. Message format: `"{retailer's real business name} is running low on {product} ({category}) — consider offering a matching listing."`

**Verified:** `npx tsc --noEmit` and `npx next build` clean, full `npm test` suite (31/31) still passing. Live: created a test Account Executive account, sold down a seeded lot from 8/10 to 2/10 (crossing the 0.2 threshold) via a real `createSale` call, confirmed exactly one notification landed for the AE with the correct retailer name and product, confirmed a second low-stock sale against the same already-flagged lot did *not* send a duplicate, and confirmed in the actual browser (logged in as the AE) that the notification bell renders the message correctly.

## 33. Marketing page rewrite + Budtender staff role (added 2026-08-18)

Two requests from the same conversation, unrelated to each other beyond both being asked back-to-back.

**Marketing page (`web/src/app/page.tsx` and `web/src/components/marketing/*`) now reflects the full built platform, not just the original wholesale-only pitch.** The page had never been touched since the early build brief and made no mention of Xcelerate POS, loyalty, order-ahead, or Account Executive — a real gap once those became a large fraction of what the app actually does. Added a new `components/marketing/xcelerate.tsx` section (amber/gold Xcelerate identity, distinct from the green Cannabliz palette, matching §27's brand split) covering scan-to-sell, loyalty/discounts, the purchase-limit tracker, the order-ahead storefront, and live METRC submission. `flow.tsx` gained a 5th step ("Sell at retail") bridging the wholesale pipeline into the register. `roles.tsx` gained an Account Executive row (the section heading was already claiming "six roles" while only rendering five — now genuinely seven with Budtender counted separately below). `compliance.tsx` gained two pillars ("Credentials encrypted at rest," "Every dollar tracked, none of it moved") formalizing claims that were already true elsewhere in this doc but never stated on the marketing page itself. `capability-strip.tsx`, `site-header.tsx`'s nav, `cta-footer.tsx`'s footer copy, and the hero subheading all picked up matching mentions. No new claims were invented — every line added here describes something already shipped and documented in an earlier section of this file.

**Budtender — a new staff-account role, scoped to Xcelerate POS only.** Raised directly by the human: *"bud tenders should only have access to POS system only and should not be able to see anything else."* This is a different kind of decision than most of the ones flagged elsewhere in this doc (§9) — it doesn't touch the Grower/Processor↔Retailer blind boundary, doesn't let the platform pick a retailer's counterparty, and doesn't expose any identity that wasn't already visible to the Retailer account it's scoped under. It's an internal, less-privileged sub-account of an existing Retailer, the same shape as Admin creating Broker/Sales-Rep staff accounts (§21's finding C) — so it was built directly rather than routed through an AskUserQuestion.

- **Data model**: `User.retailerOwnerId` (new, nullable, self-relation `FK → User.id`, `onDelete: Cascade`) — set only for `role = "budtender"`, points at the Retailer whose POS they operate. Hand-written migration per the standing rule from the §25 incident (`prisma/migrations/20260818020000_budtender_staff/`), applied via `migrate deploy`. `"budtender"` added to `ROLES`/`ROLE_LABELS` in `lib/constants.ts` — not in `LICENSED_ROLES`, `ADDRESS_ROLES`, or `SELLER_ROLES`, same non-licensed-staff posture as Broker/Sales Rep.
- **Session carries the link**: `auth.ts`'s `authorize`/`jwt`/`session` callbacks now thread `retailerOwnerId` through the JWT alongside `role` (typed in `types/next-auth.d.ts`), so every request can resolve which retailer's data a budtender should act on without an extra DB round-trip.
- **`lib/dal.ts`'s new `requirePosAccess()`** is the one access-control path a Budtender is allowed through: accepts either `role = "retailer"` or `role = "budtender"`, resolves an `retailerId` (the caller's own id for a Retailer, `retailerOwnerId` for a Budtender), and — matching Admin's exemption for the non-circumvent gate — skips that gate entirely for a Budtender, since they never negotiate a deal or touch the wholesale side at all. Every other `/retailer/*` route (`/retailer`, `/retailer/negotiations`, `/retailer/watchlist`, `/retailer/requests`, `/retailer/settings`) is untouched — they still call `requireRole("retailer")`, which now correctly rejects a Budtender session the same way it already rejects any other role mismatch (bounced to `/login`, session left intact — same behavior a Grower hitting `/broker` already got before this change). `/retailer/pos/page.tsx` and `/retailer/pos/actions.ts` were the only files that needed to switch from `requireRole("retailer")` + `session.user.id` to `requirePosAccess()` + the resolved `retailerId` — every function in `lib/pos.ts`/`lib/customers.ts`/`lib/marketing-sms.ts`/`lib/metrc-integration.ts` already took `retailerId` as a plain parameter rather than deriving it from a session itself, so none of them needed to change.
- **`roleHome("budtender")` → `/retailer/pos`** — a Budtender who signs in lands directly on the register, and the root `/` redirect (used by both the marketing page and a stray bookmark) sends them there too. `PortalShell`'s `navItems` is now conditional in `/retailer/pos/page.tsx`: a real Retailer still gets the full wholesale nav (Browse inventory, negotiations, watchlist, wanted board, POS, settings); a Budtender gets a nav array containing only "Point of sale" — there is no link anywhere in their UI to a page they can't reach. The METRC-not-connected banner on that page also branches: a Retailer gets the "Connect METRC" link into Settings, a Budtender (who can't reach Settings) gets "Ask the store owner to connect it" instead.
- **`lib/staff.ts` (new)** — `createBudtenderAccount`/`budtendersForRetailer`/`removeBudtenderAccount`, same shape as `lib/admin.ts`'s `createStaffAccount` for Broker/Sales Rep: `licenseVerification: "approved"` immediately (not a licensed role, nothing to review), bcrypt-hashed password, a generated `anonHandle` (required by the schema but never actually shown anywhere for this role). Removing a Budtender is a plain delete — a Budtender owns no `Sale`/`InventoryLot`/`Customer` rows itself (everything it touches is recorded under the Retailer's own `retailerId`), so there's nothing to orphan. Management UI is a new "Staff accounts (Budtenders)" card on `/retailer/settings` (`retailer`-only — a Budtender can't reach this page, and can't create another Budtender even if it tried): add-account form (name/email/temporary password), list with a Remove action per row.
- **Not built in this pass, raised by the human but not yet scoped**: per-budtender sales commission, and a marketing portal (email/SMS to attract customers) with budtender-level attribution. Both would need a new `Sale.soldById`-style field recording which Budtender rang up a given sale (nothing today distinguishes "the Retailer rang this up" from "Budtender X rang this up" — `Sale.retailerId` is always the owning Retailer regardless of who was at the register), plus — for commission — a standing rate someone sets per Budtender (mirroring `User.salesRepCommissionRate`'s "computed once, at the triggering event" posture), and — for marketing — a decision about whether it reuses the existing retailer-owned Twilio `SmsConnection`/`MarketingMessage` machinery (§29) or needs its own, plus whether email is scaffolded now (no vendor account exists for it, same gap as SMS before §29's TCPA-consent work) or held until asked for directly. Flagged here rather than guessed at, since committing to a `Sale.soldById` shape now would lock in an answer to a question that hasn't actually been decided yet.
- Also asked, separately: whether budtenders could get some kind of per-register visual customization (e.g. background color). Not decided or built — the app currently has no individual-operator-level UI preference storage of any kind (nothing analogous exists even for a Retailer's own account), so this would be new infrastructure either way; holding until it's raised again with an actual answer to "per-Budtender or per-Retailer."

## 34. Bulk listing import for the AI-assisted paste tool (added 2026-08-18)

`lib/ai-listing.ts`'s "paste your notes, structure with AI" feature (§4, §6) was built for one listing at a time. A real Account Executive pasted a full 21-strain delivery price list into it (`The Deli - Bubba Kush 8.12`, one line per strain, the trailing number a weight in lbs) and got "Couldn't structure that text" — reproduced live: the model, given a whole menu, doesn't return the single JSON object `structureListingDraft`'s parser expects, so extraction fails outright. Confirmed with the human this is a real, wanted feature (not just a bug to patch around) — sellers/reps genuinely paste whole delivery manifests, not one strain at a time.

- **`structureListingDraftsBulk` (new, `lib/ai-listing.ts`)** — a second Claude call with its own system prompt asking for a JSON *array*, one object per distinct product, explicit about not fabricating a price when a pasted line is just `<strain> <number>` with no currency marker (a bare number next to a strain name is very often a weight, not a price). Reuses the existing `coerceDraft` per-item, filters out any item with an empty strain name, `max_tokens` raised to 4096 to fit a full list's worth of output. Same "draft only, never auto-posts" posture as the single-item path, same not-configured fallback when `ANTHROPIC_API_KEY` is unset.
- **`ListingForm` (`components/seller/listing-form.tsx`) gained a bulk mode**, toggled by a new "Have a whole price list? Switch to bulk import" link — only shown when the page passes a new optional `bulkAction` prop, so Grower/Processor/Broker's own single-seller posting pages are untouched; only `/admin/listings/new` and `/sales/listings/new` (the "post on behalf of a seller" pages, where a whole manifest getting pasted at once is the actual demonstrated case) got it. In bulk mode the same "Structure with AI" button calls `structureListingDraftsBulk` instead and renders an editable table (strain/category/THC%/qty/unit/price per row, a remove button per row) instead of the single-item fields; the seller picker and expiration selector are shared across both modes since a whole batch posts under one seller with one expiration. Rows missing a required field (strain name, quantity, price) are highlighted amber and block the submit button until fixed or removed — no silent partial submission.
- **`handleCreateListingsAsAssistantBulk` (new, `lib/sales-actions.ts`)** — takes the reviewed draft array (submitted as a JSON hidden field, since a `<form>` can't natively post an array of objects) and loops `createListing` once per valid row, same seller/`createdBySalesRepId` attribution as the single-item path. Re-validates every row server-side independent of the client-side highlight (never trust the client's disabled-button state alone) and skips any row that still fails rather than aborting the whole batch — the whole point of surfacing per-row problems in the table first is that a partial-but-explained batch beats an all-or-nothing failure once the seller's already reviewed and approved every row.
- **Deliberately no photos in bulk mode.** A pasted price list doesn't carry per-item images, so bulk-created listings post text-only; the seller can add photos afterward from each listing's own detail page, same as any listing. Stated in the UI itself so it isn't a silent gap.
- **Verified**: `npx tsc --noEmit`, `npx next build` clean. Live in the browser on local dev (admin account): switched into bulk mode, pasted the actual 21-strain list that triggered the original bug, and confirmed the correct "AI structuring isn't configured yet" fallback rendered cleanly with no crash — local dev's `.env` has no `ANTHROPIC_API_KEY` (only the Vercel deployment does), so the real multi-item extraction itself gets its live-data confirmation from §35's testing on the deployed site instead.

## 35. Quick seller signup for "post for a seller," and the License Registry was found empty and restored (added 2026-08-18)

Same conversation as §34: while building a menu for a grower on `/admin/listings/new`, the human found no way to post for a grower/processor who hasn't signed up — the existing SellerPicker only searches accounts that already exist. The ask: type the grower's state license number and have their info auto-populate, the same way public signup already does, so an AE/Admin fielding a phone call can create the account and post the listing in one sitting instead of telling the grower to go self-signup first.

**Confirmed via AskUserQuestion before building**: whether this grower already had an account (in which case the fix would just be better search) or genuinely didn't (in which case this is new account-creation power for AE/Admin, not something built anywhere else in the app). The human confirmed the second — no account yet, create one from the license number.

- **`lib/assisted-seller-signup.ts` (new, `"use server"`)** — `createAssistedSellerAccount` mirrors public signup's license lookup/auto-approve logic (`lib/license-registry.ts`'s `lookupLicense`, CLAUDE.md §12) as closely as possible rather than a looser second path: same "Active" status → `licenseVerification: "approved"` + `licenseAutoMatched: true`, anything else → falls to the manual Admin queue, exact same honesty caveat that a license *number* match isn't proof of who's really behind the account. The one real difference from signup: the license number isn't pre-scoped to a role, since an AE on the phone won't necessarily know if they're talking to a Class B Grower, Class C Grower, or a Processor — so it tries `lookupLicense(number, "grower")` first, then `"processor"`, and takes the role from whichever category actually matched. Callable directly from a client component (returns `{ok, sellerId, businessName, role}` or an error) rather than redirect-based, since it's a sub-step inside the listing-creation page, not a page navigation of its own.
- **`SellerPickerOrCreate` (new, `components/sales/seller-picker-or-create.tsx`)** wraps the existing `SellerPicker` with a second path, toggled by "Grower/Processor doesn't have an account yet? Create one from their license number." On success it swaps in its own `name="sellerId"` hidden input and a confirmation line ("Account created for X — posting under their identity"), so `ListingForm`'s surrounding `<form>` doesn't need to know which path was used — same "the slot manages its own hidden input" pattern the bulk-import table's hidden `drafts` field already uses. Wired into both `/admin/listings/new` and `/sales/listings/new` in place of the bare `SellerPicker`.
- **Contact name, email, and a temporary password are typed in by the AE/Admin**, not looked up — the state registry has no email on file, and the account needs *some* way to sign in later. Same "share this with them directly" posture already established for Budtender account creation (§33) — the temp password is visible in the form as the AE types it, meant to be relayed to the grower by phone/text if they ever want to log in themselves.

**A real, separate gap found while testing this, fixed the same session: the live `LicenseRegistry` table was empty.** Querying it directly for a test license number returned zero rows — not just "no match for this number," the whole table had 0 rows, silently breaking this new feature *and* the pre-existing public-signup auto-populate (§12) *and* the license-lookup API, all without any visible error (they were all just falling through to their "no match" / manual-review paths). Root cause not fully diagnosed, but the timing lines up with §25's incident (an accidental shadow-database reset that wiped the live database, restored from a backup) — the restore evidently didn't include this table, or it was never re-imported after. Fixed by re-running the existing `scripts/import-license-registry.mjs` against `scripts/license-registry-merged.json` (already-parsed data still sitting on disk from the original §12 import, no new CSV needed) — restored to 3,302 rows across all 5 categories, matching the original counts exactly. The Supabase pooler dropped the connection partway through the first run (1004/3304); the script's upsert is idempotent so simply re-running it picked up where it left off with no duplicate or corrupted rows. Worth a periodic spot-check (e.g. next time `/admin/data-uploads`'s monthly refresh runs) that this doesn't silently empty out again.
- **Verified**: `npx tsc --noEmit`, `npx next build` clean. Live in the browser (local dev, admin account, against the now-restored registry): looked up a real Active Class C Grower license (`AU-G-C-001819`), confirmed the account was created with the correct auto-filled business name/address, `role: "grower"`, `licenseVerification: "approved"`, `licenseAutoMatched: true`; confirmed the confirmation banner rendered and the listing form's seller was correctly pre-selected; completed posting an actual listing and confirmed in the database that it was attributed to the new seller's own identity with `createdBySalesRepId` correctly set to the admin who built it; confirmed the new account appears correctly in Admin's user list with the "AUTO-MATCHED" badge. Test account and its listing deleted afterward — it was created against a real business's real license number (not a synthetic test one), so it wasn't left sitting in the live database.
- **Follow-up the same day: the §12 phone enrichment had also been silently lost in the same incident.** The human tried looking up a specific license (`GR-C-001968`) that genuinely isn't in any available CRA export — confirmed by re-running the registry refresh against freshly re-supplied copies of the same 5 CSV files (`scripts/refresh-license-registry.mjs`, category auto-detected per row so file order didn't matter): identical 3,302-row result, confirming these are the same August 16th snapshot, not a newer one. Separately, the human asked to match a new `lead-overrides-v1-export-2026-08-16.csv` file to the CRA registry **by license number** — tested directly against 7 real numbers from that file and got zero matches, re-confirming the license-number-scheme mismatch already documented in `scripts/enrich-license-registry-phone.mjs`'s own comments. Ran that existing name-based-matching script against the new file instead: **581 of 1,542 phone-less grower rows matched** — the exact same count as the original §12 pass, which is itself the tell — since a second pass over already-enriched rows should match fewer, not the identical number, this confirms the phone data was wiped in the same incident that emptied the whole table, and this run silently restored it rather than adding anything new. Nothing in the app's own code changed for this — pure data recovery via already-existing scripts.

**Verified:** `npx tsc --noEmit`, `npx next build`, full `npm test` (31/31) all clean after every change in this section. Live in the browser: created a real Budtender account from `/retailer/settings`, confirmed it appears in the staff list with a working Remove action; logged in as the Budtender and confirmed (a) `/` redirects straight to `/retailer/pos` with the Xcelerate POS brand and a nav containing only "Point of sale," (b) the header shows "Budtender" as the role label, (c) `/retailer`, `/retailer/settings`, and every other wholesale-side route bounce to `/login` without invalidating the session (`/api/auth/session` still returned the live Budtender session immediately after), (d) the POS page's own tabs (Register/Orders/Intake/Inventory/Sales history/Marketing) all render and correctly resolve to the owning Retailer's own (empty, in this test) inventory rather than erroring or leaking another retailer's data.

## 36. Grower lookup restricted to the real CRA registry, and shareable public listing links (added 2026-08-18)

Two related requests, worked through the same day the empty-registry incident (§35) was found and fixed.

**Grower/processor lookup now only ever matches the real state registry — the lead-file numbering scheme is gone from this path entirely.** Concretely confirmed via a real example before this was built: "123 Grow LLC" exists as `AU-G-C-001405`–`001409` in the actual Michigan CRA export, but as `GR-C-001968`/`GR-C-001969` in the platform owner's own marketing/lead-CRM export (§20) — same business, two incompatible internal numbering schemes. That mismatch was the root cause of repeated "why doesn't this license show up" confusion; it was never a bug in the lookup code, the two data sources simply don't share a key. The human's instruction was explicit: *"i only want the number of the CRA to be used."* `lib/assisted-seller-signup.ts`'s `searchLicenseRegistryForAssist(query, mode)` replaces the old free-text-license-number-only field on `/admin/listings/new` and `/sales/listings/new` with a real search — by **license number, business name, or phone** — scoped to `LicenseRegistry` only (`grower_b`/`grower_c`/`processor` categories), never the `Lead` table. `SellerPickerOrCreate` (`components/sales/seller-picker-or-create.tsx`) got a search-mode toggle and results list; selecting a result shows a confirmation card (business name/license number/address/status) before the contact-name/email/password fields appear, so an AE/Admin can visually confirm they picked the right business before creating an account against it. This does not touch §20's `Lead` model itself or its own phone-matching logic — leads are still leads; this only changes how a *license number that will become an actual account* gets found.

**Shareable public listing links — a deliberate, confirmed loosening of decision #1's "must already be a logged-in verified Retailer" gate, scoped narrowly.** The human's ask, in full: post a listing → get a link → send it to any potential buyer → they view the menu → Accept/Counter/Decline prompts for a license number → auto-fills a profile if one doesn't exist → password to finish creating the account (or log into an existing one) → **or** their broker (Admin) creates the account and submits on their behalf. Two pages implement this:

- **`/listing/[id]`** (`lib/listings.ts`'s new `publicListingView`, no `requireRole`, no session check at all) — the one other page in this app besides `/order/[slug]` (§25) that a completely anonymous visitor can load. Deliberately narrow: only `status: "active"` **and** `visibility: "all"` listings resolve — an Admin-restricted exclusive listing (§18) does not get a bypass link, since that would defeat the point of restricting it. Shows the same anonymized fields the logged-in retailer feed already shows (strain, category, THC%, quantity, price, terms, photos, the poster's `anonHandle`) — never the real seller identity. This is the same `select`-not-`include` discipline §21's audit already established for `Deal`; `publicListingView` was written with an explicit `select` from the start rather than risking the same class of leak.
- **`/listing/[id]/respond?action=accept|counter|decline`** — the gated half. `components/listing/respond-panel.tsx` (`RespondPanel`, client component) branches on the visitor's session:
  - **No session**: shows "Create Retailer account" / "Sign in" links, each carrying `?role=retailer&callbackUrl=/listing/[id]/respond?action=...` so the visitor lands right back on this exact action after finishing either flow. This needed two small additions to plumbing that already existed for the *other* direction (finishing a normal signup/login and going to your role home): `signup/actions.ts` and `login/actions.ts` now both accept an optional `callbackUrl` form field and use it as the `signIn()` redirect target instead of the hardcoded role-home default; `signup-form.tsx` reads `role`/`callbackUrl` from the URL via `useSearchParams` (the page wraps it in `<Suspense>`, required by Next for that hook) and pre-selects the role / carries the callback forward as a hidden field.
  - **New addition made here, not reused from elsewhere: `lib/safe-redirect.ts`'s `safeRedirect(url, fallback)`.** A `callbackUrl` is user-controlled form data now reaching a `redirectTo`, which is exactly an open-redirect shape (`callbackUrl=https://evil.example.com` or `callbackUrl=//evil.example.com`) if handed straight through — `safeRedirect` only accepts a same-site path starting with a single `/`, anything else falls back to the normal default. Applied to both the new signup path and the pre-existing login path, since both now carry a user-supplied `callbackUrl` and both were equally exposed.
  - **Logged in as a Retailer**: `RespondPanel` renders the action form directly (message box, plus price/terms inputs for a counter) bound to that retailer's own id.
  - **Logged in as Admin**: renders `AdminRetailerPicker` — find an existing retailer account by business name/email, or create a new one from a real CRA retailer license number (`lib/admin-retailer-assist.ts`, the retailer-side mirror of the grower quick-signup above: `searchRetailerLicenseRegistry`, `createRetailerAccountForAdmin`, `searchExistingRetailers`, all `requireRole("admin")`). Deliberately **Admin-only, not opened to Account Executive** — the human's own wording was "their broker who is admin," and AE has no equivalent standing relationship with retailers the way it does with sellers it's actively soliciting.
  - **Any other logged-in role**: a plain "this link is for Retailer accounts" message — no action form, no bypass.
- **The actual write is one shared function for both the self-serve and admin-assisted paths**: `lib/public-respond-actions.ts`'s `respondToListingAsRetailer(listingId, retailerId, action, params)` reuses the existing `getOrCreateThread`/`addOfferRound` from `lib/offers.ts` — no second negotiation state machine invented for this entry point. Because `retailerId` has to be a plain argument here (Admin needs to act on someone else's behalf, so it can't just come from `session.user.id`), authorization is checked explicitly inside the function itself — `isSelf = session.user.role === "retailer" && session.user.id === retailerId`, `isAdmin = session.user.role === "admin"`, reject unless one of those holds — rather than trusting the client component's own branching, since a client component could otherwise be made to call this with an arbitrary `retailerId`.
- **"Get shareable link"** (`components/seller/share-listing-link.tsx`) — a small client component (copy-to-clipboard, with the raw URL always shown as a visible fallback since clipboard access can fail) wired into the two places someone would actually want it: the seller's own listing detail page (`components/seller/listing-detail.tsx`, shared by both Grower and Processor — so both get this automatically) and Admin's platform-wide `/admin/listings`. Both gate it the same way the public page itself gates: only shown for `status === "active"` and `visibility === "all"`. Not added as a separate Account Executive-facing page — an AE-created listing posts under the seller's own identity (§13), so the seller (once they have login access) or Admin can always grab the link; building a third surface for AE specifically wasn't asked for and would need its own listings list that doesn't currently exist.

**Verified:** `npx tsc --noEmit`, `npx next build` clean, full `npm test` (31/31) still passing. Live in the browser end-to-end: as Admin, used the new license-number search to find a real Active grower license, created an account from it, posted a real listing, and confirmed "Get shareable link" appeared on both the seller's own listing page and `/admin/listings` with the correct copyable URL. Opened that URL — confirmed the public page renders the anonymized listing correctly and (logged in as Admin at the time) offers "Submit on a retailer's behalf" instead of the plain action buttons. Walked the full admin-assisted path: searched a real Active retailer license by number, created the account, confirmed the resulting form correctly attributed "Acting on behalf of {business name}," submitted Accept, and confirmed in the database that a real `OfferThread`/`Deal` was created with the correct retailer, price, quantity, and terms, and the listing flipped to `closed` — the exact same result a normal logged-in-retailer accept produces. The logged-out branch (no session at all) was verified a different way after the Browser pane's own session got stuck mid-test and wouldn't switch accounts (a known tool quirk, not an app bug — see the session's own notes): `curl` with no cookies against `/listing/[id]/respond?action=accept` confirmed the server correctly renders the "Create Retailer account" / "Sign in" links with the right `role=retailer` and `callbackUrl` query params baked in, and that `/signup?role=retailer&callbackUrl=...` correctly pre-selects the role and pre-fills the hidden `callbackUrl` field server-side — the same code path `RespondPanel` renders client-side, just confirmed from the actual HTML response instead of a driven browser session. All test accounts/listings created during verification were deleted from the database afterward.

## 37. Listing editing, per-row bulk-import photos/videos, and AE/Admin password reset (added 2026-08-19)

Three requests raised in one message: bulk-imported listings couldn't carry photos, a posted listing couldn't be changed at all once live, and there was no way to help a locked-out grower back into their account.

**A menu is now fully editable while it's still active.** Previously the only mutation available on a posted listing was `confirmListingFresh` (the "Still available?" freshness bump) — nothing touched price, quantity, THC%, terms, notes, or photos once posted. `lib/listings.ts`'s new `updateListing(listingId, callerId, input, newFiles, removedMediaIds, opts?)` fixes that: authorized for whoever posted it (`postedById`) **or** the Account Executive/Admin who built it on the seller's behalf (`createdBySalesRepId`) — the same two identities CLAUDE.md §13 already lets post a listing in the first place. `opts.bypassOwnership` is only ever passed `true` from an Admin-gated caller, letting any Admin fix any listing platform-wide, matching Admin's existing unrestricted reach elsewhere (`updateListingVisibility` has no ownership check either). Editing is blocked once a listing is `closed`/`expired`/`exclusive`-whatever-status-isn't-`active` — a listing mid-negotiation-and-sold is done, not a draft. Every edit also bumps `lastConfirmedAt`, matching the "any edit counts as a freshness signal" intent already noted in `schema.prisma`'s comment on that field (an edit feature was clearly anticipated there, just never built until now).

- **Seller's own portal**: `components/seller/listing-header-card.tsx` (new client component) replaces the old static top card on the Grower/Processor listing-detail page with an in-place Edit/Cancel toggle — no separate page, no navigation away from the negotiation thread underneath it. Wraps the new shared `components/seller/edit-listing-form.tsx`.
- **Account Executive's own portal**: new `/sales/listings/[id]` page — an AE can only edit a listing they personally built (`createdBySalesRepId` match), reachable via a new "Edit" link on each card in `/sales`'s "Listings you've posted" list.
- **Admin**: new `/admin/listings/[id]` page — Admin can edit **any** active listing platform-wide (`bypassOwnership: true`), reachable via a new "Edit" link on each card in `/admin/listings`.
- **Media management is part of the edit form, not bolted on separately**: `EditListingForm` shows every current photo/video as a clickable thumbnail (click to mark for removal, red border + "Will remove" label as feedback) alongside a new-files input to add more. `ListingMedia` previously had no delete path anywhere in the codebase (`grep` confirmed only a single creation-time `.create` call existed) — `updateListing` is the first place that removes rows, via `deleteMany` on the checked ids before computing `sortOrder` for anything newly added.

**Bulk-imported menus can now carry a photo/video per strain, not zero.** The bulk "paste a whole price list" flow (`structureListingDraftsBulk`, CLAUDE.md §34) explicitly shipped text-only, with the UI stating outright "Bulk-posted listings don't carry photos." A real Account Executive asked for exactly this gap to close. Fixed without inventing a second upload mechanism: `components/seller/listing-form.tsx` tags every AI-drafted row with a client-only, stable `_key` (`crypto.randomUUID()`, never sent to the AI call itself) the moment results come back — a plain array index would desync a row's file input from the row it belongs to the instant an earlier row gets removed from the review table, or worse, silently attach to whatever row slides into that freed index next. Each table row gets its own `<input type="file" name="media_<key>">`; on submit, `lib/sales-actions.ts`'s `handleCreateListingsAsAssistantBulk` reads `formData.getAll(`media_${row._key}`)` per row (the same `_key` travels inside the row's own JSON, so the correlation survives `JSON.stringify`/`JSON.parse` round-tripping through the hidden `drafts` field) and passes those files into the existing `createListing(...)` — no new media-saving path, just wiring real files into the one that already existed for the single-item flow.

**No self-serve "forgot password" flow exists anywhere in this app** (no email-sending infra, same gap already on record for SMS — CLAUDE.md §28/§29) — so a locked-out grower/processor has always had no way back in except calling someone. `lib/account-management.ts`'s `resetUserPassword(actorRole, targetUserId, newPassword)` is that "someone": scoped by actor —

- **Account Executive**: can only reset a Grower or Processor's password (`SALES_REP_ASSISTABLE_ROLES`, the exact same scope as `searchAssistableSellers`/`handleCreateListingAsAssistant` — an AE has a standing relationship with these two roles and no others). New `components/sales/reset-password-panel.tsx` on `/sales` reuses the **existing** seller-search action (`searchSellersAction`) rather than building a second lookup UI, then takes a new password and calls the new `resetSellerPasswordAction` (`app/sales/actions.ts`).
- **Admin**: can reset **any** user's password platform-wide, same unrestricted-reach posture as everywhere else Admin operates. Added as a new "Reset password" column on `/admin`'s existing "All users" table — an inline text input + Reset button per row, matching the exact UI pattern the adjacent "Commission rate" column already established, rather than a new modal or page.
- Both surfaces show the new password back to the person resetting it (same "relay this to them directly" convention already established for Budtender/staff-account creation, §21/§33) — this app has never sent anything by email or SMS, so there's no other way for the new password to reach the account holder.

**A real, pre-existing bug found and fixed while verifying this batch, unrelated to any of the three requests above.** `components/seller/share-listing-link.tsx` (§36) computed its displayed URL as `typeof window !== "undefined" ? window.location.origin + ... : "/listing/..."` — a textbook React hydration mismatch (server has no `window`, so it always rendered the relative path there, then the client re-rendered with the full origin, diverging from what SSR sent down). Next's own dev overlay caught it live ("Recoverable Error... Hydration failed") the first time this session re-rendered a listing detail page after other changes. Fixed by starting `origin` at `""` via `useState` and filling it in with `useEffect` after mount — server and client's first paint now render identically (the relative path), and the real origin only appears once hydration has already completed, which is also exactly when `window` is guaranteed to exist.

**Verified:** `npx tsc --noEmit`, `npx next build`, full `npm test` (31/31) all clean. Live in the browser: as Admin, posted two bulk-import rows (Blue Dream, White Truffle) with a synthetic image attached to only the first row, submitted, and confirmed in the database that exactly the first listing's `ListingMedia` had one row and the second had zero — proving the `_key` correlation survives a real submit, not just holds up on paper. Opened the Admin edit page for that listing, changed price and marked the one photo for removal, saved, and confirmed in the database the price updated, `lastConfirmedAt` bumped, and `media` was empty. Reset a real grower's password from the Admin table, confirmed via `bcrypt.compare` against the stored hash that the new password actually took, then reset it again from the Account Executive's own panel and confirmed that too. Logged in as that grower with the AE-set password, opened their own listing detail page, used the in-place Edit toggle, changed quantity, saved, and confirmed the display updated without a page navigation. Confirmed the hydration-error overlay was gone after the `ShareListingLink` fix. All test listings created during verification were deleted from the database afterward.

## 38. Account Executive "Accounts" restructuring, exclusive grower assignment, and a second Lead Directory data-loss recovery (added 2026-08-19)

Three requests raised in one message, plus one more moments later that folded into the same build.

**"It took every strain and did it as a listing, instead of one comprehensive menu under the Grower."** Read carefully, this wasn't a request to merge multiple strains into one `Listing` record — the negotiation engine fundamentally operates per-strain (a Retailer accepts/counters one specific product at one specific price/quantity; merging strains into a single record would break that). The actual gap was on the Account Executive's *own* side: `/sales` only ever showed a flat, ungrouped feed of individual listings (`listingsCreatedByAssistant`), with no concept of "this grower, and everything they have." Fixed by adding an account-centric layer on top of the existing per-listing data, not by changing how listings themselves work:

- **`accountsForSalesRep`/`accountDetailForSalesRep`** (`lib/sales-actions.ts`) — a new `/sales/accounts` list (one row per grower/processor this rep is assigned to) and `/sales/accounts/[id]` detail page showing the seller's real profile (business name, license number/type/status, address, phone, email — directly answering the follow-up "each grower's profile should be visible to the assigned account representative") plus their **entire** menu — every listing they've ever posted, active or not, not just ones this particular rep happened to create.
- **The menu is editable right there**, via the same `EditListingForm` already built for §37, wrapped in a new lighter `components/sales/account-menu-item.tsx` (no freshness-bump/share-link clutter — those belong on the seller's own dashboard, not the AE's account view). A new `handleEditListingFromAccount` (`lib/sales-actions.ts`) redirects back to the account page on save instead of a listing-specific page, since that's where the rep actually is.
- **`updateListing`/`getListingForEdit`'s authorization** (`lib/listings.ts`, §37) gained a third path: the Sales Rep currently assigned to the listing's seller can edit it, not just whoever personally created that specific listing (`createdBySalesRepId`) or the seller themselves (`postedById`) — an account is dedicated to one rep now, so they need to manage the whole thing, including listings a *different*, earlier rep or the grower themselves originally posted.
- **Password reset lives on the account page too** (`components/sales/inline-reset-password.tsx`, reusing §37's `resetSellerPasswordAction` directly with the known seller id — no search step needed since the rep is already looking at the right account).

**A separate, explicitly declined follow-up in the same exchange: "account representative should be able to see user name and password of the grower in case grower forgot."** Refused outright, not scoped down — this isn't a design choice to negotiate, it's cryptographically impossible. Passwords are stored as one-way bcrypt hashes specifically so nobody, including this app's own backend, can recover the original value; there is no "reveal" operation for a hash. §37's password *reset* (set a new one, relay it to the grower) is the correct, standard answer to "grower forgot their password," and is what's surfaced on the account page instead.

**"Once an account representative is working with a grower, other account representatives can no longer work with that grower — dedicated to that rep."** A real, confirmed new business rule, implemented as `User.assignedSalesRepId` (self-relation, hand-written migration `prisma/migrations/20260819000000_sales_rep_assignment/`, `onDelete: SetNull` — removing a rep frees their growers up rather than orphan-deleting them). Claims silently on first contact rather than requiring a separate "claim" step:

- **`claimOrVerifySellerAssignment`** (`lib/sales-actions.ts`) — called at the top of every AE write path that touches a seller (`handleCreateListingAsAssistant`, `handleCreateListingsAsAssistantBulk`) and inside `createAssistedSellerAccount` (`lib/assisted-seller-signup.ts`, when the actor is `sales_rep` — Admin creating one leaves it unclaimed, since Admin isn't a rep in this model). If the seller is unclaimed, the acting rep claims it and the flow proceeds; if already claimed by someone else, it's rejected with **"This grower/processor is already working with another Account Executive."**; if claimed by the same rep, it's a no-op pass-through.
- **`searchAssistableSellers`** (`lib/sales-actions.ts`) now takes the caller's role/id and, for a Sales Rep, excludes any seller already claimed by a *different* rep from search results entirely (`app/sales/search-action.ts` passes this through — a bug caught during verification: the wrapper wasn't threading the actor info through at all, so the very first live test showed a second rep still seeing an already-claimed grower in search results before this fix). Admin's own search (`app/admin/search-action.ts`) stays fully unrestricted, matching Admin's unrestricted reach everywhere else this lock applies — this is an AE-to-AE exclusivity rule, not an AE-vs-Admin one.
- **Admin gained a read-only "Assigned rep" column** on `/admin`'s existing "All users" table (`allUsers()` now includes `assignedSalesRep`) — oversight, not a new capability; Admin was already exempt from the lock itself.
- **"Once assigned, it should be noted in the Marketing lead directory under rep."** `lib/leads.ts`'s new `markLeadAssignedRep(businessName, repName)` best-effort matches the claimed business against the existing Lead CRM (§20) by normalized name — same join-key logic already used by `scripts/enrich-license-registry-phone.mjs` to bridge the CRA registry and the Lead export (the two datasets use incompatible license-number schemes, so name is the only reliable key). Fires automatically inside the claim path and at assisted-account creation. Best-effort and silent on no-match by design — `Lead.assignedRepName` is a courtesy note for the CRM view, `User.assignedSalesRepId` is the actual source of truth for the exclusivity rule itself, so a name that doesn't match closely enough (or a seller with no corresponding Lead record — true for most seed/test businesses) just doesn't get annotated, without blocking or erroring the claim.

**A second, independent data-loss discovery: the `leads` table (§20) was completely empty — 0 rows.** Same underlying class of incident as §25/§35's `LicenseRegistry` wipe, just not yet noticed for this table since nobody had looked at the Marketing suite since. Recovered in two parts:
- **MI Processors (436) and MI Dispensaries (1,276)** — these were always CRA-registry-derived, not from the source CRM tool's own export (§20), so a new one-time script (`scripts/restore-leads-from-license-registry.mjs`) rebuilds them directly from the already-restored `LicenseRegistry` table instead of needing the original state CSV files again (which are no longer on disk anywhere in this environment). Produces the identical shape `scripts/import-leads-from-cra.mjs` always did (`disposition: "NO_PHONE"` — LicenseRegistry's own phone enrichment, §12, only ever covered grower rows, never processor/retailer).
- **Lead Directory (MI) — 530 real business contacts with phone numbers** — this data has no derivable source anywhere else in this database (it only ever existed in the platform owner's own CRM tool export). The human re-supplied a fresh export (`lead-overrides-v1-export-2026-08-19.csv`) and it was re-imported via the existing `scripts/import-leads.mjs`, unchanged. Total restored: 2,242 leads across all three lists, closely matching the original documented count (2,243) — the 1-record difference is expected month-to-month churn in the source data, not a sign of an incomplete restore.

**Verified:** `npx tsc --noEmit`, `npx next build`, full `npm test` (31/31) clean. Live in the browser end-to-end with two real Account Executive sessions: posted a listing for an unclaimed grower as AE #1, confirmed the grower now shows up under "My accounts" with all 3 of their listings (not just the new one) grouped together, edited the active listing's price from the account page and confirmed it saved and redirected back to the account (not a listing sub-page). Logged in as a second AE and confirmed the same grower no longer appears in seller search at all. Directly bypassed the search UI (injected the claimed grower's real id into the hidden `sellerId` field via the DOM, since the UI itself already prevents selecting them) to prove the block is enforced server-side, not just hidden client-side — got back the exact `"This grower/processor is already working with another Account Executive."` message, and confirmed in the database that no listing was created. Confirmed in the database that the claim itself correctly set `assignedSalesRepId`. All test accounts/listings created during verification were deleted afterward.

## 39. §38 follow-through: accounts as the AE landing page, a real-data backfill, and AI contact lookup for CRA-derived leads (added 2026-08-19)

Three follow-ups to §38, raised once real usage surfaced a gap the fresh test data hadn't.

**The account-grouped view was live but easy to miss.** §38 shipped it as a new "My accounts" tab sitting *next to* the pre-existing flat "Listings you've posted" feed on `/sales` — a real Account Executive went looking for it, found the old flat feed still showing individual strains ungrouped (specifically a 34-listing grower posted as 34 separate rows with no sense of whose menu they belonged to), and didn't realize a different tab already had what they wanted. The actual fix wasn't a missing feature, it was that the grouped view wasn't where a rep would naturally land. **`/sales` itself is now the accounts list** (`accountsForSalesRep`, not `listingsCreatedByAssistant`) — the flat feed is gone, replaced outright rather than kept as a second competing view. `/sales/accounts` still exists as a plain redirect to `/sales` so nothing bookmarked or linked from earlier breaks; every `/sales/*` page's nav was updated to a single "My accounts" entry pointing at `/sales` instead of the old "My activity"/"My accounts" pair.

**A real data gap found while chasing that report down: the claiming logic only fires on new activity, never retroactively.** The 34-listing grower (real production data, not a test fixture) had every listing correctly tagged `createdBySalesRepId` from before §38 shipped, but `User.assignedSalesRepId` itself was still null — the account-claiming code didn't exist yet when those listings were created, so nothing ever set it. New one-time `scripts/backfill-sales-rep-assignments.mjs` closes this gap: finds every grower/processor with `assignedSalesRepId` still null but at least one listing already carrying a `createdBySalesRepId`, and assigns them to whichever rep created their *earliest* listing (closest available proxy for "who made first contact"), skipping any case where that creator turns out to be Admin rather than a real Sales Rep (Admin was always meant to stay exempt from this lock, not get silently opted into "owning" an account through a backfill). Idempotent, safe to re-run if this surfaces again. Also manually ran `markLeadAssignedRep` (§38) for this one business by hand, since the automatic call only fires from the live claim/creation code paths the backfill script deliberately doesn't touch (it's a data-repair script, not a re-run of the whole creation flow).

**AI-assisted phone/email lookup for MI Processors/Dispensaries — the two lead lists sourced straight from the state CRA registry (§20/§38) and, for that reason, permanently short one thing: no contact info at all**, since the state's own export never carried it. Explicitly *not* built as a bulk "search everything" operation across all ~1,700 of those records — an AI web search can attribute a wrong number to a business, and handing a rep a spreadsheet of unreviewed possibly-wrong numbers is worse than the honest "no phone" gap already shown today. Instead:

- **`lib/lead-contact-lookup.ts`'s `lookupLeadContactInfo`** — a real live call (`client.beta.messages.create` with the `web_search_20250305` server tool, `max_uses: 3`) using the lead's own business name/address/license number as the query, explicitly instructed not to guess or fabricate a number it can't attribute to a real source. Scoped to `mi_processors`/`mi_dispensaries` leads only — the Lead Directory (MI) list already has real phone data, and this isn't a general-purpose lookup tool. Same "not configured" fallback as `lib/ai-listing.ts` when `ANTHROPIC_API_KEY` is unset.
- **A per-lead "Find contact info" button** (`components/leads/contact-lookup-button.tsx`), shown only on a Processor/Dispensary row still missing a phone or email. Clicking it runs the search and shows the result **for review** — phone, email, and the source it was attributed to — with explicit "Use this" / "Discard" actions. Nothing is written to the `Lead` record until the rep clicks "Use this" (`applyLeadContactInfo`), which also flips `disposition` off `"NO_PHONE"` once a real phone is on file, since that disposition specifically means "nothing to call."
- Wired into both `/sales/marketing` and `/admin/marketing` (`lookupContactAction`/`applyContactInfoAction` added to each page's own `actions.ts`, matching the existing per-page action-wrapper convention this file already used for every other lead mutation).

**Verified:** `npx tsc --noEmit`, `npx next build`, full `npm test` (31/31) clean. Live in the browser: confirmed `/sales` now shows the account-grouped view directly with no separate tab needed. Ran the backfill script against the real database, confirmed the 34-listing grower's `assignedSalesRepId` was set to the correct real rep and the matching Lead Directory record picked up `assignedRepName`. Ran a real AI contact lookup against an actual MI Processor lead ("100 Shafer Processing LLC") — the search returned a real phone number and email with a cited source (a Yelp listing for a co-located business at the same licensed address, which the model correctly surfaced as a caveat rather than silently presenting as certain), clicked "Use this," and confirmed in the database that `phone`/`email` were saved and `disposition` flipped from `NO_PHONE` to `NEW`, with the UI's own filter counts updating to match live.

## 40. Menus as a real grouping, not just "an account's listings" (added 2026-08-19)

A follow-up to §38/§39, raised the moment a real Account Executive looked at "123 Grow LLC"'s account page: the grower's 34 strains were correctly grouped *under the grower*, but still shown as 34 separate flat rows underneath — not as the one bulk upload they actually were. The human's framing: "the entire group makes one menu, sometimes there could be several menus each menu containing multiple strains and products." That's a real, distinct grouping level this app didn't have: AE → **Account** (§38) → **Menu** (this section) → individual strain `Listing`s.

**`Listing.batchId`** (new, nullable `String`, hand-written migration `prisma/migrations/20260819010000_listing_batch_id/`) — every listing created in one form submission shares one `batchId`, generated once per submission and stamped on every row it creates. `lib/listings.ts`'s `createListing` now takes an optional `batchId` param (defaulting to a fresh `crypto.randomUUID()` when omitted, so a single-item post is simply a menu of one — no special-casing needed anywhere else). `lib/sales-actions.ts`'s `handleCreateListingsAsAssistantBulk` generates exactly one `batchId` before its per-row loop and passes the same one to every `createListing` call inside it, so a 34-row bulk paste becomes one menu with 34 products, and pasting a second list next week becomes a second, separate menu — not merged with the first just because it's the same seller.

**Grouping happens in `lib/sales-actions.ts`'s `accountDetailForSalesRep`**, not at the database query level — it fetches the seller's listings as before, then `groupListingsIntoMenus` buckets them by `batchId` (falling back to a listing's own `id` as a singleton group for anything with no `batchId` at all) and sorts menus newest-first. `/sales/accounts/[id]/page.tsx` renders each menu as its own labeled section ("Menu uploaded Aug 19, 2026 — 2 products"), with `AccountMenuItem`'s existing per-strain edit toggle unchanged underneath.

**Existing listings predate this field, so a one-time inference backfill was needed** — same shape of gap as §39's assignment backfill, and hit by the identical real "123 Grow LLC" data. `scripts/backfill-listing-batches.mjs`: per seller, sorts their `batchId: null` listings by `createdAt` and buckets consecutive ones within a 2-minute gap into one inferred batch (a real bulk-import loop creates its rows back-to-back within seconds; two genuinely separate uploads are essentially never that close together) — there's no exact record of which old listings were originally submitted together, so this is a best-effort inference, not a guarantee, same honesty posture as every other backfill in this app. Idempotent, only touches rows still `batchId: null`. Run against the real database: inferred 4 menus covering 37 previously-unbatched listings, including correctly consolidating "123 Grow LLC"'s 34 strains into the single menu they were actually uploaded as.

**Verified:** `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) clean. Ran the backfill against the real database and confirmed via direct query that all 34 of "123 Grow LLC"'s listings now share one `batchId`. Live in the browser: posted a real 2-strain bulk submission for a test grower, then a separate single-strain listing for the same grower right after — confirmed the account page showed exactly two menu sections, "2 products" and "1 product," not three flat rows or one merged group of three. (First attempt at this same test silently created nothing, traced to a dev-server cold-compile hiccup on that route right after a restart — not a bug in the feature itself; a clean retry on the warmed-up route worked correctly and is what's recorded here.) Test listings deleted afterward.

**Also fixed in this pass: the Vercel build itself was failing on every deploy since §38 shipped**, unrelated to menus but caught while getting this live. `package.json`'s `build` script was just `next build`, relying on `@prisma/client`'s own `postinstall` hook to regenerate the Prisma Client from `schema.prisma`. Vercel reused a cached `node_modules` across deploys (since `package.json`/lock weren't changing, only `schema.prisma`), so `npm install` was a no-op and that hook never re-fired — leaving a stale, older-schema Prisma Client baked into the build even though the committed `schema.prisma` was current, producing a wall of "property does not exist" TypeScript errors that don't reproduce locally. Fixed by changing `build` to `prisma generate && next build`, so the client is always freshly regenerated regardless of the install cache. Confirmed fixed via a real production deploy (`vercel --prod` from the repo root, after first correcting an accidental stray Vercel project created by running the CLI from the wrong directory — deploying from inside `web/` instead of the repo root, where the project's configured Root Directory setting expects a `web/` subfolder to exist).

**Two immediate follow-ups once this was live, both from the same real "123 Grow LLC" example.** First: the human expected clicking a menu to be what *reveals* its products, not to always see all 34 flattened under a label — `components/sales/menu-section.tsx` (new client component) makes each menu collapsed by default, a single clickable header row ("Menu uploaded Aug 19, 2026 — 34 products") that expands in place via a chevron toggle, with `AccountMenuItem`'s existing per-strain edit UI unchanged underneath once opened. Second: a menu itself should be shareable as one link, not just one strain at a time (§36) — `lib/listings.ts`'s new `publicMenuView(batchId)` mirrors `publicListingView`'s exact rule (only `status: "active"` **and** `visibility: "all"` rows, silently omitting anything Admin-restricted rather than exposing it) but returns every qualifying listing in the batch instead of one; `/menu/[batchId]/page.tsx` is the public page (no `requireRole`, same boundary as `/listing/[id]`), rendering each product as a card that links out to that listing's own existing `/listing/[id]` page for the actual Accept/Counter/Decline flow — this page is a browse view, not a second negotiation surface. `components/sales/share-menu-link.tsx` (menu-level twin of `share-listing-link.tsx`) only renders on `MenuSection`'s header when at least one listing in the batch actually qualifies — a menu that's entirely closed or entirely Admin-restricted just doesn't offer the button, same "quietly not available" posture as the public page itself.

**A larger follow-up request landed the same session, was scoped through several rounds of clarifying questions, then built.** The full request, assembled across many messages: retailers should be able to select custom quantities across multiple products at once instead of accepting/countering one listing at a time; wholesale pricing implies a minimum order, but as a soft per-listing quantity/price tier ("a grower selling 500 lb might still sell 20 lb, just at a different price") not a hard reject; smaller orders default to cash-on-delivery, with the grower free to approve other terms and bearing full responsibility for that choice, with the Account Executive held harmless; and several menus (even from different growers) should bundle into one shareable link.

## 41. Cart orders, tiered pricing, and menu collections (added 2026-08-19)

**The core design decision: reuse the existing per-`(listing, retailer)` negotiation engine, don't replace it.** A "cart order" is not a second state machine — `lib/cart-orders.ts`'s `submitCartOrder` creates one ordinary `OfferThread` + initial `OfferRound` per selected product via the exact same `getOrCreateThread`/`addOfferRound` every other negotiation in this app already goes through, just tagging each resulting thread with a shared `OfferThread.cartOrderId` (new, nullable FK to a new lightweight `CartOrder` model) purely for grouping. Every line item still negotiates completely independently after that — a seller can accept one product from a cart submission, counter another, and reject a third, because each is just an ordinary thread. This directly answers this session's own earlier question #1 ("line-item response, not all-or-nothing?") — yes, by construction, since nothing about "it came from a cart" changes how `addOfferRound` behaves.

**Tiered pricing is per-listing and a soft threshold, not a hard reject.** `Listing.minimumOrderQuantity`/`belowMinimumPricePerUnit` (new, both nullable) — `pricePerUnit` applies at `minimumOrderQuantity` or more, `belowMinimumPricePerUnit` applies to anything smaller; both null means no tiering, same as before this existed. Set by the grower/processor on the existing `EditListingForm` (single-item mode only — bulk-import doesn't get these two fields in this pass, a deliberate scope cut, editable afterward from the listing's own edit form like anything else). `submitCartOrder` recomputes the effective price server-side from the listing's own stored tier rather than trusting whatever the client submitted, same posture as every other price-sensitive path in this app; a submission at exactly the tiered price auto-accepts (matching `isAsPosted`'s existing logic), anything else becomes an ordinary counter for the seller to review.

**A real bug this exposed and fixed: accepting used to close the WHOLE listing regardless of how much was actually accepted.** Before cart orders, nothing in this app's UI ever let a retailer request less than a listing's full posted quantity, so `Deal.finalQuantity` always equaled `Listing.quantity` and unconditionally closing the listing on accept (`lib/offers.ts`'s `addOfferRound`) was always correct. A cart order buying 20 lb out of a 500 lb listing broke that assumption — closing the whole listing after a 20 lb sale would have pulled the remaining 480 lb out of the marketplace. Fixed by computing `remainingQuantity = listing.quantity - finalQuantity` and only closing the listing when it hits zero, otherwise decrementing `Listing.quantity` and leaving it active. Safe for every pre-existing code path (finalQuantity always equals the full listing quantity there, so remainingQuantity is always exactly 0, same "close" behavior as before) — this new branch can only ever activate through the new cart flow.

**Menu collections — "an account rep has 14 menus from 14 different growers, all 14 can be grouped and shared with one link."** New `MenuCollection` model (`salesRepId`, `batchIds: String[]` — a plain array, not a join table, matching the same lightweight-grouping choice `Listing.batchId` itself already made rather than inventing two different patterns for the same idea). `lib/menu-collections.ts`'s `menusForCollectionPicker` lists every menu across the AE's own assigned accounts (§38's exclusive-assignment scoping applies here too — an AE can only ever bundle menus belonging to sellers actually assigned to them); `/sales/collections/new` is a checkbox picker gated behind the terms disclaimer (below); `publicCollectionView` aggregates every currently-shareable listing (`status: "active"` **and** `visibility: "all"`, same rule as `publicListingView`/`publicMenuView`) across every batch in the collection, grouped by the seller's `anonHandle` — **never by the seller's real internal id**, which would have been the exact class of RSC-payload identity leak §21 already found and fixed once in this app; caught and corrected during this build before it shipped, not after.

**The public cart page — `components/cart/cart-builder.tsx`, shared by both `/menu/[batchId]` (one seller) and the new `/collection/[id]` (possibly several)** — a quantity input per product, live tiered-price computation as the visitor types, a running total, and a terms selector defaulting to **cash**, with inline copy stating smaller orders typically need cash on delivery but a seller can agree to other terms as their own call. Same public/gated-action split as §36's listing page: anyone can browse and build a cart, but submitting requires a logged-in Retailer session (`lib/public-cart-actions.ts`'s `submitPublicCartOrder` resolves the retailer id from the verified session, never a client-supplied value, same authorization posture as `respondToListingAsRetailer`) — a logged-out visitor sees the same sign-in/sign-up links §36 already built, carrying the same `callbackUrl` pattern back to the cart page. **Deliberately not built in this pass: an Admin/AE-assisted cart submission** (§36's admin-assisted retailer flow has no cart-order equivalent yet) — a logged-out or wrong-role visitor just can't submit, matching scope to what was actually asked and tested.

**The AE liability disclaimer is a client-side gate at collection-creation time, not a database-tracked acceptance record** — `components/cart/terms-disclaimer.tsx` holds the draft text (same "not attorney-reviewed" posture as every other legal-adjacent surface in this app), shown on `/sales/collections/new` with a required checkbox before "Create link" enables, but not logged the way the non-circumvention agreement's `AgreementAcceptance` is. A deliberate lightweight choice for a first pass, not an oversight — `CartOrder.facilitatedBySalesRepId`/`disclaimerAcknowledgedAt` exist in the schema for a future AE-assisted-submission flow but are unused (always null) until that flow is actually built.

**The Account Executive gets notified when a grower approves non-cash terms** — a small addition to `addOfferRound`'s existing accept branch (`lib/offers.ts`): when a `Deal` closes with `finalTerms !== "cash"` and the listing has `createdBySalesRepId` set, that AE gets an in-app notification via the same `notify()` every other event in this app already uses. They're not a party to the deal and bear no responsibility for the terms (the disclaimer above), but they're the one who connected the parties, so this seemed worth telling them regardless.

**Also surfaced and fixed during this build, unrelated to cart orders**: `User.minimumOrderValue` (a per-grower dollar floor) was added in an earlier pass this same session before the human's worked example ("500 lb listed, 20 lb order, same or different price") made clear the real model is per-listing and a soft price tier, not a hard per-grower reject. Since it had shipped in a migration already applied to the live database but was never read by any committed code, it was cleanly dropped in a follow-up migration rather than left as a second, contradictory "minimum order" concept sitting unused in the schema.

**Verified:** `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) clean. Live end-to-end against the real database: created a 500 lb listing with a 50 lb tier (\$900/lb at 50+, \$1100/lb below), opened its public menu link logged out (correctly gated), signed in as a real retailer, requested 20 lb — confirmed the tiered \$1100/lb price computed live in the UI — and submitted; confirmed in the database the resulting `OfferThread` auto-accepted at exactly `price: 1100, quantity: 20`, a real `Deal` was created with matching `finalPrice`/`finalQuantity` and an auto-generated invoice, the `CartOrder` row linked correctly, and — the specific bug this session found — the listing stayed `status: "active"` with `quantity` correctly decremented from 500 to 480 rather than closing outright. Separately built a real 2-menu collection as the Account Executive (checkbox picker → disclaimer → link), then confirmed via a genuinely cookie-less `curl` request (matching the public/anonymous boundary this page is supposed to have) that both menus' products rendered together on the resulting `/collection/[id]` page. All test listings/threads/deals/collections created during verification were deleted from the database afterward.

**Still not built, flagged rather than silently dropped**: letting whoever generates a menu or collection link restrict it to specific retailers (the original "who to share with" ask) — this touches decision #3's exclusivity question the same way §17/§18 already had to work through once, so it needs the same kind of explicit confirmation before building, not a quiet default. Also deferred: per-item accept/counter grouped *display* (today each cart-order line becomes its own ordinary thread visible wherever threads are already shown — grower/retailer/broker dashboards don't yet have a "these N threads came from one cart order" grouped view, though the data to build one, `OfferThread.cartOrderId`, already exists); a cart-level expiration separate from each listing's own; and live progress-toward-minimum-order UI (the tiered price already updates live, but there's no "add $X more to unlock this seller's better rate" framing yet).

## 42. Cart UX fixes, broadcast-to-role sharing, Admin/AE feature parity, and order cancellation (added 2026-08-19)

Four follow-ups from the same conversation as §41, landed once real usage of the just-shipped cart/collection pages surfaced gaps.

**A. The cart page defaulted every product to 0, not the full amount offered — fixed, plus a real three-way Accept/Counter/Reject flow.** The human's report after actually receiving a real menu link: "the cart has 0 for each product it should display the full amount available as that is the wholesale offer to the retailer... retailer should see a display that says accept offer, counter offer or reject." `components/cart/cart-builder.tsx` was rewritten around that framing rather than patched:
- Every quantity input now initializes to the listing's own full `quantity` (`fullOfferQuantities`), not empty/0 — the page now reads as "here is the seller's actual offer," not a blank order form. A +/- stepper sits next to each quantity input (`lucide-react`'s `Minus`/`Plus`) for quick adjustment without having to type, step size `1` for `lb`/`unit` and `0.1` for `liter`.
- **Per-product price is now directly editable too**, not just quantity — a follow-up in the same exchange: "in a counter offer it should allow retailer to make an offer per product that is different than the one offered." Each row got a `$___/unit` input defaulting to the seller's own (tier-aware) price; typing a different number becomes that line's real `CartItemInput.price` sent to `submitCartOrder` — a genuine per-product price counter, not just quantity. A line that's been touched (quantity and/or price) gets an amber border and an inline note ("Adjusted from the full N lb offered" / "Price adjusted from the seller's $X/lb") so it's visually obvious which lines are still exactly as offered and which aren't.
- **Three explicit buttons replace the old single "Submit order":** **Accept offer** always sends the seller's full original menu — full quantity, listing price, listing terms, on every line — computed and sent directly (not through `setState`-then-read, which would race against React's async state update and silently send stale values) regardless of whatever the retailer had been fiddling with in the boxes, so clicking it is never ambiguous. **Counter offer** sends whatever's currently in the cart (disabled while the cart still exactly matches the full offer, since there'd be nothing to counter) — this still doesn't invent a second server-side path; it's the same `submitCartOrder` as before, whose existing per-line `isAsPosted` check (§41) already resolves each line to a real "accept" or "counter" based on whether it matches the listing exactly. **Reject** is purely client-side — no thread exists yet at this point in the flow, so there's nothing to reject server-side; it just shows a "You declined this offer, nothing was sent" confirmation and stops there.
- The payment-terms selector now defaults to the **first listing's own terms** (not a hardcoded "cash") — matching it exactly on an untouched cart is what lets "Accept offer" resolve to a true accept server-side, since `submitCartOrder` compares `requestedTerms` against each listing's own `terms` field.

**B. "Share with all retailers" / "share with all processors," next to the existing "Share menus with a link."** The manual collection builder (§41) already covers "hand-pick specific menus, copy a link, send it yourself" — this is the one-click broadcast version. `lib/menu-broadcast.ts`'s `broadcastAllMenus(actorRole, targetRole)` bundles every one of the actor's own currently-shareable menus into one `MenuCollection` (the same `createMenuCollection` the manual builder uses) and pushes that link to every user of the chosen role platform-wide via the existing in-app notification bell — this app has no email/SMS sending infrastructure anywhere (§28/§29's Twilio stub is the closest thing, and it's permanently stub-only), so an in-app notification is the real, working mechanism here, not a placeholder for a future one. Retailers can act on the resulting link immediately through the cart flow above; Processors get the same link for visibility only — this app still has no processor-side cart/checkout flow (a Processor's own wholesale sourcing is the separate `SplitContract` flow at `/processor/sourcing`, §10), so this is informational parity, not a claim a Processor can check out a cart today. `components/notification-bell-client.tsx` gained a small `renderMessage` linkifier (a regex for `/collection/...` or `/menu/...` inside the message text) since every other notification in this app is plain text with nothing to click through to — without it, a broadcast notification would show the right words but the link itself would be dead text in the bell dropdown.

**C. "Admin portal should have all same features and more than account rep."** Everything §38-§41 built — the Accounts-grouped view, the menu-collection builder, and now the broadcast buttons above — existed only under `/sales/*`, gated to `sales_rep`. Rather than bolt Admin access onto the AE's own routes, Admin got its own parallel set at `/admin/accounts`, `/admin/accounts/[id]`, `/admin/collections/new`, `/admin/collections/[id]` — same components and much of the same underlying logic, reused rather than duplicated (`lib/menu-collections.ts`'s `handleCreateMenuCollection`/`menusForCollectionPicker` already took an `actorRole` param from earlier work; `menusForAdminCollectionPicker` and `lib/admin.ts`'s `accountsForAdmin`/`accountDetailForAdmin` are the new unscoped counterparts; `lib/sales-actions.ts`'s `handleEditListingFromAccount` gained an `accountsBasePath` param so the exact same in-place-edit flow works from either `/sales/accounts/[id]` or `/admin/accounts/[id]`). The one deliberate difference, matching "more than" rather than just "same as": Admin's versions are **platform-wide**, not scoped to one rep's own assigned accounts — `accountsForAdmin` has no `assignedSalesRepId` filter at all, matching Admin's existing unrestricted reach everywhere else in this app (`updateListing`/`getListingForEdit`'s `bypassOwnership`, `updateListingVisibility`, etc.). Password reset wasn't duplicated onto the new Admin account page — Admin already has that on `/admin`'s own "All users" table (§37), so adding a second reset UI there would just be redundant. All ten `/admin/*` pages' nav arrays (previously hand-duplicated per file, confirmed byte-identical before editing) picked up the new "Accounts" and "Orders" (see below) entries via the same substitution across every file rather than by hand per-page, to keep them from drifting out of sync with each other.

**D. "A button for account rep to cancel an order."** No page anywhere in this app listed `CartOrder`s at all before this — `lib/cart-order-management.ts`'s `ordersForActor(actorRole, actorId)` is the first one, surfacing every cart order touching an AE's own assigned accounts (or, for Admin, every cart order platform-wide), grouped by order with each line's own independent status shown (a cart order is several ordinary `OfferThread`s, never one combined status — same "no second state machine" discipline as §41). Canceling (`cancelCartOrder`) does not invent a new kind of authority: it finds every still-`open` thread in that order belonging to an account this actor actually has standing over, and calls the exact same `addOfferRound(..., action: "reject")` any seller-side rejection already goes through — **acting with the listing's own seller identity** (`actorRole: "seller"`, `actorId: listing.postedById`), the same "AE acts as the seller they're assigned to" posture already established for editing a listing on their behalf (§13/§38), not a new capability being invented for this feature specifically. A cart order spanning several sellers (e.g. from a multi-menu collection) only has its *own* lines canceled by a given actor — a line belonging to a different rep's account is left untouched, both in what's displayed and what a cancel action is allowed to touch. New `/sales/orders` and `/admin/orders` pages (`components/sales/orders-list.tsx` shared by both, `components/sales/cancel-order-button.tsx` for the confirm-then-cancel button) — the button calls `router.refresh()` after a successful cancel so the line's status badge updates immediately instead of showing stale "OPEN" until a manual reload, a real gap caught during live testing (the server action completes and the database is correct immediately, but a Server Component page has no reason to know to re-render on its own without that nudge).

**Verified:** `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) clean, all after every change in this section. Live in the browser against the real database: logged in as a real retailer and opened an actual 34-product menu link — confirmed all 34 quantity boxes pre-filled at their full listed amounts, the running total computed correctly ($744,357.00 across all 34), the three buttons rendered with **Counter offer** correctly disabled while the cart still matched the full offer; clicked a stepper to drop one line by 1 lb and confirmed the row turned amber with the "adjusted" note, the total recalculated, and **Counter offer** became enabled; clicked **Reject** and confirmed a clean client-only "declined" state with no server write. Logged in as a real Admin: `/admin/accounts` correctly listed all 4 real grower/processor accounts platform-wide (including ones with no AE assigned, which never show on any AE's own `/sales` page) with each one's assigned-rep status; clicked **Share with all processors** and confirmed a live "Sent to 1 account" result, then confirmed in the database a real `MenuCollection` and a `Notification` row existed with the correctly formatted, now-linkified `/collection/...` URL. Built one real (minimal, single-line) test `CartOrder` directly against production data to exercise the cancel path specifically: confirmed it appeared correctly on `/admin/orders` with the right retailer handle, seller name, and quantity/price; clicked **Cancel order** → **Yes, cancel**, confirmed the UI updated live to "Canceled 1 line" without a manual reload, and confirmed directly in the database that the underlying `OfferThread.status` flipped to `"rejected"` with a real `OfferRound` recorded (`actorRole: "seller"`, `action: "reject"`, `message: "Canceled by the seller's Account Executive."`). All test accounts, the test cart order/thread/round, and the test broadcast collection/notification created during this verification pass were deleted from the database afterward.

## 43. Full-pipeline confirmation for cart orders, and bulk photo upload for an already-posted menu (added 2026-08-19)

**A. Confirmed, not assumed: a cart-order-originated offer flows through the entire pre-existing negotiation → fulfillment pipeline unchanged.** The human asked directly whether a grower still receives a cart-order line as a normal offer they can accept/counter/reject, and whether invoice → transporter assignment → delivery → product acceptance still all work for it. Rather than answer from memory, this was checked two ways: (1) grep confirmed `threadsForSeller`/`threadsForListing` (the grower's own negotiation dashboard queries) have zero reference to `cartOrderId` anywhere, and `acceptInvoiceAndAssignTransporter`/the rest of `lib/shipments.ts` operate purely on `dealId` with zero reference to `OfferThread` at all — so a `Deal`, once created, carries no trace of having come from a cart order. (2) A real one-off integration test (`lib/cart-orders.ts`'s `submitCartOrder` → grower's own `threadsForSeller` → `Deal` + auto-invoice → `acceptInvoiceAndAssignTransporter` → `advanceShipmentStatus` through `delivered` → `acceptProduct`) was written, run against the real database using the actual production functions, passed end to end, then deleted (not kept as a permanent suite addition — it was answering a one-time question, not testing new behavior). This is exactly what CLAUDE.md §41 designed for ("reuse the existing negotiation engine, don't replace it") — nothing about cart-order origin required any downstream special-casing, and none was ever added. One terminology clarification given in the same answer: there is no single "transport rejection fee" — §14's rejection fee (negotiated by grower/retailer, triggers on retailer product rejection after delivery) and §18's transport fee (what the transporter charges, tracked not negotiated) are two separate, already-existing things; cart orders don't set a rejection-fee rate at submission, so one defaults to 0/none unless the parties separately negotiate it, identical to how a plain single-listing accept-as-posted already behaved.

**B. Bulk photo upload for a menu that's already posted — the deferred ask from earlier in this session, picked up now.** The gap, stated plainly by the human: bulk-imported menus (§34/§37) can attach a photo per row *at the time of the bulk paste*, and a single listing can always get a photo via its own edit form (§37), but there was no way to add photos to *several* products in an *already-posted* menu at once — you'd have to open each of (possibly 34) individual listings one at a time.
- **`lib/ai-listing.ts`'s `matchPhotosToMenu(filenames, strainNames)`** — a new, cheap **text-only** Claude call (no vision needed) that matches uploaded filenames against the menu's own product names, e.g. `"blue-dream-2.jpg"` → `"Blue Dream"`. Same "not configured" fallback and same "draft only, always reviewable" posture as `structureListingDraft(sBulk)` — nothing is assigned until the seller/AE confirms it in the review table, and every match is a plain `<select>` dropdown they can freely override.
- **`lib/listings.ts`'s `bulkAddMediaToMenu(batchId, callerId, assignments, opts)`** — the actual save. Re-validates every assignment server-side against the real `batchId` and the real per-listing authorization rule `updateListing` already enforces (seller who posted it, the AE/Admin who created it, or the AE currently assigned to that seller) — a file mistakenly aimed at a listing outside this menu, or one the caller has no standing over, is silently skipped rather than failing the whole batch, same "partial-but-explained beats all-or-nothing" posture as the bulk listing importer (§34). Verified with a real integration test (not the flaky browser this time — see the note below): photos landed on the right listings, a photo aimed at a listing in a *different* seller's *different* batch was correctly skipped and left that listing's media empty, `lastConfirmedAt` bumped on the listings that were actually touched, and a second grower with no relationship to the menu was correctly rejected outright.
- **`components/seller/bulk-photo-upload.tsx`** — pick several files at once, "Match with AI" pre-fills each row's dropdown, review/override, save. Wired into two places: `components/sales/menu-section.tsx` (already shared by the AE's and Admin's account pages, so both got this automatically — new `bulkPhotoSaveAction` prop, optional, since only those two pages pass one) and, new this pass, **the seller's own dashboard** (`components/seller/seller-dashboard.tsx`), which previously showed a flat ungrouped grid of listings — never grouped into menus at all, unlike the AE/Admin views §38–§40 already fixed for *their* side. Reused `groupListingsIntoMenus` (now generic over the listing shape via a type parameter, since the seller dashboard's own query includes `threads` alongside `media`, which the function's old fixed type didn't carry through) rather than duplicating the grouping logic a third time. New `components/seller/dashboard-menu-group.tsx` is a thin client wrapper owning just the collapse state and the upload panel — the existing per-listing card JSX stays exactly as it was, passed in as `children` from the server component. Defaults to **expanded**, deliberately different from `MenuSection`'s collapsed-by-default — the AE/Admin views collapse because they're managing many other people's menus at once; a seller looking at *their own* listings had never had anything hidden by default before this change, and this isn't the place to introduce that.
- Wired per-role: `src/app/grower/actions.ts` / `src/app/processor/actions.ts` (new — `SellerDashboard` didn't have its own action file before, since it was previously pure display), plus the existing AE/Admin account-detail `actions.ts` files each gained a `bulkAddPhotosForAccount` export. `lib/seller-actions.ts`'s `handleBulkAddPhotos(role, ...)` and `lib/sales-actions.ts`'s `bulkAddPhotosAsAssistant(actorRole, ...)` are the two role-scoped wrappers, mirroring the exact dual-role pattern already used throughout this app (`applyListingEditAsAssistant`, etc.) rather than inventing a new one.
- **A real environment issue hit and worked around during this pass, unrelated to the feature's own correctness**: this machine has a *different, unrelated* project's dev server already bound to port 3100 (title "Signet") — starting this app's own dev server with `PORT=3100` to match `.env`'s `NEXTAUTH_URL` silently lost the port race, and the Browser pane tool kept landing on that other app instead. Separately, on the correct port (3000), NextAuth's post-login redirect still resolves through the mismatched `NEXTAUTH_URL` (3100) and the sandboxed browser doesn't retain the session across that cross-origin bounce, which made UI-driven login unreliable in this pass even after confirming via direct `curl` and via Prisma that the accounts/credentials themselves were completely correct. Rather than force a flaky browser-only click-through, verification fell back to the same real-integration-test approach already validated for §43-A. Not a code defect — `NEXTAUTH_URL` matching the port the app is actually served on (which is what happens through the normal `preview_start` launch path, not a manually-started dev server) avoids this entirely.

**Also, in the same conversation: confirmed the license/password prompt on the shareable menu link works as expected.** When a not-yet-logged-in retailer opens a public `/menu/[batchId]` or `/collection/[id]` link, `CartBuilder` shows "Create Retailer account" / "Sign in" in place of the Accept/Counter/Reject buttons (rather than after a click, since there's nothing to act on until a session exists) — the create-account path carries `role=retailer` and a `callbackUrl` back to the same cart page into `/signup`, which already runs the real license-number lookup/auto-populate flow (§12) before password entry, then returns the retailer straight back to the cart they were building. This is the same mechanism §36 built for the single-listing respond flow, reused as-is for the cart pages — nothing new was needed here.

**Verified:** `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) clean. Real integration tests (written for this verification, run once against the real database, then deleted — not kept as permanent suite additions) confirmed both the full cart-order-to-delivery pipeline and the bulk-photo-upload authorization/isolation rules exactly as described above. All test users/listings/threads/deals/shipments created during this pass were deleted from the database afterward.

## 44. Media uploads were completely broken in production — fixed with Vercel Blob Storage (added 2026-08-19)

The human tried the just-shipped bulk photo upload on the live deployed app and hit a generic "A server error occurred" — attaches but never actually uploads. This was **not a bug in the new feature specifically** — every media upload in this app (listing photos, invoices, proof-of-delivery) has been silently broken on the deployed site since it first went live, and §7/§8 already said why without anyone circling back to actually fix it: `lib/media.ts` wrote uploaded files to local disk (`public/uploads/`), explicitly flagged as "development only... swap for real object storage before production." Vercel's deployed filesystem is **read-only** outside a short-lived temp directory — every `writeFile()` call there has always failed on the real deployment, it just hadn't been exercised against the live site until now.

**Fix: Vercel Blob Storage**, chosen over Supabase Storage (the other option offered) because it needed zero new vendor account — same platform this app is already deployed on. Provisioned directly via the Vercel CLI rather than asking the human to click through a dashboard: `vercel blob create-store mi-cannabis-exchange-media --access public --yes`, which created the store, linked it to the project, and auto-populated `BLOB_READ_WRITE_TOKEN` across Production/Preview/Development on Vercel — copied into both `.env` and `.env.local` locally so it's picked up the same way `DATABASE_URL` etc. already are. **Public access**, not signed/private — matches exactly how these URLs were already used everywhere in the app (embedded directly in `<img>`/`<video>` src with no per-request auth check on the file itself, same as the old static `/uploads/` serving; the app's actual access control is at the page/query layer, e.g. the blind-marketplace anonymization boundary, never at the file-URL layer).

- **`lib/media.ts`** — `writeUpload`'s local `fs.writeFile` replaced with `@vercel/blob`'s `put()`; `saveMediaFile`/`saveDocumentFile` now return a real `https://*.public.blob.vercel-storage.com/...` URL instead of a local `/uploads/...` path. No caller anywhere else in the app needed to change — both functions keep their exact same signatures and return shape.
- **`lib/media-redaction.ts`'s `attemptRedaction`** — used to read a file from disk (`sharp(absoluteFilePath)`) and overwrite it in place if a logo/contact-info region was found. With no local file to read or overwrite once uploads go straight to Blob, this now takes and returns an in-memory `Buffer` instead — the caller uploads whichever buffer comes back (redacted or original) rather than the redaction step touching disk at all.
- **A second, related gap raised in the same report and fixed in the same pass**: Next.js caps a Server Action's request body at 1MB by default — every upload in this app passes file bytes through a Server Action, and a real phone photo already exceeds that. `next.config.ts` now sets `experimental.serverActions.bodySizeLimit: "4mb"` — deliberately not higher, since Vercel's own platform-level request-body ceiling for a standard serverless function invocation is roughly 4.5MB regardless of what this config says, so raising it further wouldn't actually help on Vercel specifically.
- **Known, stated gap, not silently left as a landmine**: the human said "I need pictures and videos to upload" — real video files routinely exceed that ~4.5MB platform ceiling, and nothing built in this pass changes that, since every upload surface in this app still sends file bytes through a Server Action rather than uploading directly to Blob from the browser. The real fix for large video support is Vercel Blob's client-direct-upload pattern (`@vercel/blob/client`'s `upload()`, authorized by a small signing endpoint that never sees the file bytes itself, bypassing the function body-size limit entirely) — a materially bigger change touching every upload surface in the app (listing photos, POS receipts, invoices, PODs, the new bulk uploader), not done here, flagged for a follow-up once confirmed needed.

**Verified with a real integration test, not a guess**: uploaded a real (tiny, synthetic) image and PDF through the actual `saveMediaFile`/`saveDocumentFile` functions against the real, newly-created Blob store, confirmed each returned a genuine `*.public.blob.vercel-storage.com` URL, confirmed each URL was actually fetchable (`200`, non-empty body) — proving the fix works end to end, not just that it compiles — then deleted both test blobs via `@vercel/blob`'s `del()`. `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) all clean.

## 45. Client-direct-to-Blob upload for the bulk photo/video uploader (added 2026-08-19)

The flagged gap from §44 — real video files still routinely exceed Vercel's ~4.5MB per-request ceiling, since every upload was still passing file bytes through a Server Action's request body. Considered Google Drive as an alternative when the human asked, and talked them out of it: it doesn't actually solve the size problem (the bytes would still have to reach this app's server first, hitting the exact same ceiling, before being forwarded on), and Drive's "direct link" URLs are unreliable for the kind of public hotlinking `<img>`/`<video>` embedding this app needs at scale. Vercel Blob's own client-direct-upload pattern was the better fit — same store already provisioned, no new vendor, and it genuinely bypasses the body-size ceiling rather than working around it.

**How it works now, for the bulk menu photo/video uploader specifically:** the browser calls `@vercel/blob/client`'s `upload()` directly — the file bytes go straight from the browser to Blob storage, never through this app's own server at all. `/api/blob-upload/route.ts` (new) is the one small thing the server does: it authorizes the upload by issuing a short-lived client token (`handleUpload`'s `onBeforeGenerateToken` — just checks `auth()` for a real session, same posture as every other gated surface in this app), never touching the actual file. Only after the browser's direct upload finishes does `components/seller/bulk-photo-upload.tsx` call the existing `saveAction` Server Action — and by then it's carrying only the resulting blob URL + content type, a tiny string, nowhere near any body-size limit regardless of how large the original video was.

- **`lib/media.ts`'s new `finalizeUploadedMedia(blobUrl, contentType)`** is the other half — takes a URL a client already uploaded, not file bytes. Images still get the same best-effort logo/contact-info redaction pass as before (§18): fetching the blob back down is an outbound GET a server function makes freely (not subject to `bodySizeLimit`, which only ever governs an *incoming* request body), and if a region is found, the redacted version goes to a fresh blob and the original is deleted. Videos skip redaction entirely, same as they always did, and pass through untouched.
- **`lib/listings.ts`'s `bulkAddMediaToMenu`** signature changed from `{listingId, file: File}[]` to `{listingId, url, contentType}[]` — the authorization logic (seller/AE/assignedRep/bypass, only-if-still-`active`, silently-skip-what's-not-authorized) is completely unchanged, it just calls `finalizeUploadedMedia` instead of the old file-accepting `saveMediaFile`. Every wrapper that threads this through (`handleBulkAddPhotos`, `bulkAddPhotosAsAssistant`, and the four thin per-portal action files) updated to match — same call chain, different payload shape.
- **Scope, stated plainly**: this pass converts the bulk menu uploader only — the piece the human was actively trying to use for "pictures and videos... to the specific products." The single-listing `EditListingForm` photo upload still passes File objects through a Server Action (capped at 4MB per §44) — fine for a typical photo, but a large video attached to one single listing (rather than via the bulk menu tool) would still hit that ceiling. Not converted in this pass; flagged rather than silently left inconsistent, since the same `finalizeUploadedMedia`/client-`upload()` pattern would extend to it directly if asked for.

**Verified with a real integration test**: simulated exactly what the browser's own direct upload already does (a raw file landing in Blob storage via `put()`) for both an image and a video, then ran the actual `bulkAddMediaToMenu(url, contentType)` path end to end — confirmed the image went through the redaction attempt (failed closed to "not attempted" locally, no `ANTHROPIC_API_KEY` in dev, same honest fallback as every other AI-gated feature here) and the video passed through untouched, both `ListingMedia` rows created correctly, both final URLs genuinely fetchable. `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) all clean. All test users/listings/blobs created during verification were deleted afterward.

## 46. The single-listing create/edit media upload had the exact same bug — fixed the same way (added 2026-08-19)

§45 explicitly flagged this as scoped out — "the single-listing `EditListingForm` photo upload still passes File objects through a Server Action... not converted in this pass" — and the human hit it within the hour, trying to attach media "to a product directly": the same generic "A server error occurred," attaches but never uploads. Converted every remaining listing-media path to the same client-direct-to-Blob pattern §45 built, closing the gap rather than leaving it as a known landmine.

- **`lib/listings.ts`'s `createListing` and `updateListing`** both changed signature from accepting `File[]` to `{url, contentType}[]` — same shift `bulkAddMediaToMenu` already went through, calling `finalizeUploadedMedia` instead of the old file-accepting `saveMediaFile`. With this pass converting the last two callers, `saveMediaFile` had no callers left anywhere in the app — confirmed by grep, then deleted outright rather than left as unused dead code.
- **`lib/media.ts`'s new `parseMediaUploadsField(formData)`** is the one shared place that reads the `mediaUploads` JSON field every listing-media form now submits instead of raw files — written once so the shape can't drift between the four call sites that needed it (`handleCreateListing`, `handleEditListing` in `lib/seller-actions.ts`; the single-item and edit paths in `lib/sales-actions.ts`).
- **The bulk-import creation loop** (`handleCreateListingsAsAssistantBulk`, CLAUDE.md §34/§37) worked differently before — a per-row file input named `media_<rowKey>`, correlated back to its JSON draft row by a client-generated `_key`. That whole indirection is gone: the client now uploads each row's files directly to Blob and embeds the resulting `{url, contentType}[]` straight into that row's own JSON object before submitting, so the server just reads `d.media` off the already-parsed draft — one less moving part, and the `_key` field is stripped back out before sending since it was never needed for anything but that now-removed correlation.
- **`components/seller/listing-form.tsx`** (single-item AND bulk mode, since both share this one component) and **`components/seller/edit-listing-form.tsx`** both moved from a native `<form action={serverAction}>` submission to an intercepted `onSubmit`: file inputs no longer carry a `name` attribute (so a plain `FormData(form)` never picks up raw files at all), selected files are tracked in React state instead, and the submit handler uploads them all via `@vercel/blob/client`'s `upload()` first — same `/api/blob-upload` authorization route §45 already built, no second endpoint needed — before constructing the final `FormData` (text fields captured natively from the form element, `mediaUploads` or the enriched `drafts` JSON set explicitly) and calling the Server Action directly as a plain async function. Both forms show live "Uploading N/M…" progress on the submit button while this happens, same UX pattern as `BulkPhotoUpload`.

**Verified with a real integration test**: exercised `createListing` and `updateListing` directly against the real database and the real Blob store — attached a client-direct-uploaded photo at creation, then a video via a follow-up edit on the same listing, confirmed both `ListingMedia` rows landed with the correct type and a genuinely fetchable URL. Also unit-tested `parseMediaUploadsField` against both a populated and an empty `FormData` to confirm it round-trips exactly what the client now sends. `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) all clean. All test data deleted afterward.

## 47. Menu/collection share links never actually showed photos or videos (added 2026-08-19)

The human reported it directly: open a shared menu link, the product list shows, but no media anywhere. Traced to `components/cart/cart-builder.tsx` — the shared component behind both `/menu/[batchId]` and `/collection/[id]` — which had a `media` field on its own `CartListing` type and never once read it in the JSX. The data was always there (`publicMenuView`/`publicCollectionView` in `lib/listings.ts`/`lib/menu-collections.ts` both already `select` it), this was purely a missing render, not a data-layer bug — confirmed by checking `/listing/[id]` (the single-listing share page, a different component), which already showed a cover photo correctly, so the gap was specifically the cart/menu view never built for CLAUDE.md §40.

Fixed by adding a 56px cover thumbnail to each product row in `CartBuilder`, same `cover = listing.media[0]`/video-or-image branching `components/retailer/listing-card.tsx` already established for the logged-in retailer feed, falling back to the same `Leaf` icon placeholder that feed already uses for a listing with no photo — reusing the existing visual convention rather than inventing a new one.

**Verified live**: opened a real 34-product menu link in the browser against real production data — confirmed two listings with real uploaded photos ("Baklava" ×2) rendered their actual images inline in the cart, not a placeholder. `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) all clean.

## 48. AE/Admin real-identity negotiations view, a real nested-button hydration bug fixed, and a lightbox for shared-menu photos (added 2026-08-19)

Three items from the same conversation, landed together.

**A. Account Executive negotiations view — a confirmed, deliberate reversal of the blind-marketplace boundary for this role specifically.** Raised as an open question ("how would you approach this"), answered with a recommendation (default to keeping the retailer side anonymized, matching how the AE already only ever saw their own client's real identity) — and the human overrode that recommendation, confirming explicitly they want the AE to see the retailer's real identity too, on both sides, same trust tier as Broker/Admin. Same "flag, don't decide silently" posture this document already follows for this exact class of change (§9, and its own precedent reversals in §11/§17-18).

- **`lib/offers.ts`'s new `threadsForSalesRep(salesRepId)`** — identical shape to the existing `allThreadsForBroker()` (real `postedBy`/`retailer` objects, all rounds, the `Deal` if one exists), just scoped to `listing.postedBy.assignedSalesRepId === salesRepId` instead of platform-wide. A pure Server Component reads and renders this directly (no client-component boundary crossing), the same safe pattern `BrokerDashboard` already established — sidesteps the §21-class RSC-payload-leak risk by construction rather than needing a narrowed `select`.
- **`components/sales/negotiations-dashboard.tsx`** — shared by both the new `/sales/negotiations` (AE, scoped) and `/admin/negotiations` (Admin, reuses `allThreadsForBroker()` directly and renders through this same component unscoped) — open negotiations with the full round-by-round history, plus a closed/rejected summary section. No commission UI (that stays Broker-only).

**B. A real bug, not a guess: "photo attaches but won't stay with the product" traced to a React hydration mismatch, not the upload pipeline.** The human reported the failure specifically on the per-product edit form reached *from a menu* — investigating live in the browser (not just re-reading code) turned up a genuine console error: `MenuSection`'s collapsible header was a `<button>` wrapping `ShareMenuLink`'s own `<button>` — invalid HTML (interactive content can't nest), which browsers silently repair by breaking the DOM out of the nesting, desyncing it from what React's server render produced and triggering a real hydration mismatch on every menu page. This is exactly the kind of bug that can intermittently corrupt in-flight client state (a hydration mismatch can force React to discard and re-render the affected subtree), which lines up with a photo upload that starts, appears to work, and then doesn't stick — not a coincidence once the console error was actually read instead of assumed away.

- **Fixed** by changing the header from a `<button onClick={...}>` to a plain `<div role="button" tabIndex={0} onClick={...} onKeyDown={...}>` — keeps it keyboard-operable without the invalid nesting. `ShareMenuLink`'s own button already calls `stopPropagation()`, so the click-to-toggle behavior is unaffected. `components/seller/dashboard-menu-group.tsx` (the seller's own dashboard's menu header) was checked too — it never nested `ShareMenuLink` inside its button, so it didn't have this bug and needed no change.
- **Verified live, not just by inspecting the diff**: reproduced the exact console hydration error first (confirmed it was real and present), applied the fix, confirmed via `document.querySelectorAll('button')` that zero buttons contained a nested button anymore, then drove a real upload through the per-product edit form on a live test listing (a file injected via a synthetic `DataTransfer`, since a real OS file picker can't be automated) and confirmed directly in the database that a real `ListingMedia` row landed with a genuine, fetchable Blob URL — proving the fix, not just the absence of the console error.

**C. Shared-menu photos are now clickable to view large.** `components/cart/cart-builder.tsx`'s per-product thumbnail (§47) is now a real button — click it to open a full-screen lightbox (dark backdrop, image or video shown at full size, closes on backdrop click or the × button). Simple and self-contained; no gallery/multi-photo browsing added since the thumbnail itself only ever shows one (the cover) photo today. Follow-up the same session, tested on an actual mobile viewport (375×812) after the human asked for it to be "big enough to see very good from a phone": the media element now sizes to `w-full h-full` of the modal (not just `max-w/max-h`), so a small source photo gets scaled *up* to fill the screen rather than only ever being capped down from a large one — confirmed live, a real photo now fills the phone screen edge-to-edge with `object-contain` keeping it uncropped.

**Also added in this pass, not yet wired into any UI**: `User.mustChangePassword` (new, default `false`) — laid the groundwork for a still-in-progress license-first inline sign-in flow on the public menu/collection cart pages (the human's detailed spec: click Accept/Counter first, *then* get asked for a license number, then a password — an existing password if the account already exists, a brand-new one if it doesn't, or a forced new one if an Admin/AE set the current password on the person's behalf and they've never actually chosen their own). The field itself is safe to ship now (additive, defaults false, not read or written by any shipped code path yet); the actual flow is mid-build and picks up next.

**Verified:** `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) all clean for all three items. All test accounts/listings/media created during verification deleted afterward.

## 49. License-first cart checkout auth shipped and verified, a bulk-price-tool staleness bug fixed, and AE-assisted buyer offers (added 2026-08-19)

Three items, the first two picking up work left mid-build at the end of §48.

**A. The license-first inline sign-in/signup flow for `/menu/[batchId]` and `/collection/[id]` — built, and now verified live end to end.** `lib/cart-auth.ts` (new): `checkRetailerLicense` (existing account → `mustChangePassword` flag; no account but a real state registry match → `"new"`; neither → `"not_found"`), `signInWithLicense` (a normal password sign-in, just keyed by license number instead of email — resolves the real email server-side so the client never needs it), `setNewPasswordForLicense` (one function covering both a genuinely new account — mirrors `lib/admin-retailer-assist.ts`'s `createRetailerAccountForAdmin` as closely as possible — and an existing `mustChangePassword` account just getting its temporary password replaced). `components/cart/license-auth-flow.tsx` is the 3-step client UI (license number → password-or-new-account-fields → done); `CartBuilder` (§41/§42) shows Accept/Counter/Reject up front regardless of session state (the human's own spec — never gate the buttons themselves behind a login wall), and only clicking Accept/Counter while not signed in as a retailer opens this flow, remembering which action to resume (`pendingAction`) and re-firing it (`skipAuthGate: true`, since a state update from moments earlier isn't guaranteed visible in the same call) the instant auth succeeds — so "click Accept, get asked to prove who you are, land back on the exact same accept" is one continuous motion, not a restart.

Verified live against the real database, not just `tsc`/`build`/`test` (which were already clean at the end of §48): a logged-out visitor on a real test menu clicked **Accept offer**, was prompted for a license number, entered a real Active Michigan retailer license not yet in this system, got "We found '{business name}' — set a password to finish creating your account," set one, was signed in automatically, and the pending Accept resumed and completed — confirmed in the database as a genuine `Deal` at the seller's full posted price/quantity, with the listing correctly decremented to zero and closed (§41's partial-fulfillment fix, exercised for real here as a side effect). Separately, on a second test listing, clicked **Counter offer** after adjusting quantity down, entered the license number of an account an Admin/AE had quick-created on the retailer's behalf (`mustChangePassword: true`), correctly got "Your account's temporary password needs to be replaced" (no contact-name/email fields, since the account already exists) instead of the new-account form, set a real password, and the pending Counter resumed and completed — confirmed `mustChangePassword` flipped to `false` and a real `OfferThread`/`Deal` exists at the adjusted quantity. (That second thread's round recorded as `action: "accept"` rather than `"counter"` even though the quantity was below the full posted amount — correct, not a bug: §41's `submitCartOrder` resolves accept-vs-counter off whether the *price* matches the listing, not whether the full quantity was taken, exactly the mechanism that lets a retailer buy less than a tiered listing's full amount at the listed price without waiting on the seller. Noting it here since the UI's own "this line will go back as a counter-offer" copy on an adjusted line can read as a promise of a pending round when price-only-matched quantity reductions actually resolve instantly — a pre-existing §41/§42 copy nuance, not something changed in this pass, and not incorrect enough to warrant a fix on its own.) All test accounts/listings/threads/deals created during this verification were deleted afterward.

**B. A real bug found live-testing the bulk price tool (§41's follow-up plan, picked up here): the tool's own numbers went stale after a successful save.** `components/seller/bulk-price-update.tsx` computes its "Menu value: $X → $Y" preview and every price shown on the listing cards below it from a `listings` prop that is a one-time snapshot from the server render — `bulkUpdatePricing` (`lib/listings.ts`) itself always writes correct, freshly-computed prices to the database on every call, but nothing told the page to re-fetch afterward. Caught live: applying a second bulk change right after a first (a target-total change from $2,000→$1,500, then a +10% change) computed the second preview off the pre-first-change $2,000 base instead of the real $1,500 — the UI showed "$1,500 → $2,200" when the correct math was "$1,500 → $1,650." Fixed with `router.refresh()` on a successful save, same pattern already used for `CancelOrderButton` (§42D) for the identical class of gap ("a Server Component page has no reason to know to re-render on its own without that nudge"). Verified live end to end on real test data across all three modes: target-total ($2,000.00 → $1,500.00, confirmed `$75.00`/`$37.50` per-unit in the database), then percent (+10% on the now-correct $1,500.00 base → $1,650.00, confirmed `$82.50`/`$41.25` in the database) — the second application's preview and post-save total both correctly showed the *live* number without a manual page reload, proving the fix. Test grower/listings/admin account deleted afterward.

**C. Account Executives can now also submit an offer on a retailer's behalf, not just Admin.** Raised directly ("is it set up that an account rep can make an offer on behalf of a buyer?" — answered no, Admin-only, single-listing only — then "make it so the account representative can also make an offer on behalf of the Buyer, and duly noted"). This is the same class of confirmed reversal as §48-A and §36/§42-C before it — §36 originally kept the assisted-retailer-response picker on `/listing/[id]/respond` Admin-only on purpose, reasoning an AE has no standing relationship with retailers the way it does with the sellers it solicits; that reasoning is preserved in both files' comments rather than deleted, not silently overwritten.

- **`lib/public-respond-actions.ts`'s `respondToListingAsRetailer`** — authorization widened from `isAdmin` to `isAssistant` (`session.user.role === "admin" || session.user.role === "sales_rep"`).
- **`lib/admin-retailer-assist.ts`** — its three exported functions (`searchRetailerLicenseRegistry`, `createRetailerAccountForAdmin`, `searchExistingRetailers`) each called `requireRole("admin")` only; replaced with a new local `requireAdminOrSalesRep()` (mirrors `requireRole`'s redirect-on-mismatch shape, since `requireRole` itself only accepts one exact role and these three functions now need to accept either). Despite the name `createRetailerAccountForAdmin` (unchanged — matches the existing exported name other code already imports), it's now callable by either role; the search/create logic itself needed no change, since none of it was ever Admin-identity-specific.
- **`components/listing/respond-panel.tsx`'s `RespondPanel`** — the branch that renders `AdminRetailerPicker` now checks `sessionRole === "admin" || sessionRole === "sales_rep"` instead of `"admin"` alone. The picker's own copy ("Find or create the retailer account you're submitting on behalf of") was already role-neutral and needed no change.
- **Scope note, not yet asked for**: this only extends the existing single-listing respond flow (§36). A parallel AE-assisted path through the cart/menu checkout flow (§41-A above) wasn't requested and wasn't built — the human's confirmation was specifically about "an offer on behalf of a buyer," matching the capability Admin already had, not a new cart-wide extension.
- **Verified live against the real database**: created a throwaway Account Executive account and a throwaway test listing, signed in as the AE, opened the listing's public respond link, confirmed the same "Find or create the retailer account" picker Admin sees now renders for the AE too, searched a real Active Michigan retailer license by number, created the account from it, confirmed "Acting on behalf of {business name}," clicked Accept, and confirmed in the database a real `Deal` was created at the listing's exact price/quantity with the correct `retailerId`. Test AE/retailer/grower/listing/thread/deal all deleted afterward.

**Verified:** `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) all clean for all three items in this section.

## 50. Bulk price tool gets per-product selection, and the public cart's sticky panel can be minimized (added 2026-08-19)

Two follow-ups, raised right after §49 shipped, both from real usage of features that had just gone live.

**A. "If there are 34 products and I feel like updating pricing I should be able to select all or select the products I want to update."** `bulkUpdatePricing` (`lib/listings.ts`) previously always applied to every active listing in a menu — no way to scope a price change to a subset. Now takes an optional `listingIds?: string[]` (threaded through both role-scoped wrappers, `handleBulkUpdatePricing`/`bulkUpdatePricingAsAssistant`, and all four app-level action wrappers unchanged in spirit — omitted/undefined still means "the whole menu," exactly the old behavior, so nothing that already called this function needed to change). `components/seller/bulk-price-update.tsx` gained a scrollable checklist (strain name, current price, quantity) with every product checked by default — the common case, "reprice the whole menu," is still zero extra clicks — plus Select all/Select none shortcuts. The preview total, the "Selected value" label, and the Apply button's own label ("Apply to menu" vs. "Apply to N products") all recompute off the selected subset live as boxes are checked/unchecked.

Verified live against the real database: a 3-product test menu ($100/$200/$300 per lb), unchecked one product, applied +20% to the remaining two, confirmed the live preview showed the correct $1,500.00 → $1,800.00 (not the full-menu $3,000.00 base), and confirmed in the database that only the two selected listings moved ($100→$120, $200→$240) while the deselected one stayed untouched at $300 exactly.

**B. "The bottom half has a small window that says the total price, payment terms, create retailer, etc — would be nice if a person wanted to minimize this window some downward to better see the menu."** `components/cart/cart-builder.tsx`'s sticky bottom summary panel (§41) — product count/total, terms selector, Accept/Counter/Reject, the license-first auth flow (§49-A) — took up a fixed chunk of screen on every menu/collection page regardless of how long the menu was. The product-count/total row is now itself the toggle (a real `<button>`, not nested inside one — CLAUDE.md's own §48-B hydration lesson stayed in mind while adding this): clicking it collapses everything below to just that one summary line, with a chevron flipping between down (expanded) and up (collapsed) as the affordance. Starts expanded on every page load — nothing about the buttons' visibility-by-default changes, a visitor just gets the option to shrink it themselves once they're scanning a long menu.

Verified live on a real 3-product test menu: clicked the summary row, confirmed the terms selector and Accept/Counter/Reject buttons disappeared while the product-count/total row and its now-upward chevron stayed put, confirmed the menu cards above had visibly more room, then clicked again and confirmed the full panel — including a live re-check that Accept/Counter/Reject still worked — came back exactly as before.

**Verified:** `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) clean for both items. Test menu/listings/accounts created during verification deleted afterward.

## 51. Set-price bulk mode, retailer/AE/Admin bulk price adjust on a counter-offer, and an AE/Admin-assisted cart flow (added 2026-08-19)

The human's full ask, in one message: "select all or select products at will and change pricing based on a percentage or change to a set price on all... growers should be able to also do this and the buyers should be able to bulk adjust price when doing a counter offer so can account executives and admin... Growers's should be able to update they're menus." Four distinct pieces, landed together.

**A. A fourth `PriceAdjustment` mode — `setPrice` — added to the one shared bulk-pricing engine (`lib/listings.ts`'s `bulkUpdatePricing`).** §50 already covered per-product selection (percent/dollar/target-total); "change to a set price on all" was a genuinely different operation none of those three covered — every selected line goes to the *exact same* number, overriding whatever it was, not a relative move. `components/seller/bulk-price-update.tsx` (the grower/processor/AE/Admin tool from §41/§50) gained a fourth mode tab; the selection checklist, preview math, and per-product/whole-menu Apply button all already generalized cleanly since they were written mode-agnostic in §50.

**B. Growers/processors already had this — confirmed, not rebuilt.** `SellerDashboard`/`DashboardMenuGroup` (§40/§41) already wire `bulkPriceSaveAction` on both `/grower` and `/processor`, so the new set-price mode landed there for free. Verified live: signed in as a real test grower, opened their own menu's "Bulk price change" panel, selected "Set price," set both of a 2-product menu's listings to $175/lb in one action, and confirmed both landed at exactly $175 in the database — same tool, same authorization, no separate code path for the grower's own dashboard vs. the AE/Admin account pages.

**C. New: `components/cart/cart-bulk-price-adjust.tsx` — the buyer's own bulk price tool, usable while building a counter-offer on a shared menu/collection link.** Deliberately lighter than the seller's tool: only two modes (percentage, set price — no dollar/target-total, since "the whole cart's total" isn't something a buyer is normally negotiating toward the way a seller manages menu value), and purely client-side — nothing is written to the database until Accept/Counter is actually clicked. Applying a bulk adjustment writes into `CartBuilder`'s existing `priceOverrides` state via the exact same per-line mechanism the individual `$` inputs next to each product already use, so a bulk change and a manual edit are indistinguishable to the rest of the cart once applied — no second pricing concept invented. Verified live on a real 3-product menu: deselected one product, applied +10% to the other two, confirmed the live preview and the per-line cards updated correctly ($300→$330, $400→$440, third product untouched at $500), then actually submitted a counter — confirmed in the database that the two adjusted lines landed as real `counter` rounds at the bulk-adjusted prices and the untouched line correctly auto-resolved as an `accept` (§41's existing price-match logic, unaffected by this change).

**D. New: an Admin/AE-assisted path through the cart/menu pages, not just the single-listing respond page.** §36/§49-C built "find or create the retailer account you're submitting on behalf of" for `/listing/[id]/respond` only, with an explicit scope note that a cart-based equivalent "wasn't requested and wasn't built." This request — "so can account executives and admin [bulk adjust price] when doing a counter offer" — only makes sense if AE/Admin can reach the cart page's checkout flow at all, so that's what got built.
- **`components/shared/retailer-picker.tsx`** — the picker UI (search existing retailer / create new from a real CRA license number) extracted verbatim out of `respond-panel.tsx`'s previously-private `AdminRetailerPicker` into a shared, exported `RetailerPicker`, so `CartBuilder` reuses the identical component instead of a second drifting copy. `respond-panel.tsx` now imports it; no behavior change on the listing-respond page itself.
- **`CartBuilder`** — when `sessionRole` is `admin` or `sales_rep`, the button area is replaced by `RetailerPicker` until a retailer is resolved; a `sales_rep` additionally has to check an "I have read and understand the terms disclaimer" box (reusing `TermsDisclaimer`, same text and same client-side-only-gate posture as §41's menu-collection-creation checkbox — not a database acceptance record) before the buttons unlock, since submitting on a retailer's behalf is the same kind of AE-facilitated event `CartOrder.facilitatedBySalesRepId`/`disclaimerAcknowledgedAt` already exist for; Admin gets no disclaimer step, matching Admin's existing no-disclaimer posture everywhere else in this app. Once resolved, "Acting on behalf of {business name}" renders above the same Accept/Counter/Reject buttons everyone else sees — same component, same bulk price adjust tool, no second UI.
- **`lib/public-cart-actions.ts`'s `submitPublicCartOrder`** gained an optional `assistedRetailerId` param — when present, authorization checks `session.user.role === "admin" || "sales_rep"` (never trusting the client-supplied id without that check, same posture as `respondToListingAsRetailer`), then calls `submitCartOrder` with `facilitatedBySalesRepId`/`disclaimerAcknowledged` set only for the `sales_rep` case — Admin's submissions leave that field null, since the disclaimer exists specifically for the AE's ongoing-relationship liability question, not something Admin's broader authority needs. `CartOrder.facilitatedBySalesRepId`'s name predates this (§41 wrote it Sales-Rep-specific, before Admin parity existed) and is loosely reused for audit purposes only when it's actually a sales_rep — same "one field, reused across AE/Admin without a schema change" precedent already established by `Listing.createdBySalesRepId`.
- Verified live end to end, two full passes: (1) signed in as a real test AE, resolved a real Active Michigan retailer license into a brand-new account via the picker, checked the disclaimer box, clicked Accept — confirmed in the database a real `CartOrder` with `facilitatedBySalesRepId` and `disclaimerAcknowledgedAt` both correctly set, and a real `Deal` at the listing's full price/quantity. (2) signed in as a real test Admin, resolved an *existing* retailer account via the "existing account" search tab (no disclaimer step shown), reduced quantity by one unit, clicked Counter — confirmed a real `OfferThread`/`Deal` at the adjusted quantity, correctly auto-resolved as an `accept` since only quantity (not price) had moved. All test accounts/listings/threads/deals/cart orders from both passes deleted afterward.
- **Scope note**: this does not touch §17/§18's "who can force a listing exclusive" boundary, and does not change the retailer's own self-serve license-first flow (§49-A) — that remains exactly as built, just now sitting alongside a second, assistant-only entry path into the same `CartBuilder` component.

**Verified:** `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) all clean for every item in this section.

## 52. `lib/cart-auth.ts` was a real, unthrottled anonymous surface — rate-limited, closing the exact gap §21/§30 already fixed twice elsewhere (added 2026-08-19)

Not requested — found while checking a self-review question after §49's license-first cart auth flow shipped: does every genuinely unauthenticated write/lookup surface in this app have the Postgres-backed rate limiter (`lib/rate-limit.ts`, §30) that `/api/license-lookup` and the storefront order action already use? It didn't. `lib/cart-auth.ts`'s three exported functions — `checkRetailerLicense`, `signInWithLicense`, `setNewPasswordForLicense` — are all reachable by a completely anonymous visitor (the whole point of the license-first flow is working before a session exists) and had no throttling at all, missed when that file was first built and verified (the verification at the time covered correctness, not this specific gap).

Three concrete exposures this closed, in order of how serious each one is:
- **`signInWithLicense` was an unthrottled password-guessing surface.** License numbers are public record (the same MI CRA export this app already imports); an anonymous script could iterate license numbers and password-guess against each one with no rate limit anywhere in the path. This was the most serious of the three.
- **`checkRetailerLicense`** hits `lookupLicense` against the ~3,300-row `LicenseRegistry` — the exact same registry-scraping vector §21 already fixed for `/api/license-lookup`, just through a second, newer door that didn't get the same fix.
- **`setNewPasswordForLicense`** creates real user accounts and sets real passwords — an unthrottled anonymous write, the same class of gap the storefront order action was fixed for.

**Fix**: all three now call the existing `isRateLimited`/`clientIp` (`lib/rate-limit.ts`) before doing any real work — no new limiter, no new table, same Postgres-backed fixed-window primitive already proven to hold across multiple serverless instances. Limits: `checkRetailerLicense` 20/min (matches `/api/license-lookup`'s own limit exactly, since it's the same kind of lookup), `signInWithLicense` 8/min (tighter — this is the password-guessing one), `setNewPasswordForLicense` 10/min (matches the storefront order action's write limit). `LicenseCheckResult` gained a `"rate_limited"` status distinct from `"not_found"` — silently returning "not found" when someone's actually just been throttled would be a dishonest error message, same "don't fabricate a misleading result" convention this app follows elsewhere (§10's seller-rating "no fabricated default," §23's tax-rate caveat); `components/cart/license-auth-flow.tsx` shows a distinct "Too many attempts — try again in a minute" message for it.

**Verified**: `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) clean. The underlying rate-limit primitive was exercised directly against the real database with the exact scope/window/max `checkRetailerLicense` uses (`cart-auth-check`, 60s window, max 20) — confirmed requests 1–20 pass and request 21 onward is correctly flagged as rate-limited, then cleaned up. Not re-verified through an actual anonymous browser session hitting the real Server Action 21 times (Next.js Server Actions carry their own internal action-id headers that a plain script can't easily replicate outside a real page load) — the primitive itself is already proven correct here and via its two pre-existing call sites, and the three new call sites were confirmed by direct code review to pass matching scope/window/max arguments before any real work happens.

## 53. Secrets-hygiene audit — clean, plus one Vercel dashboard setting fixed (added 2026-08-19)

A full audit, not tied to any specific request: `git log --all` scanned for any real `.env` file ever committed (none — only the placeholder `web/.env.example`, values all blank/instructional), for hardcoded API keys/tokens/passwords in any commit or the current tree (none found, across both source files and one-off `scripts/`), and for any `NEXT_PUBLIC_`-prefixed variable that would leak a server secret to the client bundle (none exist in this codebase at all). Also checked that `.vercel/` (the local project-link config) was never accidentally tracked — it wasn't.

**One real, if minor, finding: `BLOB_READ_WRITE_TOKEN` was set as "Non-sensitive" on Vercel while every other credential (`DATABASE_URL`, `DIRECT_URL`, `NEXTAUTH_SECRET`, `ANTHROPIC_API_KEY`) is "Sensitive."** Non-sensitive just means the raw value is shown in plaintext in the Vercel dashboard/CLI to anyone with project access instead of being masked — not a public leak, but inconsistent with how every other credential in this project is configured, and worth matching since this token can write to the production Blob store. Flagged to the human before touching it (a live production credential, even though the fix doesn't change the value itself) — confirmed, then fixed: removed and re-added via `vercel env rm`/`vercel env add --value` with the identical token value, now `Sensitive` for Production and Preview. Development stays `Non-sensitive` — Vercel doesn't allow a Sensitive var there, since local dev needs `vercel env pull` to actually read it, same reason the token was synced into the local `.env`/`.env.local` files back when it was first provisioned (§44). No functional change — the token value is identical, and the live site was confirmed still responding (`200`) immediately after.

## 54. Search on the AE/Admin negotiations and orders dashboards (added 2026-08-19)

Self-directed follow-up (not a specific request): `/sales/negotiations`, `/admin/negotiations`, `/sales/orders`, and `/admin/orders` (§42/§48) were plain server-rendered lists with no way to find one deal or order in a long list — fine at demo scale, a real gap once a rep or Admin is looking at dozens of real accounts' worth of activity.

- **`components/sales/negotiations-dashboard.tsx`** (shared by both the AE-scoped and Admin platform-wide pages) and **`components/sales/orders-list.tsx`** (shared by `/sales/orders` and `/admin/orders`) both converted from plain Server Components to `"use client"`, each adding a single search input and a `useMemo`-filtered view. Plain client-side substring matching on the already-fetched list — same "no new index needed at this data volume" posture as the retailer feed's own search (§18) and the Marketing suite's lead search, not a new server query or a new database index. Negotiations search matches strain name, either side's business/contact name, or either side's license number; orders search matches the retailer's handle, any line's strain name, or any line's seller label.
- **Scope note**: the Broker dashboard (`components/broker/broker-dashboard.tsx`) has the same underlying need (platform-wide, real-identity, potentially the largest list in the app) but wasn't touched here — it's an `async` Server Component that fetches its own data and renders nested `CommissionForm`s inline, a different shape than the two components above, and adding search there would mean splitting it into a server/client pair rather than a like-for-like change. Left as a flagged follow-up rather than done partially.
- **Verified live against the real database**: created two growers with one listing each under one AE's assigned accounts, a real retailer, and two open counter-offer threads — confirmed `/sales/negotiations` showed both, searching by one grower's business name ("Zenith") correctly narrowed to just that thread, searching by the other grower's real license number correctly narrowed to the other thread, and a non-matching query correctly showed "No open negotiations match your search" rather than an empty blank state. Separately tagged one thread with a real `CartOrder` and confirmed `/sales/orders` search correctly matched on the order's product strain name and correctly excluded it when searching for an unrelated strain. All test accounts/listings/threads/orders deleted afterward.

**Verified:** `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) clean.

## 55. Password-lifecycle overhaul: self-service reset (Resend), a real forced-change gate everywhere, and self-service change-password (added 2026-08-19)

Prompted by a direct question: "if an Account Executive sets a temporary password for a grower or retailer, are they prompted to set a new one at login?" The honest answer, found by reading the actual code rather than assuming: **no, not reliably.** `User.mustChangePassword` (§48) was only ever checked inside `lib/cart-auth.ts`'s license-first flow — itself only reachable via a public shared menu/collection link, and only for retailers. The main `/login` page every role signs in through — Admin included — never read this field at all. A grower/processor whose AE reset their password, or a retailer who just used `/login` directly instead of a cart link, could sign in with the old temporary password indefinitely. Confirmed by reading `auth.ts`'s `authorize()` directly: it checks email+password and nothing else.

A related audit turned up the same gap in three more places that hand someone a password without flagging it at all: `createAssistedSellerAccount` (AE/Admin quick-creating a grower/processor, §35), `createBudtenderAccount` (a Retailer creating Budtender staff, §33), and `createStaffAccount` (Admin creating Broker/Sales Rep staff, §21). None of these set `mustChangePassword` — the flag simply didn't exist for them. Also found: **`/login` had no rate limiting anywhere** — a bigger, more universal version of the gap already fixed once for the cart-checkout-only flow (§52), except this one covers every account on the platform including Admin. And: **no self-service "change my own password" existed anywhere** — the only ways a password ever changed were an Admin/AE reset or the retailer-only cart link.

The human's own framing shaped the fix: "think of how other software deal with passwords... will the system send an email to reset it?" — confirmed building against **Resend** (chosen over SendGrid/Postmark/a permanent stub) after being told plainly that a real forgot-password flow needs a real, working send, unlike the AI-listing/marketing-SMS features which have a genuinely useful degraded mode when unconfigured.

**A. `lib/email.ts` (new) — Resend integration**, gated behind `RESEND_API_KEY` exactly like `ANTHROPIC_API_KEY` gates `lib/ai-listing.ts`: unset locally by default, `/forgot-password` shows an honest "not set up yet — ask an Admin" message instead of pretending to send. `RESEND_FROM_ADDRESS` defaults to Resend's own sandbox sender; a real sending domain needs verifying before real users rely on this.

**B. `PasswordResetToken` model (new, hand-written migration per the standing rule from §25's incident)** — single-use, 1-hour-expiry tokens stored as a sha256 hash (not bcrypt — a reset link only needs exact-match lookup, not brute-force resistance the way a password does). `lib/password-reset.ts`: `requestPasswordReset` never reveals whether an email matched a real account (same "don't help enumerate accounts" posture `authorize()`'s uniform error message already has), `validateResetToken`/`completePasswordReset` handle the actual reset. New pages: `/forgot-password` (request), `/reset-password?token=...` (set new password, auto-signs in on success, same `redirectTo` pattern signup/login already use). Both rate-limited with the existing Postgres-backed limiter (§30/§52) — 5/min for the request (anonymous, spammable), matching the app's established tiers.

**C. The forced-change gate now actually applies everywhere.** `lib/account-management.ts` gained `userMustChangePassword` (a **live DB read**, deliberately not trusted from the JWT/session — this app uses stateless JWT sessions with no server-side revocation, so a live check is the one piece of "an Admin's reset takes effect even on an existing open session" this app can do without a bigger session-architecture change) and `setPasswordAfterForcedChange`. `lib/dal.ts`'s `requireRole`/`requirePosAccess`/`requireAuth` — the three functions every protected page in this app already calls — now check this first, before the non-circumvent gate, redirecting to a new `/account/change-password` page (mirrors `/agreements/non-circumvent`'s own shape, including a "Sign out instead" escape hatch reusing the same shared `logout` action). No role is exempt, including Admin — unlike the non-circumvent agreement, there's no "not a party to this" carve-out for a forced password change. The three previously-silent account-creation paths (A/B/C above) now all set `mustChangePassword: true`, so this is finally consistent across every "someone else picked this password" path in the app.

**D. Self-service "change my own password"** — `lib/account-management.ts`'s `changeOwnPassword` (requires the current password, unlike the forced-change path) + a shared `components/shared/change-password-panel.tsx`, added to all seven role settings pages: the five that already existed (grower/processor/retailer/broker/transporter) plus two new ones (`/admin/settings`, `/sales/settings` — neither role had a settings page before this; both NAV arrays across all 26 admin/sales route files got a "Settings" entry added via a scripted substitution, matching the byte-identical-nav-array pattern already established in §42-C). **Budtender is the one role that still has no self-service path** — they only ever land on `/retailer/pos` with a deliberately minimal single-link nav (§33), and adding a settings surface there would be a bigger, more deliberate UX change than this pass intended; they still get the forced-change gate (`requirePosAccess` is covered), just not voluntary self-service. Flagged here rather than silently left inconsistent.

**Verified live, end to end, against the real database** (not just `tsc`/`build`/`test`, though all three are clean — 31/31, one unrelated pre-existing flaky Prisma-connection-pool test per §31's own documented note, confirmed by immediate retry): created a real test grower with `mustChangePassword: true`, signed in with the temporary password, confirmed visiting `/grower` redirected to `/account/change-password` (not the portal), set a new password, confirmed `mustChangePassword` flipped to `false` in the database and the new password hash verified correctly, confirmed the redirect chain correctly continued through the non-circumvent gate and landed on the real `/grower` portal afterward. Separately exercised the self-service panel on `/grower/settings`: wrong current password correctly rejected with "Current password is incorrect," correct current password correctly updated the hash (verified via direct bcrypt comparison). Confirmed `/forgot-password` shows the honest "not configured" message in this environment (no `RESEND_API_KEY` set locally). All test data deleted afterward.

**Not done, flagged rather than silently skipped**: true session invalidation on password reset (an already-issued JWT keeps working until its ~30-day expiry regardless of a reset — the live `mustChangePassword` check closes the practical gap this creates for that one specific field, but a stolen session token itself isn't revoked). Doing this properly needs either a DB-backed session strategy (a bigger architecture change than this pass) or hand-rolling JWT invalidation inside NextAuth's callbacks (judged too easy to get subtly wrong and risk breaking every session in the app for a lower-urgency hardening item) — deliberately not attempted here.

## 56. Full negotiation-to-delivery workflow audit — real notification gaps found and fixed (added 2026-08-19)

Directly requested: "run an audit to assure that everything will work as expected... all parties should receive notification of accept, counter or deny including the account rep... give us a extensive workflow." Traced every write path from `getOrCreateThread`/`addOfferRound` (`lib/offers.ts`) through `Deal` creation, invoice, transporter assignment, shipment status, and product accept/reject (`lib/commission.ts`, `lib/shipments.ts`), cross-checking every `notify()` call against who actually receives one. Full findings delivered as a published Artifact; the concrete code fixes:

**A. The Account Executive was only ever notified on one of many outcomes.** `addOfferRound` previously called the assigned rep's `notify()` only when a deal **accepted with non-cash terms** — a plain cash accept, any counter-offer, and any reject were all silent to the AE, even though they're the one who connected the parties and the human explicitly named this exact expectation. Fixed: a new `notifyAssignedRep` helper fires on all three outcomes (`reject`, `counter`, `accept`) whenever the listing has `createdBySalesRepId` set — a cash accept now gets a plain "a deal closed" message, non-cash still gets its own more pointed wording (the original reason this existed at all).

**B. `lib/shipments.ts` — every function in this file sent zero notifications, to anyone, before this.** The entire post-acceptance fulfillment lifecycle (invoice uploaded, invoice accepted + transporter assigned, schedule proposed, schedule accepted, shipment status advanced) was completely silent. The single most consequential instance: **"delivered" is the exact status `acceptProduct`/`rejectProduct` (`lib/commission.ts`) require before a retailer can act** — without a notification, a retailer had no way to know it was time to accept/reject except manually reloading the deal page. Fixed, function by function: `uploadInvoice` → notifies the retailer; `acceptInvoiceAndAssignTransporter` → notifies the seller (invoice accepted) *and* the transporter (a shipment they previously had no way to learn about except reloading `/transporter`); `proposeShipmentSchedule`/`acceptShipmentSchedule` → notify both sides / the transporter; `advanceShipmentStatus` → notifies both grower and retailer on every step, with `delivered` getting its own explicit "review it and accept or reject" wording; `setTransportFee` → notifies whichever party actually owes it. Deliberately not wired (lower-urgency, tracked-not-processed administrative events nothing else in the app blocks on): `uploadTransportInvoice`, `markTransportFeePaid`.

**C. A real, separate bug caught in the same pass:** `acceptProduct` (`lib/commission.ts`) sent the seller a **second, near-duplicate "product_accepted" notification** whenever no broker had set commission on the deal — the code's fallback (`deal.commission?.setByBrokerId ?? deal.sellerId`) was written to mean "if there's no broker to tell, don't send this second one," but the `?? deal.sellerId` actually re-notified the seller with slightly different wording instead of skipping. Fixed to only fire when a broker actually set commission. The same message also had a literal `$${deal.commission.rate}%` typo (a dollar sign in front of a percentage) — fixed to just `${rate}%`.

**D. A second silent-recipient gap found the same way: the Account Executive's own `SalesRepCommission`** (computed the moment product is accepted, per §18) **was never announced to them.** Fixed — reuses the already-computed amount from the same function (no duplicate query) to notify the rep exactly how much they just earned.

**Deliberately not changed, confirmed as correct-by-design rather than a gap**: the Broker dashboard gets nothing pushed to it — per CLAUDE.md's own long-standing documented posture ("Brokers still get nothing pushed to them outside the app," §8/§10), a broker's real-time, platform-wide dashboard view *is* their notification mechanism; and a cart order's several line items each notify independently per-thread rather than one consolidated "your cart order was responded to" message, matching §41's own "no second state machine" discipline — flagged as a minor UX rough edge for a large multi-product cart, not a functional bug.

**Verified with a real one-off integration test** (written for this pass, run once against the real database via the same real production functions, then deleted — not kept as a permanent suite addition, same convention as §43's own one-off verification): a real accept → invoice-accept-and-assign-transporter → three shipment-status advances (picked up / in transit / delivered) correctly produced, in order: an `offer_accepted` notification to the AE on a plain cash deal (previously would have been zero), a `shipment_assigned` notification to the transporter, and three `shipment_status` notifications each to both grower and retailer, with the delivered one carrying the "review it and accept or reject" wording. All notification text inspected directly against the database, not just asserted programmatically. `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) all clean. All test accounts/listings/threads/deals/shipments/notifications deleted afterward.

## 57. CI pipeline, driver tracking, address geocoding, Broker search, and a Vonage-powered lead-texting system with real opt-out handling (added 2026-08-21)

A large batch from one conversation — a general "what else would help" question, several concrete follow-ups, then a real Vonage integration built live with the human's own API credentials.

**A. GitHub Actions CI** (`.github/workflows/ci.yml`) — typecheck, lint, the real Prisma-backed test suite, and a production build now run on every push/PR to `main`, not just when someone remembers to run them by hand. The test suite hits the real dev Postgres database directly (genuine integration tests, not mocked) — same tradeoff this already accepted for local runs, just now automatic. Needs `DATABASE_URL`/`DIRECT_URL` repo secrets added by hand in GitHub's UI — no `gh` CLI available in this environment to script that step, so it's the one manual setup action left for the human.

**B. Two notifications flagged-but-deferred in §56 are now wired up**: `uploadTransportInvoice` and `markTransportFeePaid` (`lib/shipments.ts`) now notify whichever party owes the transport fee, matching the pattern every other shipment event already got.

**C. Broker dashboard search** — the one dashboard §54 explicitly left out (`BrokerDashboard` was an async Server Component fetching its own data, a different shape than the AE/Admin ones that already got search). Split into `broker-dashboard.tsx` (still the Server Component, unchanged data-fetching) + a new `broker-dashboard-view.tsx` (client component, holds the search state, renders everything). Same plain client-side substring filter as every other search box in this app — arguably needed it most, since it's the single largest, real-identity, platform-wide list.

**D. Address geocoding** (`lib/geocoding.ts`, `GOOGLE_MAPS_API_KEY`) — closes a documented, long-standing gap: grower/processor/retailer signup addresses were free text with zero validation. Now, right after signup creates the account, an `after()` call (off the critical path, same posture as METRC submission) geocodes the address via Google's Geocoding API and stores `User.addressLat/addressLng/geocodedAt`. Same "not configured" honest fallback as every other optional external API here — without the key, addresses stay exactly as free text as before, no error, no behavior change.

**E. Driver tracking** (`Shipment.driverName/driverPhone`, opt-in live location) — a real, previously-nonexistent gap: this app only ever recorded which Transporter *company* account was assigned to a shipment, never an individual driver. Now the transporter can set a driver name/phone (free text, same pattern as pickup/delivery instructions), and — while a shipment is `picked_up` or `in_transit` — opt in to live location sharing. The driver's own phone (already logged into this PWA) reports position via the browser's own Geolocation API (`components/transporter/location-sharing-toggle.tsx`, `navigator.geolocation.watchPosition`) to `lib/shipments.ts`'s `reportShipmentLocation`. Deliberately **not** a third-party fleet-tracking service or Google's enterprise Fleet Engine product — those were considered and explicitly set aside in favor of the simplest thing that actually answers "where's my shipment right now" without a new vendor contract.

- **Privacy-conscious by construction, not just by policy**: sharing is off by default, requires an explicit opt-in click, and `advanceShipmentStatus` automatically clears `locationSharingEnabled`/`lastLat`/`lastLng`/`lastLocationAt` the instant a shipment reaches `delivered` — same as if the driver had manually turned sharing off. No lingering final point after the load is done.
- **Display**: `components/deal/shipment-timeline.tsx` (shared by the grower's and retailer's deal panels, plus a compact one-liner on the Broker dashboard) shows driver name/phone and, if a live point exists, either a real embedded map (Google Static Maps API — a plain `<img>`, not the full interactive JS SDK, since one point doesn't need pan/zoom) when `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` is set, or raw coordinates + "updated N min ago" as an honest fallback when it isn't. Google Maps API keys are designed to be exposed client-side (restricted by HTTP referrer in the Cloud console, not kept secret) — this is the one credential in this app that's *supposed* to be visible in the browser, unlike every other key here.
- Both new deal-panel client components' local `DealShipment` types were extended explicitly (not just relying on the broader Prisma `include` already returning these fields) — same discipline §21's audit already established, even though this specific addition isn't a new privacy leak (driver/location data is meant to be visible to both parties, unlike the `Deal.sellerId` case §21 actually fixed).

**F. Real Vonage SMS integration for the Lead Directory** — the human had a direct question about whether Vonage has relevant APIs (yes: Messages API for SMS, Voice API for calling), picked "click-to-call" as the priority scope, then supplied real Vonage credentials and Vonage's own quickstart SDK sample. Built as `lib/vonage-sms.ts` (`@vonage/server-sdk` + `@vonage/messages`), gated behind `VONAGE_API_KEY`/`VONAGE_API_SECRET`/`VONAGE_FROM_NUMBER` with the same honest "not configured" fallback convention as every other optional API here. A new "Text" button + compose box on each lead card (`components/leads/leads-manager.tsx`) sends via `sendSmsToLead`, which logs the message onto the lead's own `LeadActivityLog` timeline — same place call logs and notes already go.

**This is explicitly NOT the same posture as §29's Twilio marketing-SMS scaffolding, which is *permanently* stub-only.** That one texts end retail consumers, and this app isn't the system of record for their TCPA consent — a real send was deliberately never wired up. This one is B2B outreach to a licensed business's own listed contact number, a different regulatory bucket — flagged to the human directly before building (not silently assumed equivalent), who confirmed proceeding.

**Real opt-out handling, not just a send-and-forget** — the human raised this unprompted: "if I text the list and someone says wrong number, stop, do not call, we would want the directory to update that and not allow the system to contact that number again." Built as `lib/vonage-inbound.ts` + `/api/vonage/inbound-sms` (the webhook URL to configure in the Vonage dashboard once deployed):
- A reply containing "wrong number" (or close variants) blocks *that specific number* (`LeadPhoneNumber.blocked`/`blockedReason`) — the business itself may still be reachable via another number on file.
- A reply containing "stop"/"do not call"/"unsubscribe" blocks the number **and** flips the whole `Lead.disposition` to `DNC` — the real, already-existing platform-wide suppression signal.
- `sendSmsToLead` checks both conditions — the lead's `DNC` disposition and the primary number's `blocked` flag — before ever calling Vonage, so there's no path (single text, bulk send, a future AI-composer) that can accidentally re-contact a suppressed lead.
- Every inbound reply — recognized as an opt-out or not — gets logged to the lead's activity timeline, so a rep sees it even when nothing gets auto-blocked.
- **Known, stated gap**: Vonage's basic inbound webhook isn't signature-verified (this endpoint has no session and can't confirm a POST genuinely came from Vonage). Worst case if someone found the URL and spoofed a payload: a real number gets incorrectly blocked — a nuisance, reversible by hand — not a data leak, since this endpoint only ever writes a block flag and never reads/returns lead data. Flagged rather than silently assumed safe.
- **Verified with a real integration test** (written for this pass, run against the real database, then deleted): confirmed "wrong number" blocks only the specific number without DNC-ing the lead, "STOP" blocks the number **and** sets `DNC`, an ordinary reply blocks nothing, and `sendSmsToLead` correctly refuses to send once a number is blocked.

**G. Multi-phone-number support for the Lead Directory** — the human's own follow-up, same conversation: "the lead directory only has place for only one phone number, can we increase this for up to 5 numbers total and an option to set one of them by click as the main number and click to set the other as 2nd choice along with a name for each number, sometimes the company has different sales reps." New `LeadPhoneNumber` model (`leadId`, `phone`, `name`, `sortOrder`) — up to 5 per lead, ordered rather than flagged: sortOrder 0 is "main," 1 is "2nd choice," clicking "Set main"/"Set 2nd" reorders the list rather than toggling a separate boolean per slot.

- **`Lead.phone` (the old single field) stays in sync with whichever number is at sortOrder 0** (`lib/leads.ts`'s `syncLeadPhoneField`, called after every add/edit/remove/reorder) — deliberately, so every already-existing phone-dependent code path (search, the `tel:` click-to-call link, `sendSmsToLead`, the Power Dialer) keeps working completely unchanged. None of them were rewritten to look at the new table; they just keep reading the one field that's now kept current automatically.
- **Backfilled** (`scripts/backfill-lead-phone-numbers.mjs`, idempotent) — every existing lead's old `phone`/`altPhone` became real `LeadPhoneNumber` rows (467 rows created across 336 leads) so the new UI has real data to show immediately, not an empty list for the whole existing directory.
- New `components/leads/lead-phone-numbers-editor.tsx` — a collapsible "Phone numbers (N)" section per lead card: add (up to 5), edit, remove, set-main, set-2nd. A blocked number (see F above) shows its block reason struck-through and can't be silently reused.
- **Verified with a real integration test**: add → reorder (set a 3rd number as main, confirmed `Lead.phone` updated) → remove the new main (confirmed fallback to the next in line, confirmed no gap left in `sortOrder`) → confirmed a 6th number is rejected. Also verified live in the browser: added a second named number, clicked "Set main," confirmed both the phone-numbers list *and* the card's top-level `tel:` link updated to the new main number without a page reload.

**H. Power Dialer auto-advance** — the human's own follow-up: "we want the power dialer to call one number and after that call is dispositioned the next number is automatically dialed." The dialer already auto-advanced its *queue position* after a disposition; what it didn't do was actually trigger the next call. `handleDisposition` (`components/leads/power-dialer.tsx`) now attempts `window.location.href = <next lead's tel: link>` immediately after advancing. Stated honestly as best-effort in the code comment: a `tel:` navigation reliably fires only when triggered synchronously off a real user gesture, and some mobile browsers may silently ignore one that happens after an intervening `await` (the disposition save) — when that happens, the next lead's own big call button is still right there to tap manually, so the flow never breaks, it just occasionally needs one manual tap instead of zero.

**Also corrected in this pass**: both Marketing Suite pages (`/sales/marketing`, `/admin/marketing`) had stale copy claiming "Power Dialer, callback calendar, and dashboard are a planned follow-up" — all three were already fully built and wired (confirmed by reading the actual code, not assumed) from earlier work this session; the copy just never got updated. Fixed to describe what's actually there.

**Confirmed already correct, not touched**: the public signup form's license-number auto-populate (business name, street address, city, zip, from the real Michigan CRA registry, with auto-approval on an Active match) — the human asked directly whether this existed; re-verified by reading the live signup form code rather than assuming, confirmed it's been working since earlier in the build.

**Verified:** `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31, one retry needed for the same pre-existing flaky Prisma-connection-pool test documented in §31 — confirmed not a regression) all clean. All test accounts/leads/phone numbers created during verification deleted afterward.

## 58. Power Dialer dials all of a lead's numbers before moving on, and real per-AE lead ownership — another confirmed reversal (added 2026-08-21)

Two follow-ups to §57, same conversation.

**A. Power Dialer sequencing.** With up to 5 numbers per lead now possible (§57-G), the human asked for the obvious next behavior: "dial one number first then move to the second number and so forth within the same lead before moving to the next lead." Previously the dialer's queue was purely lead-level — one call target per lead, straight to the next lead after disposition. `power-dialer.tsx` now tracks a second index (`phoneIndex`) alongside the lead index: `numbersFor(lead)` resolves a lead's dialable numbers (the new `LeadPhoneNumber` list, blocked ones excluded, sorted main-first — falling back to the legacy single `phone` field for the rare lead with no `LeadPhoneNumber` rows). `handleDisposition` only advances to the *next lead* once every number on the current one has been dialed and dispositioned; otherwise it stays on the same lead and moves to its next number, auto-dialing it the same best-effort way §57 already established. The header now shows "number 2 of 3 for this lead" whenever a lead has more than one number, and the contact name shown switches to that specific number's own name (e.g. "Sales rep Dana") when set.

**B. Real per-AE lead ownership — a confirmed reversal of a previously-confirmed decision.** The human asked directly: "can we set it so that the account representative calls only leads in the lead directory that are assigned to only that account rep?" This is the exact opposite of §20's own explicit, previously-confirmed choice ("every sales_rep and admin sees every lead across every list... the human explicitly chose platform-wide visibility over per-rep silos") — flagged back plainly before touching anything, since undoing a documented decision is exactly the class of thing this app's own governance posture says to confirm, not silently reverse. Also surfaced a real technical wrinkle in the same question: `Lead.assignedRepName` (existing) is free text carried over from the original CRM import — it was never a reliable link to a real logged-in AE account. Three options were laid out (real per-AE ownership claimed on first contact / match on the fragile free-text name / leave it alone); the human picked the first, matching the exact mechanism already proven out for grower/processor accounts.

- **New `Lead.assignedSalesRepId`** (real FK to `User`, distinct from the pre-existing free-text `assignedRepName`) — claimed automatically the first time an AE makes real contact with a lead: `lib/leads.ts`'s new `claimOrVerifyLeadAssignment` mirrors `lib/sales-actions.ts`'s `claimOrVerifySellerAssignment` exactly (same three outcomes: Admin always exempt, unclaimed → claim it, claimed by someone else → reject with "This lead is already being worked by another Account Executive," claimed by the same AE → no-op). Wired into `logLeadCall`, `setLeadDisposition`, and `sendSmsToLead` (`lib/vonage-sms.ts`) — the three real "contact" actions — never into a read path, since browsing/searching the list isn't contact.
- **`leadsForList`/`leadCountsByList`** now take an optional `actor` — for a `sales_rep`, both filter to `assignedSalesRepId IS NULL OR = actor.id`, so an unclaimed lead stays visible to every AE (nobody's blocked from working a fresh lead) and a claimed one only shows up for its owner. Admin's own calls (both pages already called these with no actor) stay completely unrestricted, unchanged — matching Admin's unrestricted reach everywhere else in this app.
- **Claim conflicts surface as a real, visible error, not a silent failure or an uncaught throw.** `setLeadDisposition`/`logLeadCall` now throw on a rejected claim; the action-layer wrappers (`sales/marketing/actions.ts`, mirrored in `admin/marketing/actions.ts` for type-shape consistency even though Admin's own claim check always passes) catch it and return `{ok: false, error}` instead of `void`. `LeadsManager` shows the error inline on the lead's own card; `PowerDialer` shows it via `window.alert` and — since this should only ever happen from a genuine race (two AEs contacting the same still-unclaimed lead in the same instant, given the dialer's queue is already built from a pre-filtered list) — just skips to the next call rather than getting stuck.
- **Verified with a real integration test** (written for this pass, run against the real database, then deleted): an unclaimed lead is visible to two different AEs; AE1 logging a call claims it; AE2 then no longer sees it in their own `leadsForList` while AE1 still does; AE2 attempting to disposition it directly is rejected with the expected error; AE1 continuing to work it succeeds with no conflict; Admin sees it throughout regardless of claim state.

**Verified:** `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) all clean. All test accounts/leads created during verification deleted afterward.

## 59. AI text-message creator for the Lead Directory — bulk personalized SMS, immediate or scheduled (added 2026-08-21)

The last piece of an earlier request (§57 built real Vonage texting and multi-phone-number support; the "AI text message creator" half was flagged as still outstanding). Full ask: draft one generic message, have AI customize it per lead, send to many leads at once, and optionally delay the send to a specific date/time — "if desired a message can be set to be sent a week from now."

**This app has no background-job infrastructure anywhere else** (§8's already-documented gap — no SMS/push/email queue, no cron). A message scheduled for a week out needs a real mechanism to fire without anyone signed in at that moment, so this introduces the app's first one: **Vercel Cron** (`vercel.json`), hitting `/api/cron/send-scheduled-messages`, authorized by a `CRON_SECRET` env var Vercel automatically attaches as a bearer token to its own cron calls (generated and set on Production via the CLI this session — the route simply skips the check when the var is unset, which is why it works unauthenticated in local dev). This is a genuinely new capability for the app, not a repurposing of an existing pattern — flagged here for that reason. **A real constraint discovered only at deploy time, not assumed up front**: the first deploy attempt used an hourly schedule (`0 * * * *`) and Vercel rejected it outright — this account is on the **Hobby plan, which caps cron at once per day**. Fixed to `0 13 * * *` (once daily, 13:00 UTC). Stated honestly in the compose UI itself rather than glossed over: a scheduled send is honored to within about a day, not a specific hour — perfectly fine for the actual "a week from now" use case that was asked for, materially less precise for a same-day "in 2 hours" schedule. Upgrading to Pro would unlock finer-grained cron if that precision is ever needed.

**Architecture — one send path, two triggers, same "draft only, human reviews before anything real happens" posture as `structureListingDraftsBulk` (§34).**
- `LeadMessageCampaign` + `LeadMessageCampaignItem` (new models, hand-written migration) — a campaign is a template + a scheduled time (`null` = as soon as possible) + one item per lead with its own final, human-reviewed text and its own send status (`pending`/`sent`/`failed`/`skipped_dnc`/`skipped_blocked`/`skipped_claimed`/`canceled`). Nothing is persisted until the reviewed array is explicitly submitted — the AI personalization step (`lib/lead-messaging.ts`'s `personalizeMessagesForLeads`) is a preview only, returned to the client for editing, same as the bulk listing importer's review table.
- `personalizeMessagesForLeads` calls Claude in chunks of 15 leads (bounding a single request's output tokens), asking for a lightly personalized version of the template per lead (contact name/company only — explicitly told not to invent any fact, price, or offer beyond what's already in the template). Never hard-fails: on a missing `ANTHROPIC_API_KEY` or any API error, it falls back to sending the literal template to everyone, flagged via a `personalized: false` note the compose UI surfaces honestly — the review step itself is the real safety net, since a fallback line just reads identically to the template and is obviously editable right there.
- **`processDueCampaigns`** is the one real send loop, reused by both triggers: (a) a best-effort call via `after()` immediately after an "as soon as possible" campaign is created, so a small campaign goes out without waiting for the next cron tick, and (b) the recurring once-daily cron, which is the durable backstop — it's what actually makes a future `scheduledFor` fire, and what finishes off a large immediate campaign that didn't complete in its first pass. Bounded to 40 messages per invocation (`MAX_MESSAGES_PER_RUN`) with a 150ms stagger between sends, oldest-due-campaign-first, so one huge campaign can't starve a smaller one scheduled right behind it and no single invocation risks a function timeout.
- **Reuses `sendSmsToLead` exactly as-is** (§57) for the actual send — same DNC/blocked-number checks, same claim-on-first-contact ownership check (an AE can't route around lead ownership by scheduling a text a week out any more than by texting directly), same activity-log entry. `processDueCampaigns` just classifies `sendSmsToLead`'s error string into the item's terminal status (`skipped_dnc`/`skipped_blocked`/`skipped_claimed`/`failed`) rather than reimplementing any of those checks.
- **`resolveSelectableLeads`** scopes which leads an AE can even select the same way `leadsForList` already does (unclaimed + their own claimed leads; Admin unrestricted), and drops DNC leads outright before they're ever offered as selectable — with counts surfaced to the compose UI so a shrunk selection is explained, not silently smaller.
- **`MAX_CAMPAIGN_LEADS = 150`** — the personalization preview runs synchronously inside a Server Action (unlike sending itself, which is bounded/batched), so an unbounded selection risks the request timing out. Enforced server-side, not just in the UI: a larger selection is rejected with a clear "split into more than one campaign" message rather than silently truncated.

**UI**: `/sales/marketing/campaigns/new` and `/admin/marketing/campaigns/new` (`components/leads/campaign-composer.tsx`, shared) — pick a list, search/select-all-filtered leads, write the template, "Preview & personalize," review/edit every line (with a Remove per row), then "Send as soon as possible" or "Schedule for a specific date & time" with quick +1 day/+3 days/+1 week/+2 weeks buttons directly answering the human's own "a week from now" framing. `/sales/marketing/campaigns` and `/admin/marketing/campaigns` (`components/leads/campaign-list.tsx`, shared) — history with per-item status counts and a Cancel action for anything still `scheduled`. Reachable via a "Message campaigns →" link on the existing Marketing suite pages rather than a new top-level `PortalShell` nav entry — Admin's nav is already at 8 items and was previously fixed once for overflowing at realistic desktop widths (§26); this avoids reopening that.

**A campaign targets one list at a time, by design** — switching list tabs mid-compose clears the current selection, stated plainly in the UI rather than silently dropped. A cross-list send isn't supported in this pass; scope cut for the same "start simple" reason used throughout this app's build history.

**A real bug caught during live verification, fixed on the spot**: `CampaignList`'s Cancel button called `cancelCampaignAction` and `revalidatePath`, but nothing told the already-rendered client tree to actually re-fetch — the exact same gap `CancelOrderButton` (§42D) hit for cart orders. Confirmed live: canceling correctly flipped the campaign and its items to `canceled` in the database immediately, but the page kept showing "Scheduled"/`pending` until a manual reload. Fixed with the identical `router.refresh()` call after the action resolves, same pattern as `CancelOrderButton`.

**Verified live, end to end, against the real database** (a disposable test AE account created directly rather than touching a real user's credentials — this environment's local dev points at the same Supabase database as production, so no separate staging DB exists): searched and selected 2 real test leads, wrote a template, clicked "Preview & personalize" — Claude returned genuinely distinct, contact-name-aware drafts per lead (`"Hey Alex, ... Verify Test Farms?"` / `"Hey Sam, ... Verify Test Processing?"`), confirming `ANTHROPIC_API_KEY` is in fact configured in this environment (contrary to earlier sections' notes about local dev having no key — apparently added at some point since). Clicked "In 1 week," confirmed the datetime input correctly computed 7 days out, submitted, and confirmed in the database a real `LeadMessageCampaign` with `status: "scheduled"`, the correct `scheduledFor`, and both items' exact personalized text. Confirmed the campaigns list page rendered it correctly, clicked Cancel, caught the stale-UI bug, fixed it, and reconfirmed live (no reload) that the status flipped to "Canceled" with `canceled: 2`. Separately tested the "send now" path: submitted a single-lead immediate campaign, confirmed `after()` fired `processDueCampaigns` automatically, and the item landed as `status: "failed"` with the honest `"Texting isn't configured yet..."` error (Vonage isn't set up in this environment) while the campaign itself correctly flipped to `status: "sent"` (meaning "finished processing," not "delivered" — a deliberate distinction from `sent` items' own per-item status). Hit `/api/cron/send-scheduled-messages` directly and confirmed it runs and returns a clean summary when `CRON_SECRET` is unset (local dev), and — by code review, since production's real secret wasn't exercised against a live anonymous request in this pass — that the same route rejects with 401 when the header doesn't match a configured secret. `npx tsc --noEmit`, `npm run build` (all new routes, including the cron route, confirmed present in the build's app-paths manifest), full `npm test` (31/31) all clean. All test campaigns/leads/the disposable AE account created during verification were deleted from the database afterward.

**Not built in this pass**: per-list or cross-list send limits beyond the flat 150-lead cap; a "preview as it'll actually render" step showing exactly what an SMS looks like on a phone; retry of a `failed` item without recreating the whole campaign. None of these were asked for — flagged in case they come up.

## 60. "Text back" — schedule a single-lead text right from the disposition row (added 2026-08-22)

A follow-up to §59: rather than always going through the multi-lead campaign composer, the human asked for a way to schedule one text to one lead right where the disposition/call-logging controls already live, with its own date/time picker.

**No new scheduling mechanism — a one-lead campaign is still just a campaign.** `lib/lead-messaging.ts`'s new `scheduleTextForLead(leadId, text, scheduledFor, actor)` is a thin wrapper around the same `createCampaign` §59 already built, with a single item. It shows up in the same `/sales/marketing/campaigns`/`/admin/marketing/campaigns` history, gets picked up by the same `processDueCampaigns` loop (best-effort immediate pass + the once-daily cron backstop), and goes through the same `sendSmsToLead` at send time — same DNC/blocked-number checks, same claim-on-first-contact ownership check. Nothing about scheduling itself needed a new ownership check, since that's already enforced where it actually matters (at send time inside `sendSmsToLead`, not at schedule time).

**UI**: a new **"Text back"** button (`components/leads/leads-manager.tsx`) sits directly next to the existing immediate "Text" button — same gating (only shown when Vonage is configured and the lead has a phone on file). Clicking it opens an inline form (message text + a `datetime-local` input, defaulting to an hour from now) with **Schedule**/**Cancel** buttons, matching the existing inline-form pattern already used for notes and immediate texts. Wired via a new optional `scheduleTextAction` prop (same "optional, gate on presence" convention as `sendTextAction`) into both marketing pages' action files.

**Verified live against the real database**: a disposable test AE + test lead (deleted afterward), logged in, opened the lead's "Text back" form, typed a message, picked a date a week out, submitted — confirmed in the database a real `LeadMessageCampaign` (`status: "scheduled"`, `personalized: false`, the correct `scheduledFor` in UTC) with exactly one `LeadMessageCampaignItem` (`status: "pending"`) pointing at the right lead with the exact typed text. Also added and ran a real one-off integration test (written for this pass, then deleted) covering the same path plus the empty-message rejection. `npx tsc --noEmit`, `npm run build`, full `npm test` (31/31) all clean.

## 61. Real marketing email to leads, alongside SMS — CAN-SPAM-compliant by construction (added 2026-08-22)

The human confirmed the Lead Directory's email addresses should actually be used for outreach, not just stored. Scoped via AskUserQuestion before building: mirror the AI text-campaign tool exactly (one template, AI personalizes per lead, review before anything sends, send now or schedule) rather than a bare one-off "email this lead" button, and use a real physical mailing address in the required CAN-SPAM footer (`2680 S Division Ave, Grand Rapids, MI 49507`) rather than a placeholder — the human supplied it directly.

**Architecture — one campaign system, two channels, not two parallel ones.** `LeadMessageCampaign` gained `channel` (`"sms"` default | `"email"`) and `subject` (email-only). `lib/lead-messaging.ts`'s `createCampaign`/`processDueCampaigns`/`resolveSelectableLeads`/`personalizeMessagesForLeads` all became channel-aware rather than duplicated — `processDueCampaigns` branches the actual send call (`sendSmsToLead` vs. the new `sendEmailToLead`) based on `campaign.channel`, everything else (the once-daily cron, the best-effort immediate `after()` pass, the bounded-batch send loop, the campaign history page) is unchanged and shared. Subject is a single value for the whole campaign, not AI-personalized per lead like the body — matches how a real email blast tool works, and keeps scope reasonable.

**`lib/lead-email.ts` (new)** mirrors `lib/vonage-sms.ts`'s `sendSmsToLead` as closely as possible: same claim-on-first-contact ownership check, same `isEmailConfigured()`-first check ordering (reuses the Resend account already wired up for password-reset email in `lib/email.ts`, whose private `sendEmail` helper is now exported for reuse), same "not configured" honest fallback, same `LeadActivityLog` entry on send.

**CAN-SPAM compliance is enforced server-side, not left to whoever writes a campaign.** Every email `sendEmailToLead` sends gets the mailing address and a real unsubscribe link appended to both the HTML and plain-text body automatically — this isn't part of the compose UI and can't be edited or omitted from a campaign. The unsubscribe link uses a deterministic per-lead HMAC token (keyed by the existing `NEXTAUTH_SECRET`, no new DB column) rather than a stored one-time token, so the same link keeps working across every future email to that lead. `Lead.emailUnsubscribedAt` (new) is the real opt-out signal — checked before every send, alongside the existing DNC disposition as a courtesy carryover from phone/SMS opt-outs (a DNC lead can't be emailed either, but DNC and email-unsubscribe are tracked as two distinct signals, not merged into one).

**`/unsubscribe/[leadId]`** (new, public, no `requireRole` — the same class of anonymous-visitor page as `/listing/[id]` and `/order/[slug]`) is a real click-to-confirm page, not a bare-GET mutation — a known issue with unsubscribe links otherwise: some corporate email security scanners "click" every link in a delivered email as a malware check, which would silently unsubscribe someone who never asked to be if the GET request itself did the write. The GET only ever renders a confirm button; `unsubscribeLead` (verifies the HMAC token, rejects a wrong one) only runs on that button's own click, via a real POST-shaped server action.

**UI**: `components/leads/campaign-composer.tsx` gained a Text/Email channel toggle (Email disabled with a "not set up" note when Resend isn't configured, same gating pattern as everywhere else) and a subject field shown only for Email. `components/leads/leads-manager.tsx` gained "Email"/"Email later" buttons next to the existing "Text"/"Text back", shown whenever the lead has an email on file — same optional-prop-gated pattern (`sendEmailAction`/`scheduleEmailAction`), reusing the exact inline-form UX already established for texting.

**A real, useful discovery made while writing verification tests for this, not by inspection alone**: `lib/email.ts` reads `RESEND_API_KEY` into a module-level constant at import time, not per-call — so a test can't dynamically toggle "configured" mid-run by mutating `process.env`. This is normal, correct Node.js behavior (and means updating the env var on a running deployment needs a redeploy/restart to take effect, worth knowing), but it meant two test assertions had to be rewritten to verify what's actually reachable in an environment with no real Resend key, rather than asserting a specific downstream error message that can't be reached without one — the same known limitation this app already lived with for testing `sendSmsToLead`'s Vonage-configured branches locally.

**Verified live against the real database and a real browser session** (disposable test AE + lead, deleted afterward): the campaign composer's Email option correctly shows disabled ("not set up") with no `RESEND_API_KEY` in this environment; the lead row's "Email"/"Email later" buttons render only when the lead has an email on file; "Email later" with a real subject/body/date created a genuine `LeadMessageCampaign` (`channel: "email"`, correct `subject`, correct `scheduledFor`, one pending item with the exact text); the immediate "Email" button correctly failed with the honest "Email isn't configured yet — add RESEND_API_KEY" message and left the compose form open rather than silently succeeding or crashing; the public `/unsubscribe/[leadId]` page rendered for an anonymous, logged-out request, and clicking "Unsubscribe me" with a real computed token correctly set `Lead.emailUnsubscribedAt` in the database. A dedicated one-off integration test (written for this pass, then deleted) additionally confirmed: `resolveSelectableLeads` excludes unsubscribed leads for channel `"email"` but not for `"sms"`; `scheduleEmailForLead` stores the subject and channel correctly; `processDueCampaigns` correctly routes an email-channel item through `sendEmailToLead`; `createCampaign` rejects an email campaign with no subject.

**A real, pre-existing test-suite flake resurfaced during this pass, unrelated to anything built here — worth recording since it cost significant verification time.** `npm test` intermittently failed on `pos.concurrency.test.ts`'s loyalty-accrual concurrency test (`expected length 8 but got 6`/similar) — this is the exact, already-documented flake from CLAUDE.md §31 ("an unrelated Prisma Client connection-pool ceiling in this dev environment"), not a regression: this session's changes never touch `pos.ts`/`customers.ts`/inventory/loyalty code at all. Made unusually severe this time by a second, completely unrelated Claude Code session actively running its own heavy `vitest` workload on the same physical machine concurrently (a different project entirely) — confirmed via `ps aux` and via a simple direct `prisma.user.count()` query completing in ~2 seconds (proving the database itself was healthy, not the bottleneck). Under that contention, even basic single-row `prisma.user.create()` calls and `afterEach` cleanup hooks timed out. Once that contention cleared, `npm test` came back to the same steady-state as before this session (29/31, only the one known-flaky test file affected) — not investigated further, and not something to "fix" here, since it predates and is unrelated to this work. `npx tsc --noEmit` and `npm run build` were clean throughout, unaffected by any of this (no database access at those stages).

## 62. Welcome email on signup — closes the gap a code audit surfaced (added 2026-08-22)

The human asked directly why a new signup doesn't get a welcome email "like every other company sends out." A full onboarding-workflow audit (delivered as an artifact the same session) confirmed this precisely: zero email existed anywhere in signup, and grepping the whole codebase for "welcome" turned up nothing but UI copy on the login page.

**Scoped deliberately to public self-signup only, not every account-creation path.** `lib/email.ts`'s new `sendWelcomeEmail(to, fullName, role)` is called from `signup/actions.ts` via `after()` (off the critical path — a slow or failed send never delays or blocks account creation, same posture as the existing geocoding call right below it) right after the new `User` row is created. Deliberately **not** wired into Admin/AE-assisted account creation (Budtender, quick-signed-up Grower/Processor/Retailer, Broker/Sales Rep staff accounts) — those flows already show/relay the login details directly to whoever's creating the account, who isn't necessarily the person checking the new account's inbox; sending "welcome, here's your account" there could tip off the wrong person that an account exists before its real owner ever sees it. Public self-signup is the one flow where the email address on the account is guaranteed to belong to whoever just typed it in.

**Same honest "not configured" fallback as every other Resend-backed feature** — reuses the account already wired up for password-reset email and lead marketing email, no new vendor or key. Content is simple and role-aware (uses `ROLE_LABELS`/`roleHome` to link straight into the right portal) — no HTML template system, matching `sendPasswordResetEmail`'s existing plain-but-real style rather than building new infrastructure for one email.

**Verified live**: a real integration test (written for this pass, then deleted) confirmed the function returns a clean failure with no RESEND_API_KEY set locally and doesn't crash on a single-word name. Live in the browser: completed a real retailer signup end to end — account created, session established, correctly redirected to the non-circumvention agreement gate — and confirmed via the dev server's own logs that the welcome-email attempt ran with zero errors (a silent no-op, exactly as designed, since nothing logs on that path). `npx tsc --noEmit`, `npm run build` both clean. Test account deleted afterward.

## 63. AI-assisted negotiation for Retailer↔Grower/Processor deals, with human-Broker mediation as the escape hatch (added 2026-08-22/23)

Raised while auditing the onboarding/contract-sending workflow, then specified in detail across several messages, with worked examples, before any code was written — the human wants two tiers of AI involvement in the core wholesale negotiation threads: (1) a default, always-on "AI suggests a compromise price" hint, purely advisory, a human still clicks Accept/Counter themselves; and (2) an opt-in mode where a party gives AI a real price range ("negotiate starting around $X, go up to $Y") and AI actually submits real counter-offers on their behalf, no click required — escalating to a human Broker to finish the deal if AI's approved range doesn't converge.

**Named explicitly as a real governance departure, not a routine feature.** Every AI feature elsewhere in this app (`lib/ai-listing.ts`, `lib/lead-messaging.ts`, `lib/lead-contact-lookup.ts`) is strictly draft-then-human-confirms; `lib/media-redaction.ts` is the one partial exception, and even that only ever degrades a photo, never fabricates or commits money/price state. Mode 2 here is the first time AI in this app autonomously writes real transaction/price state with no human confirming that specific output in the moment. Treated as such: the human's own repeated, detailed, explicit confirmation across multiple messages is the record of this decision, the same way §11/§17-18 already document other deliberate reversals of this app's default AI posture. Design work went through three parallel Explore agents (tracing the real `addOfferRound` state machine, the existing "AE acts as a party" authorization precedent, and Broker's actual — zero — negotiation-write access) followed by a Plan agent that pressure-tested the first draft and caught three real gaps (an opt-in that only reacted to *future* rounds and missed a counter already on the table; a range clamp with no independent monotonicity guard; a Broker suggestion that could have silently become an auto-accept) — all three fixed in the design before implementation, not shipped as bugs.

**The core mechanism: reuse `addOfferRound` as-is, never invent a second negotiation path.** `lib/ai-negotiation.ts`'s engine computes a decision, then calls the *exact same* `src/lib/offers.ts` `addOfferRound` every human negotiation already goes through, passing the real opted-in party's own `actorId`/`actorRole` — identical in spirit to `cancelCartOrder`'s "AE acts as the seller" precedent. Nothing about the anonymization boundary or the existing notification triggers needed to change; both already work correctly for any caller. Every AI-submitted round carries `OfferRound.aiGenerated: true` — visibly badged in the UI, never disguised as a human decision.

**Pacing — confirmed via AskUserQuestion: one visible step at a time, never an instant synchronous chain.** If both parties had AI active, a naive design (an AI round re-triggering the next AI step immediately) could resolve a deal before anyone saw an intermediate round, and creates a real concurrency race. Fixed by construction: `addOfferRound`'s end-of-function hook (`after(() => runAiNegotiationStep(...))`) only fires when the round just written is human-submitted (`action === "counter" && !aiGenerated`) — an AI-submitted round never re-arms it. A lightweight client poll (`components/ai-negotiation/ai-negotiation-poll.tsx`, mirrors `components/retailer/pos/live-refresh.tsx`'s `router.refresh()`-on-interval pattern) is what lets an AI-vs-AI thread keep advancing beyond that first automated reply — meaning it only progresses while someone actually has the thread page open to watch, the correct conservative default for a first version.

**Data model** (hand-written migration per the standing rule from the §25 incident, `prisma/migrations/20260823000000_ai_negotiation/`): `AiNegotiationMandate` (`threadId`, `partyRole` "retailer"|"seller", `partyId`, `openingPrice`, `walkAwayPrice`, `roundsUsed` @default(0), `maxRounds` @default(5), `active`, `@@unique([threadId, partyRole])` — one mandate per party per thread). `BrokerSuggestion` (`threadId`, `brokerId`, `suggestedPrice`, `message?`, `resolvedAt?`). `OfferThread` gained `needsBrokerMediation`/`mediationFlaggedAt`. `OfferRound` gained `aiGenerated`.

**`lib/ai-negotiation.ts` — the engine, safety rails first:**
- **Every price is computed by a pure, deterministic, unit-tested step function (`computeNextOffer`) — never an LLM-guessed number.** Opens at the mandate holder's own stated `openingPrice` on the first move (no concession math yet); every move after that closes half the remaining gap to the opposing side's last price. Hard-clamped to `[opening, walkAway]` **and** independently checked for monotonicity against the mandate's own last AI price — a bug in the clamp math alone can't produce a worse-than-last-time offer. The only LLM call in this file (`draftAiMessage`, same `claude-opus-5`/`output_config: {effort:"low"}` convention as `lib/ai-listing.ts`) drafts cosmetic message text only, wrapped in its own try/catch with a plain templated fallback — it can never affect the price.
- **`runAiNegotiationStep(threadId)`** — the core decision, called from both the human-triggered hook and the poll, safe to call redundantly. No-ops if the thread isn't open or already flagged for mediation. Determines whose turn it is from the last round; no-ops if that party has no active mandate. **Optimistic-lock claim** on `roundsUsed` (`updateMany({where:{id, roundsUsed: expected}, data:{roundsUsed:{increment:1}}}`, `count===0` → abort silently) — mirrors `lib/pos.ts`'s atomically-guarded stock decrement, the one real concurrency case (poll + reactive trigger landing at once). Independently rate-limited via the existing `lib/rate-limit.ts` (`"ai-negotiation-round"` scope). If the incoming counter is already at-or-better than the mandate's `walkAwayPrice`, submits `action:"accept"` directly. If exhausted (round cap hit, **or** the mandate is already sitting at its walk-away boundary with nowhere left to move) — **escalates**, never auto-declines: sets `needsBrokerMediation`/`mediationFlaggedAt`, deactivates the mandate, notifies the opted-in party and fans out to every Broker (mirrors the existing "notify all AEs on low stock" pattern from `lib/pos.ts`). Otherwise computes the next offer and submits it via `addOfferRound({..., aiGenerated:true})`, wrapped in try/catch that treats a human-accepted/rejected-at-the-same-instant race as a normal silent outcome.
- **`createOrUpdateMandate`** — validates direction (retailer's `walkAwayPrice >= openingPrice`, seller's `<=`), authorizes exactly like `addOfferRound` does, upserts on `[threadId, partyRole]`, then **immediately calls `runAiNegotiationStep`** — the Plan-agent-caught fix: opting in is usually a *reaction* to a counter already sitting on the table, and a reactive-only trigger would never fire for that first move.
- **`deactivateMandate`** ("take back control") and **`suggestedMidpoint`** — the pure, schema-free mode-1 advisory function (midpoint of the two most recent opposing rounds), rendered as a one-line hint, nothing ever written from it.

**Broker mediation is a proposal, not new negotiation-write authority.** The Broker had zero negotiation-write access before this (confirmed by code read — purely `allThreadsForBroker`/`allDealsForBroker` reads plus post-acceptance commission-setting) and granting it directly would be a bigger authority jump than real-world brokering norms. `lib/broker-mediation.ts`'s `suggestPrice` (authorization mirrors `setCommission` exactly — any broker, any flagged thread, no per-thread ownership check) just creates a `BrokerSuggestion`; it never touches `OfferThread`/`OfferRound`. The Broker dashboard's new "Needs mediation" section (`broker-dashboard-view.tsx`) shows the suggestion as a banner to both parties with a **"Counter at $X"** button that pre-fills the *existing* Accept/Counter/Reject form's price — the party still submits their own real round through the normal human path, exactly the correction the Plan agent's pressure-test forced (a suggestion alone can't just become an auto-resolved accept).

**UI**: `components/ai-negotiation/ai-mandate-panel.tsx` (opt-in form / status+rounds-used / "take back control", rendered in both `retailer-listing-detail.tsx` and `seller/listing-detail.tsx` right after the round history), `components/ai-negotiation/negotiation-controls.tsx` (wraps the ordinary form together with any open Broker suggestions), `components/ai-negotiation/ai-negotiation-poll.tsx`, `components/broker/broker-suggestion-form.tsx` (mirrors `commission-form.tsx`'s open/edit toggle). **While a party's own mandate is active, their manual Accept/Counter/Reject controls are hidden** (a Plan-agent-caught UX gap — without this, a party could manually counter at the same moment their own AI is mid-step) — `RespondForm` gained a `hidden` prop for this. The read-side anonymization boundary carries over exactly: `threadsForRetailer`/`threadForRetailerListing` only ever `include` the **retailer's own** mandate (`where: {partyRole:"retailer"}`), `threadsForSeller`/`threadsForListing` only the seller's — a party can see that AI is negotiating on their own behalf, never the other side's private price range. `allThreadsForBroker`/`threadsForSalesRep` include both, matching their existing real-identity trust tier.

**Two real bugs found and fixed during live browser verification, not caught by the automated tests (which covered engine correctness, not UI wiring):**
1. **The opt-in form's default "opening offer" was wired to `thread.rounds[...].price` (the last round on the table) instead of the listing's own posted price** — harmless-looking but wrong for the seller's case specifically: prefilling a seller's "opening ask" with the *retailer's* low counter makes no sense as a starting position. Fixed to default both roles' opening-offer field to `listing.pricePerUnit`.
2. **A Broker's "Counter at $X" click silently did nothing the first time.** `RespondForm`'s `useState(initialPrice != null ? "counter" : "idle")` only evaluates that initializer once, at mount — the component was already mounted (idle) by the time the suggestion banner's click changed `initialPrice` from `undefined` to a real number, so the mode never switched and the price `<input>`'s `defaultValue` (also mount-only for an uncontrolled input) never updated either. Fixed with a `useEffect` that switches `mode` to `"counter"` whenever `initialPrice` changes, plus a `key={initialPrice}` on the price input to force a fresh mount with the new `defaultValue`. Both fixes verified live: clicking "Counter at $800" now correctly opens the form pre-filled at $800, and submitting it produced a real `OfferRound` in the database.

**Scope cuts, explicit, matching this app's "start simple" convention elsewhere**: price-only negotiation (quantity/terms stay fixed at the thread's existing values); core Retailer↔Grower/Processor threads only, not cart orders/split-contracts/wanted-board responses; per-thread opt-in only, no account-level standing default; escalation is one-directional (AI → Broker), no un-escalating back to AI once a human is mediating.

**Verified**: 12 real integration tests (`lib/ai-negotiation.test.ts`, run against the real dev database) covering the pure step function's monotonicity/clamp guard, the mode-1 midpoint function, opt-in's immediate-move behavior, the reactive hook correctly resolving to the *other* party's mandate only, round-cap/walk-away exhaustion escalating cleanly with zero counter submitted, an 8-way concurrent `runAiNegotiationStep` race producing exactly one AI round, and the anonymization boundary on `threadsForRetailer`/`threadsForSeller`. Full live browser walkthrough end to end with three real disposable test accounts (grower/retailer/broker, deleted afterward): retailer counters → seller opts into AI → AI immediately counters back with a real Claude-drafted message at a clamped price → retailer counters again → AI's second move matches the hand-computed halfway-then-clamp math exactly → one more retailer counter pushes the mandate to its walk-away boundary → thread correctly flips to "needs mediation," seller's manual controls reappear → Broker's dashboard shows the flagged thread in real identity with full round history (AI badges included) → Broker submits a suggestion → retailer sees the banner, clicks "Counter at $800," form pre-fills correctly, submits, real round created. `npx tsc --noEmit` and `npm run build` both clean throughout.
